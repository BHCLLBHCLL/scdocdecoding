# 代码功能完整度与深度分析 + 下一步开发计划

> 依据：当前工作树（含 G1-01…G1-08、G2-01…G2-07 提交与未提交改动）实测。
> 环境：conda `occ`（Python 3.12.14 + pythonocc-core 7.9.3 + OCCT 7.9.3 + numpy 2.4.6 +
> vtk 9.6.1 + PyQt5 5.15.11）。**pytest 全量：67 passed / 1 skipped**（skip＝盒体拔模诚实报错）。

---

## 一、功能完整度（命令维度）

审计口径：125 个目录命令；`live_commands()` 113 个；分类＝**kernel**（handler 直接调用
内核 / 工具类）、**real**（工具/运行时真实行为）、**ui**（对话框/视图/样式等 UI 行为）、
**absent**（无 `_do_*_ handler，其中 safety.tab 按设计保留空）。

| 维度 | 总 | live | kernel | real / runtime | ui | absent |
| --- | --- | --- | --- | --- | --- | --- |
| 全目录 | 125 | 113 | 18+ | 30+ | 64（含真实对话框） | 12（8 草图图元 + 3 准备项 + safety 设计空壳） |
| Design 页 | 59 | 51 | 8 | 20 | 22 | 8 |
| Display / Measure / Repair | 11 / 3 / 6 | 11 / 3 / 6 | — / 2 / 3 | 9 / 1 / — | 2 / — / 3 | — |
| File（后台） | 12 | 14 | — | — | 14 | — |
| User 新增页 | 见下 | — | — | — | — | — |

### 1.1 已实现的波次（全部有提交）
* **M0–M6**：.sim→.scdoc 解析器、Ribbon 壳、树/属性/视口、测量、导出、打磨 —— 里程碑表已标完成。
* **M2–M5 建模层**：工具态机（Pull/Move/Fill/Combine/Split/Replace）、拖拽预览、草图 2D
  约束求解（9 种约束）、闭环拉伸、倒圆/倒角/阵列/镜像/抽壳/螺旋/修复、装配（组件/锚定/配合）、
  参数重建、增材（构建体/取向/支撑/点阵）、BOM/尺寸、脚本录制回放（11 种 OP）、
  **原生 .scdoc 写出（官方 SpaceClaim 2019 R3 实际打开成功）**、分面（STL/反转/光滑/简化/填孔）。
* **G1（对齐与快赢）**：目录-处理一致性守卫（G1-01）、包围体（G1-02）、草图网格（G1-03）、
  参数发布 JSON（G1-04）、轮廓边 FeatureEdges（G1-05）、静态剖切平面（G1-06）、
  打印预览（G1-07）、视口右键菜单（G1-08）—— **全部完成**。
* **G2（P0 收尾）**：崩溃恢复（G2-01）、插入组件/文件（G2-02）、视口注释（G2-03）、
  命名选择（G2-04）、自定义功能区持久化（G2-05）、投影（G2-06）、分面三件套（G2-07）——
  **全部完成**。

### 1.2 仍未落实（占比很小，但都集中在“草图”）
| 命令 | 缺口 |
| --- | --- |
| sketch.tangent / rect3 / circle3 / ellipse / spline / construction / offset / layout | 无 handler、不在 live（8 项） |
| prep.share / prep.mid / prep.small | 无 handler（其中 small 与 G5-02 修复组相关） |

---

## 二、深度分析（子系统的“能力厚度”）

### 2.1 建模内核（scdm/kernel.py，深度中高）
| 能力 | 厚度 | 主要限制 |
| --- | --- | --- |
| 布尔/变换/阵列/镜像/分割/缝合 | 高 | 功能正确；分割仅平面工具（平面切体） |
| 拉动/偏移（正取/对称）/ 替换 | 中 | 平面对面可用；非平面面仅正取；replace 平面近似 |
| 倒圆/倒角/抽壳 | 中高 | 全边集默认；抽壳单开口面 |
| 拔模 | **低** | 普通盒体直接抛 Standard_ConstructionError（实现非产品级） |
| 填孔 fill | 中 | 仅 BRepAlgoAPI_Defeaturing，无保留边/相切连续参数 |
| STEP/BREP/STL I/O | 高 | 全链路可用与往返测试 |
| 网格化 | 高 | 面级三角化 + VTK 拾取 |

### 2.2 .scdoc 写出（深度中；官方互操作已验证）
* 平面面实体（box/prism/草图拉伸）：**官方 SpaceClaim 打开成功**；自读报告校验 19/19。
* 缺口：非平面面 raise；缺 `rgb_color` 外观记录（官方打开无色）；曲边/曲面的 SAB 记录未验证。

### 2.3 草图（深度中低；**最大的功能空洞**）
* 数据模型：line/rect/circle/point + 9 种约束求解 + 闭环（仅 rect/折线）拉伸。✓
* 缺口：**无点击绘制**（仍为固定尺寸图元）；8 个图元命令缺；约束作用于固定点索引而非
  用户选中图元；无平面/面拾取进入草图、相机不贴法向；`sketch_outline` 只认矩形与折线。

### 2.4 工具交互（深度中低；“手感”的最大短板）
* Pull/Move 有拖拽预览 + 单击回退；Combine/Split/Replace/Fill 单击提交。✓
* **选项面板多数只展示不消费**：拉动「两侧/复制/到面」、移动「沿轴/到点/到面」、填充
  「保留边/相切连续」、替换「延伸目标面」、分割「保留两侧/仅切割面」、捕捉（栅格/端点/中点）。
* **边/顶点不可独立拾取**（整棵合并 line actor）；双击“选环边”实际走选体；过滤器对拾取无效。
* 测量仅两点距离、仅进状态栏（无角度/半径/边面距离、无视口标注、无 Esc 清除）。

### 2.5 视图/显示（深度中高）
* 着色+边/线框/透明、面/边/顶点/平面/轴显隐、草图渲染、预览体、轻量化 bbox、
  轮廓边（G1-05）、静态剖切（G1-06）、右键菜单（G1-08）。

### 2.6 会话/I/O/脚本（深度中高）
* 会话包(scdm)、STEP/STL、原生 scdoc（平面）、自动保存恢复、录制/回放（OPS 11 种）、
  命名选择/注释持久化。**局限**：scdoc 曲面+颜色（G6-01）、OPS 覆盖面（G5-05）。

### 2.7 测试深度（中高）
* pytest 67 passed/1 skipped；含 G1-01 目录-处理一致性守卫；每波回归 + interop 审计脚本。
* **缺口**：G3–G5 交互类（拾取/标注/拖拽）基本无法用无头 VTK 覆盖，需要人工走查清单。

---

## 三、下一步开发计划（建议顺序，与 DEV_PLAN 第 19/20 节一致，按“价值/难度”重排）

### 第一阶段：G3 交互基础（直接建模“手感”的地基，优先）
| ID | 内容 | 验收 | 预估 |
| --- | --- | --- | --- |
| G3-01 | **工具选项逐项生效**：拉动两侧/复制/到面；移动沿轴(手柄轴向)/到点/到面；填充保留边/相切（→ShapeFix 参数）；替换延伸目标面；分割保留两侧/仅切割面；捕捉（栅格/端点/中点→影响放置坐标） | 每个选项有可观察行为；kernel 参数有单测 | 3–5 天 |
| G3-02 | **边/顶点独立拾取 + 双击环边**：边/顶点改 per-edge actor（或 cell 拾取区分）；双击竖边选中 4 边环（相切连续）；状态栏点/边过滤器真正影响拾取 | 双击盒体竖边选中整环；关「边」过滤点不到边 | 3–5 天 |
| G3-03 | **测量补全**：点点/边边/面面距离、线线角度、圆柱面半径；视口数字标注 actor；Esc 清除 | 盒体对角线/两面夹角/圆柱半径有标注 | 3–4 天 |
| G3-04 | **左栏三页激活**：图层勾选→scene 显隐；群组页存/取选择集；视图页存相机/剖面一键回放 | 图层改显隐；群组可存；存视图一键回位 | 3–5 天 |

### 第二阶段：G4 草图与截面（最大空缺，用户价值最高）
* **G4-01 草图交互模式**：拾取基准面/平面进入 → 相机贴法向 → 网格/半透明 → 点击拖画
  line/rect/circle/point → Esc/右键退出回三维。**替换固定 10mm 图元路径**。（5–7 天）
* **G4-02 草图图元八件**：切线(线-圆相切拾取)、三点矩形、三点圆、椭圆、样条(插值)、
  构造线(不入闭环)、偏移(等距曲线)、布局；`sketch_outline`/`extrude_sketch` 支持
  椭圆/样条闭环。（8–12 天）
* **G4-03 截面模式完整**：可拖剖切面手柄（vtkPlaneWidget）、六向定位；截面轮廓可生成
  草图/拉伸。（5–8 天）
* **G4-04 约束选择化**：约束作用于用户选中图元/端点（替换现有固定点索引）；约束符号与
  尺寸标注显示。（3–5 天）

### 第三阶段：G5 建模命令补全（可与 G4 部分并行）
* **G5-01 生成组参数化**：阵列(线性间距/数量/方向+圆周轴/角度)、镜像(拾取基准面)、
  抽壳(厚度输入+开口面多选)、拔模(角度+中性面+方向)、螺旋(参数对话框)。
  **重点**：解决盒体拔模（先用正确的 MakeDraft 用法，失败则用“后处理锥面+布尔”替代实现）。3–5 天
* **G5-02 修复组真实现**：缺失面检测(开放边界环→补面)、小面识别(面积阈值+逐个确认)、
  实体化(壳体定向闭合判定) —— 替换现有“别名实现”。3–5 天
* **G5-03 装配组补全**：文件插入组件；移动三轴手柄；爆炸方向/距离可调；
  配合扩展(轴对齐/平行/距离约束)。5–8 天
* **G5-04 参数页扩展**：对已建几何(柱体/拉伸结果)提取参数并绑定重建。3–5 天
* **G5-05 剪贴/脚本收尾**：粘贴改点选放置；OPS 扩到全部已实现命令。2–3 天

### 第四阶段：G6 研究型（按需排期，不阻塞）
* G6-01 scdoc 曲面+颜色（官方打开带回归属验收）· G6-02 工程图 HLR 三视图 · 
  G6-03 共享拓扑/中面 · G6-04 自有渲染。

### 贯穿性工作（每波必做）
1. 每波全量 pytest + `scdm_interop_check.py` 审计 + 目录-处理一致性守卫（G1-01 常驻）。
2. G4/G5 完成后做端到端演练（新建→草图矩形→拉伸→抽壳→阵列→剖面→命名选择→录制→
   回放→存 scdm/scdoc→重开校验体积）。
3. 交互类功能（G3-02/03、G4）无头 VTK 覆盖不了的部分，登记人工走查清单并在提交信息说明。

### 附：当前未提交改动
工作树有未提交修改（`scdm_gui.py` +97、`left_panel.py` +53、`direct.py` +70、
README +2）与未跟踪文件（`laptop_3d_geom.stp/.x_t` 测试数据）。建议先核对/提交，
再进入 G3，避免与后续改动冲突。
