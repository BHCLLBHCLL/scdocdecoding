# R30 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R30）开 P175（特征族：孔/螺纹/成形）第一片。
> 交付：**攻丝孔族**（tap-drill 规则 + 非法参数拒绝 + 盲孔深度），3 项闭式体积测试通过。

## 改动：`scdm/kernel.py::hole_tapped`

```python
hole_tapped(solid, face, nominal, pitch, depth=None, origin=None)
# d_tap = nominal - pitch（公制规则）；螺纹本身是符号/元数据，不参与布尔
```

要点（都在测试里钉死）：
1. **按底孔直径切除**——`removed = pi*(d_tap/2)^2*depth`；用公称直径会“静默把每个攻丝孔做大 44%”（M6×1 实测 44%）；
2. **非法参数拒绝**：螺距为 0 / 为负 / 不小于公称直径 → `KernelError`（纪律 3：几何算子必须覆盖极端参数）；
3. **盲孔深度生效**：`depth=0.005` 时切除体积 = `pi*r_tap^2*0.005`（rel 2e-3）。

## 测试（新文件）

`tests/test_threads_p175.py`（3 项）：
①M6×1 通孔体积等于底孔闭式；②公称直径假设被显式排除（差 > 10%）；③非法螺纹三连拒绝；④盲孔深度闭式。

| 套件 | 结果 |
|---|---|
| `tests/test_threads_p175.py` | **3 passed** |

## 下一步（写入 R31）

P175 剩余：①**螺纹示意**（把 `pitch/nominal` 存进特征历史并在视口画牙型示意线）；②命令 `prep.hole`/脚本 op 接线；
③成形族（凸台/凹坑/百叶/敲落）按同一“闭式体积 + 非法参数”模板扩。