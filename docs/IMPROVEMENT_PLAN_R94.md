# 改进规划 R94（2026-09-12 立，R93 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R94。
> R93 交付见 `docs/ROUND_R93_20260912.md`（环面裁剪复活：0.54→0.045 s/面；缺口 1352→1328、环 290→269）。

## 1. R94 起点基线（R93 实测）

| 口径 | 实测 |
|---|---|
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 **335/72**、samplemodel2 **990/195**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 面数 | SampleModel1 109/109、samplemodel2 **1813/1813**、samplemodel3 111、samplemodel5 1288、samplemodel6 28、SampleModel4 **145/177**（ref 27 面未重建） |
| 裁剪白名单 | 柱面 / 球面 / **环面**（锥面＝原生崩溃，永久拒绝） |
| 型材库 | 7 种截面 / 15 条标准规格 |
| 测试 | 全量 CI 口径全绿（**678 passed**，659 条 `def test_`） |

## 2. R94 工作项（择优取一）

### P430 · ref 族 27 面：唯一的面数缺口，做"最后一轮攻坚"

- **背景**：27 个未重建面里，51 条失败边的曲线头**只有 `ref` 载荷**（R81 逐边计数）；R72/R81 的三条否证仍然成立。
  本轮换一个方向：**从已建面反推**——检查这些面的边界是否与某些已建面**共边**（同一条边被已建面用了两次以上），
  若是，则它们可能是"姊妹面"（同一曲面的另一侧/另一片），可用已建面的曲面**镜像/平移**得到；若否，给出否证。
- **验收**：姊妹面命中数可数；命中则面数上升且包围盒漂移 ≤0.01；未命中给出否证表。**成本**：1 天。

### P431 · 尺寸链 SVG/DXF 端到端断言

- **动作**：给 `write_dxf`/`svg_sheet` 加一条端到端测试：同一链的两条标注（±worst/±rss）与链线都出现在导出文本里。
- **验收**：两个导出各断言两条公差文字与链线文字；不改几何。**成本**：0.5 天。

### P432 · 每轮维护（必做）｜P433 · 后续候选

- P432 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P433 候选：装配爆炸动画帧、性能基线复测（本轮导入路径又一次变动）、GProp 闸门的缓存化。

## 3. 建议顺序

`P431（快）→ P430（攻坚）→ P432（收尾）`

## 4. 纪律（延续）

1–89 条延续。
