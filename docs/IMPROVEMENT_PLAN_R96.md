# 改进规划 R96（2026-09-12 立，R95 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R96。
> R95 交付见 `docs/ROUND_R95_20260912.md`（ref 族 27 面逐面否证 + `ref_no_curve` 口径与恒等式）。

## 1. R96 起点基线（R95 实测）

| 口径 | 实测 |
|---|---|
| 面数 | 5/6 样例与声明一致；SampleModel4 **145/177**（ref 族 27 面：6 无边界 + 21 曲线不全，**数据不在本 part**） |
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 **335/72**、samplemodel2 **990/195**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 导入耗时（R94 口径） | 1.47 / 1.48 / 7.57 / 0.11 / 2.86 / 0.10 s |
| 性能基线 | `docs/PERF_BASELINE.json` 是 **R71** 落的盘（建型/细分/网格/样例加载），此后导入路径大改 |
| 测试 | 全量 CI 口径全绿；`def test_` 扫描 6xx 条 |

## 2. R96 工作项：P433 性能基线复测

- **动作**：用 `tools/perf_baseline.py` 复测并落盘（工作负载：20×20×2 板 + 5×5 孔、曲面细分、面网格、包围盒+属性、官方样例加载），
  与 R71 基线逐行对比；若某一行回退超过 **20%**，定位到具体改动（裁剪/缝合/解码）并给出处置或明确记录。
- **验收**：`docs/PERF_BASELINE.json` 更新并提交；对比表写明每行的 R71 → R96 变化；
  若有回退，附**归因实验**（例如临时关闭裁剪策略重测同一工作负载）。**成本**：0.5 天。

## 3. P434 · 每轮维护（必做）｜P435 · 后续候选

- P434 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P435 候选：装配爆炸动画帧、GProp 闸门缓存化、`rep.small` 之外的产品面收尾。

## 4. 纪律（延续）

1–89 条延续。R95 的教训值得写进注释就写：**模块级计数器必须在每次导入时重置**（同一个坑已出现两次）。
