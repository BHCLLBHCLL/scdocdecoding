# 改进规划 R26（2026-09-12 立，R25 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R26。
> R25 交付见 `docs/ROUND_R25_20260912.md`（部件显隐/隔离/提示的查看器接口 + offscreen 测试）。

## 1. R26 起点基线

| 口径 | 实测 |
|---|---|
| 除 ref 族外 | 6/6 精确 |
| 层级链路 | 数据（`import_report.hierarchy`）→ 原语（groups/visibility/isolate）→ **查看器接口**（R25）→ 待接**树控件** |
| 测试 | `test_lightweight_gui.py` 9 项、`test_import_fidelity.py` 25 项 |
| 性能 | 导入 0.06–6.4 s |

## 2. R26 工作项（P151–P156）

### P151 · 树控件接线（最高优先，P145 收尾）

- **动作**：`scdm/gui/left_panel.py` 为 `imported` 分组渲染节点 + 勾选框 + 右键“隔离/全部显示”；
  事件 → `set_import_group_visible` / `isolate_import_group`；节点提示取 `show_import_group_hints()`。
- **验收**：offscreen 测试覆盖“勾选→视口可见数变化”与“右键隔离”；samplemodel2 的 25 部件在树里逐组可切换；几何恒等。
- **成本**：1–1.5 天。

### P152 · 大模型 GUI 可用性（积压十四轮）

- **动作**：导入 → 视口构建 → 首次渲染耗时/内存实测（samplemodel2/5），必要时按体/视锥懒装配。
- **验收**：< 20 s 可交互 + 帧率基线。**成本**：1–1.5 天。

### P153 · 特征族与梁焊件｜P154 · 图面尺寸第二批（多选/吸附/undo）｜P155 · 可选：`ref` 外部曲面库否证实验

### P156 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P151 -> P152 -> P153/P154 -> P155（可选）`（P156 收尾）

## 4. 纪律（延续 + R26 新增）

1–44 条延续。
45. **新增**：控件接线要“一次只接一条链路”并在提交信息里写清“本层已测 / 待接层”（本轮查看器接口已测、树控件待接），
    让每轮都有可独立验收的产物，而不是“半截 GUI”。