# 改进规划 R21（2026-09-12 立，R20 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R21。
> R20 交付见 `docs/ROUND_R20_20260912.md`（SAT/SAB 4052 条 1:1；`ref N` 再否两假设）。

## 1. R21 起点基线

| 口径 | 实测 |
|---|---|
| 官方库导入 | 4/6 精确；samplemodel2 1810；SampleModel4 143 |
| 切分 | 0 |
| 唯一根因 | `ref` 间接引用（53 个面：samplemodel2 1 + SampleModel4 52 面族中的可见面） |
| 已否证 | ①实体序号（SAT 与 SAB 同坐标系，实测随机 kind）②曲面表序号（0/52，N 超表长）③曲线级 `exactsur` 簇 ④曲面共享（4/52） |

## 2. R21 工作项（P122–P127）

### P122 · 实体 kind 直方图 → 找 `ref` 表（最高优先，可证伪的小步）

- **动作**：统计 4052 个实体的 kind 计数；筛出“未分类 + 计数与引用规模吻合（52/542 量级）”的类型；
  若有，解析其记录形态（是否含 52/542 个 int），验证“N→实体”映射；命中即接回 `_bspline_surfaces`。
- **验收**：`--strict` 退出 0；若证明表不在本 part，则改口径验收：**除 `ref` 族外 6/6 精确**，且 `ref` 族在 `import_warnings` 里如实计数。
- **成本**：1–2 天。

### P123 · 官方层级交互（积压九轮）｜P124 · 大模型 GUI（积压九轮）｜P125 · 特征族/梁焊件

### P126 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P122 -> P123 -> P124 -> P125`（P126 收尾）。P122 若判为“数据不在本 part”，立即转 P123/P124，把 `ref` 族写进口径与告警。

## 4. 纪律（延续 + R21 新增）

1–37 条延续。
38. **新增**：深水区探索每轮必须**把否证写进轮次文档的表格**（假设 / 实测 / 结论），并同步更新“已否证清单”；
    本轮起该清单已到五条，是防止重复投入的唯一依据。
39. **新增**：当某类残量被证明“数据不在本 part”时，**允许**把它从“必须归零”改为“如实上报 + 口径写清”，
    但必须同时给出“除该类外 6/6 精确”的验收，避免用口径逃避工程量。