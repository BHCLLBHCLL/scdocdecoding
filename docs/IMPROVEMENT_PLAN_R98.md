# 改进规划 R98（2026-09-12 立，R97 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R98。
> R97 交付见 `docs/ROUND_R97_20260912.md`（裁剪判定按导入缓存：79→41 次、7.13→6.89 s）。

## 1. R98 起点基线（R97 实测）

| 口径 | 实测 |
|---|---|
| 导入耗时（空载） | 1.39 / 1.34 / 6.89 / 0.09 / 2.45 / 0.08 s |
| 导入内存（负载中，R96 记录） | samplemodel2 峰值 **768.2 MB** |
| 真缺口 / 自由环 | 0/0、335/72、990/195、0/0、0/0、3/2 |
| 面数 | 5/6 与声明一致；SampleModel4 145/177（ref 族数据不在本 part） |
| 测试 | 全量 CI 口径全绿；`def test_` 扫描 6xx 条 |

## 2. R98 工作项：P438 导入内存构成分解

- **动作**：用 `scdm/perf.py` 在导入的三个阶段（解析 / 建面 / 缝合）各取一次进程峰值，给出**可数分解**；
  找出可以**分阶段释放**的对象（例如解析中间结构、失败面的临时面），尝试一处释放并量峰值变化。
- **验收**：分解表可复算；释放后峰值下降且几何/耗时三个口径不回退；若无处可释放，给出否证（各阶段占比 + 为何不能释放）。**成本**：1 天。

## 3. P434 · 每轮维护（必做）｜P439 · 后续候选

- P434 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P439 候选：装配爆炸动画帧、尺寸链第四批、`rep.small` 产品面收尾。

## 4. 纪律（延续）

1–89 条延续。
