# R27 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R27）执行 P157（树右键菜单与节点提示）。
> 交付：官方部件节点带**导入计数提示** + 右键“隔离显示 / 全部显示”，端到端测试 11 项全绿。

## 改动

1. `scdm/gui/left_panel.py`：
   - 新增信号 `group_isolate(str)` / `group_show_all()`；
   - `group_list` 设 `CustomContextMenu`，右键弹出“隔离显示 / 全部显示”（`_on_group_menu`，菜单在按下时构建）；
   - 分组标签追加导入计数后缀：`P1（3）· ref 2 · 未重建 1`（取 `import_report`，仅对 `imported` 分组显示）。
2. `scdm_gui.py`：
   - 连接两个新信号到 `isolate_import_group` 与新增的 `show_all_import_groups()`（把全部体置可见并刷新视口、更新状态栏）；
   - `show_import_group_hints()` 仍是提示数字的唯一来源，标签与状态栏共用。

## 测试

`test_p157_group_hints_and_show_all`：①`show_import_group_hints()` 的 `ref_faces/unbuilt_faces` 与 `import_report` 一致；
②隔离 P1 后**恰好** P1 可见；③`show_all_import_groups()` 恢复全部可见并返回体数。
（控件→信号→业务→状态 的分层验证按纪律 46：本轮验“业务→状态”，R26 已验“控件→信号”，R23 验“几何恒等”。）

## 验证

| 套件 | 结果 |
|---|---|
| `tests/test_lightweight_gui.py` | **11 passed** |
| `tests/test_import_fidelity.py` | 25 passed |

## P145 状态：**收尾完成**

官方层级链路已全线贯通：`import_report.hierarchy`（R22）→ 分组/显隐/隔离原语（R23/R24）→ 查看器接口（R25）→ 树勾选框（R26）→ 右键隔离/全部显示 + 节点提示（R27）。

## 下一步（写入 R28）

转 **P158 大模型 GUI 可用性**（积压十五轮）：导入 → 视口构建 → 首帧耗时/内存实测（samplemodel2/5），必要时按体/视锥懒装配，给出帧率基线。