# 改进规划 R27（2026-09-12 立，R26 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R27。
> R26 交付见 `docs/ROUND_R26_20260912.md`（树勾选框 → 部件显隐，端到端测试）。

## 1. R27 起点基线

| 口径 | 实测 |
|---|---|
| 除 ref 族外 | 6/6 精确 |
| 层级链路 | 数据 → 原语 → 查看器接口 → **树勾选框**（R26） |
| 测试 | `test_lightweight_gui.py` 10 项、`test_import_fidelity.py` 25 项 |
| 性能 | 导入 0.06–6.4 s |

## 2. R27 工作项（P157–P162）

### P157 · 树右键菜单与节点提示（最高优先，P145 收尾）

- **动作**：分组列表右键菜单“隔离/全部显示” → `isolate_import_group` / 全部可见；部件节点后缀显示 `ref_faces`/未重建计数（取 `show_import_group_hints()`）。
- **验收**：offscreen 测试覆盖右键隔离路径；samplemodel2 25 部件节点提示数字与 `import_report` 一致。**成本**：0.5–1 天。

### P158 · 大模型 GUI 可用性（积压十五轮，优先级紧随）

- **动作**：导入 → 视口构建 → 首次渲染耗时/内存实测（samplemodel2/5），必要时按体/视锥懒装配。
- **验收**：打开到可交互 < 20 s + 帧率基线写入轮次文档。**成本**：1–1.5 天。

### P159 · 特征族与梁焊件｜P160 · 图面尺寸第二批（多选/吸附/undo）｜P161 · 可选：`ref` 外部曲面库否证实验

### P162 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P157 -> P158 -> P159/P160 -> P161（可选）`（P162 收尾）

## 4. 纪律（延续 + R27 新增）

1–45 条延续。
46. **新增**：交互链路测试按“**控件 → 信号 → 业务 → 状态**”各验一段（本轮：P151 验控件→状态，P133 验业务→几何恒等），
    一段失败即可定位层级，避免一个巨型 GUI 测试同时断言显示与几何。