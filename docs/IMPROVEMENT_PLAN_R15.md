# 改进规划 R15（2026-09-12 立，R14 收尾后重新基线）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划——本文件是 R15。
> R14 交付见 `docs/ROUND_R14_20260912.md`（圆柱多弧消歧，**模型级切分归零**，samplemodel2 1813→1809）。

## 1. R15 起点基线（R14 结束后实测）

| 口径 | 实测 |
|---|---|
| 官方库导入 | **4/6 精确**（SampleModel1 109、samplemodel3 111、samplemodel5 1288、samplemodel6 28）；SampleModel4 2/143（未重建 35）；samplemodel2 37/**1809**（SAB 1813，**−4**） |
| 切分 | **0**（模型级 `extra by model {}`） |
| 残量 | samplemodel2：1 个未重建面 + 4 个被丢弃碎片；SampleModel4：35 个未重建面 |
| 漂移 | 5/6 为 0.0%、samplemodel2 0.6% |
| 性能 | 导入 0.07–6.80 s |
| 测试 | `tests/test_import_fidelity.py` 22 passed |

## 2. R15 工作项（P87–P92）

### P87 · samplemodel2 归零（最高优先）

- **动作**：①1 个未重建面：用 R14 的仪器化手法定位分支（无环/无多边形/环退化）；
  ②4 个被丢弃碎片：打印它们属于哪个 SAB 面、被 `_shape_within` 判出界的轴向与超出量，判断是“面本身越界”还是“裁剪后碎片越界”。
- **验收**：samplemodel2 **1813 ± 0**、漂移 **0.0%**。
- **成本**：0.5–1 天。

### P88 · SampleModel4 的 35 个未重建面（`ref` 引用表）

- **动作**：定位 ACIS `ref N` 引用表/延迟几何节（R8 四条排除证据 + SAT 极点交叉定位）。
- **验收**：`tools/import_fidelity.py --strict` 退出 0。
- **成本**：2–3 天（高风险）。

### P89 · 官方层级参与交互（积压五轮）｜P90 · 大模型 GUI（积压五轮）｜P91 · 特征族与梁焊件

- 各自验收判据沿用 R14 规划（勾选即时生效/几何不变；< 20 s 可交互 + 帧率基线；闭式体积/惯性矩断言）。

### P92 · 每轮维护（必做）

- **动作**：快照/CI/口径同步；编号递增；**每完成关键功能即提交并推送**（纪律 25/27）。

## 3. 建议顺序

`P87 -> P89 -> P90 -> P88 -> P91`（P92 收尾）

## 4. 纪律（延续 + R15 新增）

1–27 条延续。
28. **新增**：解析窗口给出**多条候选区间**时，一律“按记录 bbox 消歧后只发射一个”，不得“每个候选各建一个面”——
    本轮 2 个 surplus 面就是这么来的（窄凸包把圆裁成左右两段弧）。
29. **新增**：仪器化诊断优先写成**旁路探针脚本**（不改产品代码即可复算分支的每一步），确认根因后再动 `scdm/` 下的实现。