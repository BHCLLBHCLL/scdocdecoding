# 改进规划 R23（2026-09-12 立，R22 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R23。
> R22 交付见 `docs/ROUND_R22_20260912.md`（导入层级数据层 + 测试）。

## 1. R23 起点基线

| 口径 | 实测 |
|---|---|
| 除 ref 族外 | **6/6 精确**（4 样例完全精确；samplemodel2 1810/1813、SampleModel4 143/177 的差额已定性：ref 族 + 正确丢弃） |
| ref 族 | 如实上报（SampleModel4 52 面引用、samplemodel2 20 面引用，写入 `import_warnings`/`import_report.ref_faces`） |
| 层级 | `import_report["hierarchy"]`：part → body ids（只读，R22 新增） |
| 切分 | 0 |
| 性能 | 导入 0.06–6.4 s |

## 2. R23 工作项（P133–P138）

### P133 · 官方层级接 GUI（最高优先，P127 收尾）

- **动作**：`hierarchy` → 只读显示分组（树节点），勾选显隐/隔离 → 视口同步；组内体隐藏不改几何；
  `ref_faces`/未重建计数在同一节点上以提示形式展示。
- **验收**：勾选即时生效、几何不变；树与视口一致；samplemodel2（25 部件/37 体）逐组切换正确。
- **成本**：1.5–2 天。

### P134 · 大模型 GUI 可用性（积压十一轮）

- **动作**：导入 → 视口构建 → 首次渲染耗时/内存实测（samplemodel2/5），必要时按体/视锥懒装配。
- **验收**：< 20 s 可交互 + 帧率基线。**成本**：1–1.5 天。

### P135 · 特征族与梁焊件（广度）｜P136 · 图面尺寸交互第二批（多选/吸附/undo）

### P137 · 可选支线：`ref` 外部曲面库否证实验（低优先）

### P138 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P133 -> P134 -> P135/P136 -> P137（可选）`（P138 收尾）

## 4. 纪律（延续 + R23 新增）

1–41 条延续。
42. **新增**：交互类功能的“数据层先行”是标准做法——先把只读层级/计数接到 `import_report` 并加断言，再做 GUI 绑定；
    本轮即按此拆成两步（R22 数据、R23 绑定），避免 GUI 里再反推数据。