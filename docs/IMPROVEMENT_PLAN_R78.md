# 改进规划 R78（2026-09-12 立，R77 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R78。
> R77 交付见 `docs/ROUND_R77_20260912.md`（裁剪闸门放宽 + 未封闭度进报告/界面；缺口 2145→1953）。

## 1. R78 起点基线（R77 实测）

| 口径 | 实测 |
|---|---|
| **真缺口 / 自由环** | SampleModel1 **36/4**、SampleModel4 **387/96**、samplemodel2 **1953/396**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 2/2 |
| 缝边（非缺口） | 0 / 18 / 34 / 0 / 0 / 0 |
| 裁剪率 | SampleModel1 0.87、samplemodel2 0.66、samplemodel4 0.57、samplemodel5 **0.99**、samplemodel6 0.00 |
| 裁剪失败主因 | samplemodel2：**no-curve 165**、invalid 56、bbox-gap 26（R77 后更少） |
| 面数保真 | 除 ref 族（SampleModel4 33 面）外 6/6 精确；samplemodel2 1813/1813、丢弃 0 |
| 导入耗时 | 1.59 / 1.47 / 6.35 / 0.10 / 2.72 / 0.07 s |
| 产品指标 | `import_report` 带 `open_edges`/`seam_edges`/`free_loops` + Qt-free 提示 |
| 占位命令 | 2 条（`prep.small`、`safety.tab`） |
| 测试 | 全量（CI 口径）**635 passed, 2 skipped, 8 deselected**；`def test_` 扫描 618 条（18 页签 / 162 命令 / 176 live / 占位 2） |

## 2. R78 工作项（P383–P386）

### P383 · no-curve 面（samplemodel2 最大单因）

- **动作**：定位 165 个"环上有边解不出曲线"的柱面：逐条统计缺的是哪种曲线（intcurve 无 nubs？ref 间接？range 缺失？），
  产出可数分解；对"曲线数据其实在、只是路径没走通"的那一类补解码。
- **验收**：分解表可数 + 修掉一类；缺口与裁剪率同向改善；面数/包围盒/耗时不回退。**成本**：1–2 天。

### P384 · 缺口按两侧分支细分

- **动作**：把缺口按"裁剪面 vs 窗口面"两侧分类（需要把分支标签带出导入器：`trace_faces` 已有，接进报告即可），
  挑条数最多的一类再修。
- **验收**：分类表可数；修一类并给出前后对比。**成本**：0.5–1 天。

### P385 · 每轮维护（必做）｜P386 · 后续候选

- P385 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P386 候选：ref 族 33 面、装配爆炸图、型材库标准规格表、图面尺寸链第三批、占位命令 `prep.small`。

## 3. 建议顺序

`P383 -> P384 -> P385（收尾）`

## 4. 纪律（延续）

1–89 条延续。
