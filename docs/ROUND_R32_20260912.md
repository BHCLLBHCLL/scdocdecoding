# R32 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R32）执行 P187 前两段：**特征层 + 脚本层**接线。
> 交付：`hole_tapped` 进入 `Feature`（标签带螺纹规格）与脚本 op（`create.hole_tapped`），3 项测试通过；命令行与视口示意留给 R33。

## 改动

1. `scdm/features.py`：
   - 标签新增 `"hole_tapped": "攻丝孔 M{nominal:g}×{pitch:g}"`；
   - `_apply_one` 的孔族分支纳入 `hole_tapped`，并按其参数调 `K.hole_tapped(nominal, pitch, depth)`——**螺纹规格随特征保存**，回放能重建同一底孔切削。
2. `scdm/scripting.py`：
   - `_op_hole` 新增 `kind == "tapped"` 分支（与其他孔共用 `target/face_i/depth`，另加 `nominal/pitch`），返回摘要“攻丝孔 M6×1”；
   - 新增 `op_hole_tapped` 并注册 `OPS["create.hole_tapped"]`。

## 测试（新文件 `tests/test_threads_p187.py`，3 项）

①特征标签含 `M6` 与螺距；②`create.hole_tapped` 已在 `OPS` 注册；
③脚本 op 切除量 == 底孔闭式（`pi*(2.5mm)^2*20mm`，rel 1e-3），且**同一刀第二次不改变体积**（rel 1e-6）——即“同参重放不产生额外切除”。

| 套件 | 结果 |
|---|---|
| `tests/test_threads_p187.py` | **3 passed** |
| 线程相关三文件合计 | 11 项 |

## 待接（写入 R33）

①命令 `prep.hole` 增“攻丝”选项与公称/螺距输入（GUI）；②视口用 `thread_lines` 画牙型示意（不参与拾取/布尔）；
③脚本**回放链路**的端到端断言（本轮只验了“op 幂等”，回放体积等价留给命令落地后一起测）。