# 改进规划 R117（2026-09-12 立，R116 类型化冲突/全量体检/多段路径之后）

> 上轮：R116 交付 A-1 冲突标注带类型与双色、A-2 全量引用体检、A-5 沿曲线多段路径 + 起点偏移，
> 见 `docs/ROUND_R116_20260912.md`。本文件 = R116 新暴露的候选 + 顺延项。

---

## A. 草图链（承 R116）——**A-1 冗余指认 / A-2 体检报告进树 / A-6 删体或保留可选 已交付（R117）**

| # | 项 | 现状证据 | 验收（可复算） |
| --- | --- | --- | --- |
| A-1 ✅ | **冗余指认「谁重复了」**（R117 交付：`duplicate_of` 追到原始那条；来源可视化转 R118） | 冗余标记只说它重复，不说重复的是哪一条 | 冗余标记带 `duplicate_of`（先出现的那条序号）：同一约束重复三次 → 后两条各指向前一条；断言 `marks[i]["duplicate_of"] == 前一条` |
| A-2 ✅ | **体检报告可导出/可跳转**（R117 交付：`report_text` + 树节点 + 双击定位草图/体；一键修复转 R118） | 体检只在状态栏一句话 | `file.health` 命令：把 `document_warnings()` 出成对话框/文本；双击一条 → 选中对应草图/配合；断言文本行数 = 告警数 |
| A-3 | **尺寸驱动预览** | 驱动后立即重解并重建，没有预览态 | 改值 → 半透明预览 → 提交/取消；断言「未提交体积不变、提交后 = 闭式」 |
| A-4 | **拉之前先重解表达式尺寸** | `extrude_active` 按**草图所画**拉伸（R111 口径） | 存了 `2*dim6` 而几何未解时，拉伸前自动 `redrive_expressions` → 体积 = 表达式闭式；同轮更新 R111 的锁口径测试 |
| A-5 | **沿曲线偏移/锚点可控** | 锚点固定为路径起点，几何不在起点时整体平移 | `pattern_curves(..., anchor=(u,v))`：把指定点对准路径；断言锚点落在路径上、与路径起点的偏移 = 指定值 |
| A-6 ✅ | **删体给确认/降级**（R117 交付：`detach=True` 保留为普通实体；每次询问转 R118） | 闭环消失即删体（靠撤销） | `detach=True` 或 GUI 确认：体降级为「断开参数的普通实体」；断言体还在、`can_replay` 为假、特征栈为空 |
| A-7 | **三维模式下选中草图曲线** | 草图只在草图模式可选；退出即清空 | 三维模式点中草图曲线 → 高亮 + 「双击进入草图编辑」；断言模式切换后选择集仍指向同一条曲线 |

## B. 装配与配合（承 R105/R106，仍未开工）

| # | 项 | 验收 |
| --- | --- | --- |
| B-1 | 装配层级 / 局部坐标系 | 两级链 A→B→C：移动 A 10mm，B、C 按链式矩阵更新（1e-12） |
| B-2 | 配合驱动量的多帧求解 | 螺旋副转 90°：位移 = pitch·(θ/2π) 闭式 |
| B-3 | 组件移动后重新求解 | 移动基座后 `solve_mates`：从动件位姿 = 重新求解的解 |
| B-4 | 脚本层文档级特征 | 脚本跑完 `document_features.ops()` 与 GUI 同序列 |
| B-5 | handler 端到端（QTimer） | 驱动 `det.params` / 树双击 / `asm.mate`，各断言一次闭式 |
| B-6 | 爆炸图走可回放平移 | 爆炸后 `can_replay` 真；还原后包围盒回原位 |

## C. 其他顺延项

1. **数字单一来源**：`gen_devplan_snapshot.py` 与 `state_inventory.py` 交叉校验（纪律 84）。
2. **配置语义扩展**：替换件 / 驱动尺寸 / 配置对比；BOM 行数与质量 = 快照值。
3. **工程图标注补齐**：孔标注 / 中心线 / 粗糙度 / 剖面线；DXF 与 SVG 实体计数 = 闭式条数。
4. **钣金折弯表 + 展开图工程化**：`ΔBA = θ/2·(t·K₂ − t·K₁)` 闭式；DXF 折弯线数 = 折弯数。
5. **未封闭度**：SampleModel4 335/72 → 缺口 < 200、环 < 40；samplemodel2 990/195 → 缺口 < 700。
6. **导入峰值内存**：768 MB（负载中）目标 −30%，否则否证表。
7. **P2 整域**：去毛刺 / 参考图像+标定 / 用户图层 / 3D PMI（边界）/ 型材库+尺寸链第四批 / `scdm_gui.py` 拆分。

## D. 明确不做（沿用）

ref 族 27 面（三条否证）、字节恒等、CATIA/Parasolid 直读、ACIS bit 等价、求解链、KeyShot 材质库、
3D PMI 读取（NYI-1..6）、`safety.tab` 空壳（按设计保留）。

## E. 口径（R117 起沿用）

1. **标记要能说话**（R116）：颜色给状态、label 给类型；几何既给并集也给逐条。
2. **体检覆盖所有按 ID 指向的结构**（R116）：尺寸/配合/命名选择/组/配置/实例。
3. **选择顺序是用户意图**（R116）：路径链方向优先按选择顺序；不确定的断言写不变式。
4. **不连续就拒绝**（R116）：宁可报差距，不静默桥接。
5. **诊断落在几何上**（R115）；**一行坏数据不许让整张报告失效**（R115）；
   **模式的真相在会话，不在工具**（R115）；**文件类型看内容**（R115）；
   **删要有据、可回退**（R115）。
6. **画出来的和能选中的是同一份几何**（R114）；**拾取偏好可预期**（R114）；
   **按折线走就按折线说**（R114）；**失败指到具体对象且仍列可引用名**（R113/R114）。
7. **一个概念一个数**（R112/R113）；**局部化与计数同源**（R112）；**测量只读**（纪律 85）；
   **多闭环体积断言求和**（R107）；**再拉一次不复制已建体**（R112）。
8. **尺寸单位三分**（R110）：约束里数值是米、表达式与 `dimN` 是毫米、API/UI 传入是毫米。
9. 每轮结束照旧：定向 + 定向回归批 + 全量回归 + 快照 `--check`（两种解释器）+ 同轮文档 + 提交推送。
