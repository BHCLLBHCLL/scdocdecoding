# R18 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R18）执行 P105（最后 1 面 + 碎片）。
> 交付：把“被丢弃碎片”查清并判定其性质；结论是**丢弃是对的**，真正剩下的只有 1 个未重建面。下一轮规划见 `docs/IMPROVEMENT_PLAN_R19.md`。

## 诊断：最后 1 个被丢弃碎片（本轮补全了 R16 被截断的线索）

探针（带完整上下文，按纪律 31）打印：

```
DROP m6 f271 plane  overshoot_lo [0.311, 0.848, -0.0]  hi [0.0, 0.0, 5.606]  pieces 1
```

该平面片相对模型包围盒：y 方向低端超出 **84.8%** 对角、z 方向高端超出 **560%** 对角——即几何量级是模型的 6 倍，
**属错误几何，丢弃是正确行为**（纪律 24：先判“漏建”还是“消除错误几何”；此处是后者）。

另：R17 的几何重连兜底使“因超界被丢弃”的面数从 4 降到 3，方向正确（新纪律 34 要求的三项计数已记录）。

## 当前状态

| 样例 | SAB | 实测 | 漂移 |
|---|---|---|---|
| SampleModel1 | 1/109 | **精确** | 0.0% |
| SampleModel4 | 2/177 | 2/143（未重建 35） | 0.0% |
| samplemodel2 | 37/1813 | **1810** | 0.6% |
| samplemodel3 / 5 / 6 | — | **精确** | 0.0% |

模型级切分 **0**；`tests/test_import_fidelity.py` 22 passed。

## 结论：samplemodel2 只剩一个真目标

**1 个未重建面**（模型 21 面 209，`spline`）——它的 uv 是 `[-8.3e-06, 1.000008, 0.0, 0.8196]`，
属“样条面 + uv 窗口”组合；R16 的环探针显示平面面不闭合，而这个是样条面，需单独取证（下一轮 P111）。