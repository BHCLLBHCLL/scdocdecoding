# 改进规划 R116（2026-09-12 立，R115 冲突几何/引用体检/删体之后）

> 上轮：R115 交付 A-1 冲突几何进视口、A-2 打开工程引用体检、A-6 闭环消失即删体，
> 并修掉两个真 bug（点「三维」不收尾 / 工程改名另存打不开），见 `docs/ROUND_R115_20260912.md`。

---

## A. 草图链（承 R115）

| # | 项 | 现状证据 | 验收（可复算） |
| --- | --- | --- | --- |
| A-1 | **冲突标注带类型与颜色** | `conflict_geometry` 只给点与线段，不说是哪种冲突 | 每个标记带 `kind`（尺寸/水平/垂直/重合/相切…）与 `state`（冲突/冗余）；视口用两种颜色；断言标记集合 = `conflict_cons` 的 `(kind, 序号)` 列表 |
| A-2 | **全量引用体检** | 体检只查尺寸引用 | `reference_warnings()` 扩展：mates 指向已删组件、named selection / 图层指向已删面、配置指向已删体，各报一条；打开时一并汇总 |
| A-3 | **尺寸驱动预览** | 驱动后立即重解并重建，没有预览态 | 改值 → 半透明预览 → 提交/取消；断言「未提交体积不变、提交后 = 闭式」 |
| A-4 | **拉之前先重解表达式尺寸** | `extrude_active` 按**草图所画**拉伸（R111 口径） | 存了 `2*dim6` 而几何未解时，拉伸前自动 `redrive_expressions` → 体积 = 表达式闭式；同轮更新 R111 的锁口径测试 |
| A-5 | **沿曲线阵列扩展** | 路径只能是一条草图曲线，首件必落在起点 | 多段路径按弧长跨段均布；`offset`（起点偏移）使首件不在起点；间距 = 弧长/(count−1) 仍成立 |
| A-6 | **删体给确认/降级** | 闭环消失即删体（靠撤销） | `sync_sketch_bodies(detach=True)` 或 GUI 确认框：把体降级为「断开参数的普通实体」；断言体还在、`can_replay` 为假、特征栈为空 |
| A-7 | **三维模式下选中草图曲线** | 草图只在草图模式可选；退出即清空 | 三维模式点中草图曲线 → 高亮 + 「双击进入草图编辑」；断言模式切换后选择集仍指向同一条曲线 |

## B. 装配与配合（承 R105/R106，仍未开工）

| # | 项 | 验收 |
| --- | --- | --- |
| B-1 | 装配层级 / 局部坐标系 | 两级链 A→B→C：移动 A 10mm，B、C 按链式矩阵更新（1e-12） |
| B-2 | 配合驱动量的多帧求解 | 螺旋副转 90°：位移 = pitch·(θ/2π) 闭式 |
| B-3 | 组件移动后重新求解 | 移动基座后 `solve_mates`：从动件位姿 = 重新求解的解 |
| B-4 | 脚本层文档级特征 | 脚本跑完 `document_features.ops()` 与 GUI 同序列 |
| B-5 | handler 端到端（QTimer） | 驱动 `det.params` / 树双击 / `asm.mate`，各断言一次闭式 |
| B-6 | 爆炸图走可回放平移 | 爆炸后 `can_replay` 真；还原后包围盒回原位 |

## C. 其他顺延项

1. **数字单一来源**：`gen_devplan_snapshot.py` 与 `state_inventory.py` 交叉校验（纪律 84）。
2. **配置语义扩展**：替换件 / 驱动尺寸 / 配置对比；BOM 行数与质量 = 快照值。
3. **工程图标注补齐**：孔标注 / 中心线 / 粗糙度 / 剖面线；DXF 与 SVG 实体计数 = 闭式条数。
4. **钣金折弯表 + 展开图工程化**：`ΔBA = θ/2·(t·K₂ − t·K₁)` 闭式；DXF 折弯线数 = 折弯数。
5. **未封闭度**：SampleModel4 335/72 → 缺口 < 200、环 < 40；samplemodel2 990/195 → 缺口 < 700。
6. **导入峰值内存**：768 MB（负载中）目标 −30%，否则否证表。
7. **P2 整域**：去毛刺 / 参考图像+标定 / 用户图层 / 3D PMI（边界）/ 型材库+尺寸链第四批 / `scdm_gui.py` 拆分。

## D. 明确不做（沿用）

ref 族 27 面（三条否证）、字节恒等、CATIA/Parasolid 直读、ACIS bit 等价、求解链、KeyShot 材质库、
3D PMI 读取（NYI-1..6）、`safety.tab` 空壳（按设计保留）。

## E. 口径（R116 起沿用）

1. **诊断落在几何上**（R115）：冲突要给点/线段，库层保持 Qt-free 以便 headless 断言映射。
2. **一行坏数据不许让整张报告失效**（R115）：无法解析的表达式跳过并记录（`skipped`）。
3. **模式的真相在会话，不在工具**（R115）：`_exit_sketch` 以 `_sketch_session` 判定。
4. **文件类型看内容，不看扩展名**（R115）：工程 = zip + manifest.json。
5. **删要有据、可回退**（R115）：闭环消失才删体、点名原因、可靠快照恢复。
6. **画出来的和能选中的是同一份几何**（R114）；**拾取偏好可预期**（顶点优先，圆采样点不是顶点）。
7. **按折线走就按折线说**（R114）：采样相关的量断言收敛。
8. **失败要指到具体对象**（R114）且**仍列出可引用名**（R113）；`dimN` / `S1_dimN` 必须写在树上。
9. **一个概念一个数**（R112/R113）：拾取半径 = 捕捉半径；焊接容差独立（默认 0.1mm）。
10. **局部化与计数同源**（R112）；**测量只读、只报开着的缝**（纪律 85 / R112）；
    **多闭环体积断言必须求和**（R107）；**再拉一次不复制已建体**（R112）。
11. **尺寸单位三分**（R110）：约束里数值是米、表达式与 `dimN` 是毫米、API/UI 传入是毫米。
12. 每轮结束照旧：定向 + 定向回归批 + 全量回归 + 快照 `--check`（两种解释器）+ 同轮文档 + 提交推送。
