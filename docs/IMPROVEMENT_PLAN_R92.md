# 改进规划 R92（2026-09-12 立，R91 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R92。
> R91 交付见 `docs/ROUND_R91_20260912.md`（prep.small 与 repair.check 共用一处实现；占位 1、live 177）。

## 1. R92 起点基线（R91 实测）

| 口径 | 实测 |
|---|---|
| UI 命令面 | 18 页签（+1 backstage）/ 162 命令 / **177 live** / 占位 **1**（`safety.tab`，按设计保留空壳） |
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 347/76、samplemodel2 **1002/212**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 图面 | 尺寸链并列标注 + 链线（R90） |
| 型材库 | 12 条标准规格（R87） |
| 测试 | 全量 CI 口径全绿（**671 passed**，652 条 `def test_`） |

## 2. R92 工作项（择优取一）

### P426 · 槽钢（C 形）截面

- **动作**：`scdm/beams.py` 增加 `c` 截面（h, b, tw, tf）与闭式截面特性（重心偏心 + 平行轴），
  配 3–4 条标准规格（如 [10、[12.6、[16）与**双重对拍**（矩形分解 vs 实测 `K.section_props`）。
- **验收**：闭式 vs 独立分解 ≤1e-12；几何 vs 闭式 ≤1e-9；规格表可被 `create.beam` 按名使用。**成本**：0.5–1 天。

### P427 · 环面裁剪的"单候选 + 便宜兜底"再试

- **动作**：R89 已测出环面成本在"两个候选曲面 × 采样兜底"；本轮试**只用一个候选**（major/minor，另一读法仅在失败时试一次），
  并给失败的环面用**便宜**窗口兜底（accurate=False）。若 36 面总成本 < 3 s 且缺口下降，则纳入白名单。**成本**：0.5 天。

### P428 · 每轮维护（必做）｜P429 · 后续候选

- P428 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P429 候选：ref 27 面、装配爆炸动画帧、尺寸链第三批导出侧补测（SVG/DXF 带两条标注的端到端断言）。

## 3. 建议顺序

`P426 -> P427 -> P428（收尾）`

## 4. 纪律（延续）

1–89 条延续。
