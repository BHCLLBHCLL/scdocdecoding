# R15 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R15）执行 P87（samplemodel2 归零）。
> 本轮为**诊断轮**：把“1 个未重建面 + 4 个碎片”查清到分支级，修法留给 R16（本文件与探针结论一起提交推送）。

## 诊断结果（旁路探针，未改产品代码）

对 samplemodel2 全模型逐面复算“重建 + 多边形兜底”，得到：

| 类别 | 数量 | 典型对象 | 判读 |
|---|---|---|---|
| 环游走无多边形（`loops=0`） | 多数 | 模型 8/15/21/24 的**平面**面（如 24 面 3090、21 面 269/334） | **环/协边解码**问题：面有 loops 却取不到多边形，属拓扑层而非曲面层 |
| 环有 2 点退化多边形 | 2 | 模型 24 面 1097/1665 | 已有 R7 的 `_loop_polygon_sampled` 兜底（探针未调用它，导入路径里这两面其实能建） |
| 被 `_shape_within` 丢弃的碎片 | 1 | （探针输出被截断，下一轮复现） | 需打印所属面与超出量 |

**关键结论**：samplemodel2 的残量**不在曲面解码**（R9–R14 已把切分打到 0），而在**平面面的环/协边解码**——
`face_loops_polygons` 对这些面返回空，导致平面兜底无输入。这解释了为什么“切分归零”之后仍有未重建面。

## 当前状态（R14 基线，未变）

| 样例 | SAB | 实测 | 漂移 |
|---|---|---|---|
| SampleModel1 | 1/109 | **精确** | 0.0% |
| SampleModel4 | 2/177 | 2/143（未重建 35） | 0.0% |
| samplemodel2 | 37/1813 | **1809**（−4） | 0.6% |
| samplemodel3 / 5 / 6 | — | **精确** | 0.0% |

模型级切分 **0**；`tests/test_import_fidelity.py` 22 passed。