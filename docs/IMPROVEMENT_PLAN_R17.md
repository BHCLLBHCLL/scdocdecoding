# 改进规划 R17（2026-09-12 立，R16 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R17。
> R16 交付见 `docs/ROUND_R16_20260912.md`（`loops=0` 的成因 = **环按 next 链不闭合**）。

## 1. R17 起点基线

| 口径 | 实测 |
|---|---|
| 官方库导入 | **4/6 精确**；SampleModel4 2/143（未重建 35）；samplemodel2 37/**1809**（SAB 1813） |
| 切分 | 0（模型级） |
| 残量根因 | 平面/样条面的环**不闭合**（`next` 链 + 端点走不通；`_walk_sensed`/`_walk_ring` 均 None） |
| 性能 | 导入 0.07–6.80 s |

## 2. R17 工作项（P99–P104）

### P99 · 环闭合修复（最高优先）

- **动作**：①补全探针（打印整个环的全部 coedge，含第 6 条/末条）；
  ②候选修法 A：环不闭合时用**顶点源端点**重走（`edge_endpoints` 的容差内优先曲线端点可能是偏差源）；
  ③候选修法 B：**按几何重连**——把面的 coedge 端点当无向边做邻接重建闭合环（不依赖 `next` 链顺序）。
  两法都先做旁路探针验证闭合率，再进产品代码（纪律 29）。
- **验收**：samplemodel2 **1813 ± 0**、漂移 **0.0%**。
- **成本**：1–1.5 天。

### P100 · 最后 1 个被丢弃碎片（复现，含完整上下文）

- **成本**：0.5 天。

### P101 · 官方层级交互（积压六轮）｜P102 · 大模型 GUI（积压六轮）｜P103 · 特征族与梁焊件

### P104 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P99 -> P100 -> P101 -> P102 -> P103`（P104 收尾）

## 4. 纪律（延续 + R17 新增）

1–31 条延续。
32. **新增**：诊断输出的“截断”本身要当成缺陷修（本轮 `[:5]` 把第 6 条协边截掉，线索断在半路）——探针必须打印完整环/完整集合，或显式打印总数与“还有 N 条”。
33. **新增**：“环不闭合”这类几何一致性问题，优先尝试**按几何重连**（端点邻接）而不是继续调 `next` 链的读取；链顺序是**提示**，几何才是**判据**。