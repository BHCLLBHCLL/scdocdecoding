# R81 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R81）取 `docs/IMPROVEMENT_PLAN_R81.md` 的 C1：**ref 族 29 面**。
> 交付：一条**真实的解码缺陷**（两节点 nubs 记录被 R78 的守卫跳过）+ 一份把剩余 51 条失败边**逐边定性**的否证表。

## 1. 三个假设的否证（纪律 86，全部本轮实测）

| 假设 | 实测否证 |
|---|---|
| H1 **内联几何**：ref 记录里的 4 个 double 是平面 (nx,ny,nz,d) | 把候选平面与面自身顶点比：最佳拟合 **0.012 m**、多数 ≥0.9 m；且 29 个未重建面里 **20 个载荷根本没有任何数字**、3 个只有 2 个 |
| H2 **索引**：记录里的 int 指向模型内某个曲面实体 | 该 int 指向的实体类型是 loop/coedge/face/string_attrib/vertex（无曲面）；样本里也找不到"未使用曲面池"（R72 已测：每个 model 的曲面头**全部**被某个面引用） |
| H3 **边界重建**：曲线修准之后就能从边界填/建出这些面 | 29 个面里 **22 个仍有边解不出曲线**、7 个线框不闭合；填面（`BRepOffsetAPI_MakeFilling`）连一次机会都没得到 |

## 2. 修：两节点 nubs 记录（R78 守卫写窄了）

R78 的端点重数修正写成 `if len(raw) > 2`，恰好**跳过两节点记录**（单个三次片段，mults `[3, 3]`）。
这类记录交给 OCCT 会抛 `Poles and degree mismatch`，而它在函数末尾被 `except` 吞掉——5 条 ref 族的边因此永远解不出来。
改为 `len(raw) >= 2` 后：

| 样例 | 面数 | ref 重建/未重建 | 真缺口 | 自由环 |
|---|---|---|---|---|
| SampleModel4 | 147 → **149**（SAB 177） | 23/29 → **25/27** | 378 → 385 | 95 → **94** |
| samplemodel2 | 1813（不变） | 19/1（不变） | 1395 → **1393** | 374 → **372** |
| SampleModel1 / 3 / 5 / 6 | 不变 | — | 36 / 0 / 0 / 2 | 4 / 0 / 0 / 2 |

## 3. 剩余的 ref 族：逐边定性（拒绝的最终形态）

27 个未重建面的失败边共 **51 条**，逐条查曲面/曲线头的载荷：**51/51 载荷只有 `ref`**（没有 nubs/exactcur），
即"共享曲线库、数据不在本 part"——与 R21/R72 的判定一致，但现在是从**族级断言**降到了**逐边计数**。
6 个面无任何边界边、7 个面线框不闭合（这两类不需要曲线数据即可判定）。

## 4. 测试

| 文件 | 项 | 内容 |
|---|---|---|
| `tests/test_import_geometry_r81.py` | 2 | ①SampleModel4 中**所有**带 nubs 的 intcurve 边都能解码（含两节点记录，实测两个计数相等）；②27 个未重建 ref 面的失败边恰好 **51 条且全部只有 ref 载荷** |
| `tests/test_ref_family.py` | 改 | 四元组 52/**25**/**27**/6、警告文本、逐面统计 |
| `tests/test_free_edges_r75.py` | 改 | 三元组：SampleModel4 **403/18/385**、samplemodel2 **1427/34/1393** |
| `tests/test_import_geometry_r73.py` | 改 | ref_unbuilt 29 → 27 |

全量（CI 口径 `-m "not official"`）**637 passed, 2 skipped, 8 deselected**；`def test_` 扫描 620 条（快照：18 页签 / 162 命令 / 176 live / 占位 2）。

## 5. 下一步（写入 R82）

C1 已到底（剩余全部只有 `ref` 载荷，三条否证齐全），下一轮转向 `docs/IMPROVEMENT_PLAN_R82.md` 的：
P-未封闭度：`invalid 32` 类裁剪失败面 + `no-curve` 的真缺口分类；以及 C3 产品面（几何体检命令 / `prep.small`）。
