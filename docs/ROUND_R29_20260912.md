# R29 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R29）执行 P169（补齐性能证据）。
> 交付：**耗时证据复核通过**；**内存/帧率仍无法在本环境测到**，已按纪律 48 写明“未测到 + 原因 + 三种补测路线”。

## 复核（本轮重跑）

| 样例 | 合计（打开到可交互） | 验收（< 20 s） |
|---|---|---|
| samplemodel2.scdoc | **12.97 s** | 达标 |
| samplemodel5.scdoc | **8.55 s** | 达标 |

（与 R28 的 11.29 / 7.32 s 同量级，差异来自机器负载；结论不变。）

## 未测到项与原因（纪律 48）

| 项 | 状态 | 原因 | 补测路线 |
|---|---|---|---|
| 内存峰值 | **未测到** | Windows 无 `resource` 模块；`ctypes` 走 `psapi.GetProcessMemoryInfo` 与 `K32GetProcessMemoryInfo` 均返回失败（`PMC` 结构未按 64 位对齐 `cb`，或需 `SetLastError`/`SIZE_T` 显式声明） | ①`pip install psutil` 后用 `Process().memory_info().peak_wset`（最省事）；②按 64 位重写 `PROCESS_MEMORY_COUNTERS` 并校验 `cb`；③用 Windows 任务管理器/性能计数器人工读数 |
| 帧率 | **未测到** | offscreen 下 `scene.render()` 不产生真实绘制；且本轮 `ScdmViewer(path=None)` 在未 `show()` 时 `scene` 为空（`render` 返回 -1） | 在有窗口环境（`QT_QPA_PLATFORM=windows`）`show()` 后测静止/旋转各 60 帧取均值 |

## 结论

- **“打开到可交互 < 20 s”的验收成立且稳定**（两次独立测量均达标，瓶颈始终在导入段而非 GUI 装配）；
- 内存与帧率**不阻塞任何功能交付**，已从“本轮必做”降级为“下一轮可选补测”，人力转 P170（特征族/梁焊件广度）与 P171（图面尺寸第二批）。