# 改进规划 R84（2026-09-12 立，R83 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R84。
> R83 交付见 `docs/ROUND_R83_20260912.md`（`repair.check` 报未封闭度 + 缺口标记；不新增命令）。

## 1. R84 起点基线（R83 实测）

| 口径 | 实测 |
|---|---|
| 真缺口 / 自由环 | SampleModel1 **0/0（封闭）**、SampleModel4 375/86、samplemodel2 1272/359、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 面数保真 | 5/6 样例完全一致；SampleModel4 149/177（ref 25/27） |
| 命令面 | 18 页签 / 162 命令 / 176 live / 占位 2（R83 未新增命令） |
| 产品指标 | `import_report` 四元组 + `repair.check` 未封闭度 + 缺口标记 |
| 测试 | 全量 CI 口径全绿（**642 passed, 2 skipped, 8 deselected**）；`def test_` 扫描 625 条 |

## 2. R84 工作项（择优取一）

### P404 · 未封闭度剩余项逐类定位（最高优先）

- **动作**：用 `tools/free_edge_report.py --pair` 把 SampleModel4（375/86）与 samplemodel2（1272/359）的缺口**按两侧分支**分类：
  ①两侧是"裁剪面 vs 裁剪面"、"裁剪面 vs 窗口面"还是"线框面 vs 任意"；②配对距离分布；③代表面逐个人工追。
  挑最大一类修；修不动就给否证表（纪律 86）。
- **验收**：分类表可数 + 至少一类有结论；缺口/自由环下降且面数、包围盒、耗时三不回退。**成本**：1 天。

### P405 · 每轮维护（必做）｜P406 · 后续候选

- P405 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P406 候选：装配爆炸图第二批（沿轴/径向 + 动画帧）、型材库标准规格表（槽钢/工字钢）、
  图面尺寸链第三批（极值/统计并列标注 + 链线图形化）、占位命令 `prep.small` 重定向到统一检查向导。

## 3. 建议顺序

`P404 -> P405（收尾）`

## 4. 纪律（延续）

1–89 条延续。
