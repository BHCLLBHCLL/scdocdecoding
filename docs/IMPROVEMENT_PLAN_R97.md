# 改进规划 R97（2026-09-12 立，R96 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R97。
> R96 交付见 `docs/ROUND_R96_20260912.md`（性能基线复测：无回退；补两条**负载中**的导入记录 + 768 MB 峰值）。

## 1. R97 起点基线（R96 实测）

| 口径 | 实测 |
|---|---|
| 交互路径性能 | 五行全部与 R71 基线相差 ≤2%（无回退） |
| 导入性能（空载） | SampleModel1 1.47–1.5 s、samplemodel2 7.0–7.6 s |
| 导入性能（负载中） | 3.34 s、**40.26 s**；峰值内存 232.7 MB、**768.2 MB** |
| 面数 | 5/6 与声明一致；SampleModel4 145/177（ref 族数据不在本 part，已定性结束） |
| 真缺口 / 自由环 | 0/0、335/72、990/195、0/0、0/0、3/2 |
| 测试 | 全量 CI 口径全绿（**684 passed**，665 条 `def test_`） |

## 2. R97 工作项：P436 导入内存与重复计算

- **动作**：①`watertightness` 与策略探针在**同一导入内多次调用** `K.area`/`open_edges`（同一面重复求面积/缝边判定）；
  给 `_trim_gates_ok` 的面积判定加**按导入的缓存**（键为 face 的哈希/TShape），并量缓存命中数与峰值内存变化；
  ②量 `import_scdoc_bundle` 的峰值内存构成（解析 vs 建面 vs 缝合），给出可数分解。
- **验收**：重复 GProp 调用数下降（可数）、导入耗时与峰值内存**不回退**；分解表可复算。**成本**：1 天。

## 3. P434 · 每轮维护（必做）｜P437 · 后续候选

- P434 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P437 候选：装配爆炸动画帧、导入内存的分阶段释放、尺寸链第四批。

## 4. 纪律（延续 + R96 提示）

1–89 条延续。R96 给纪律 85 补一句：**测量顺序也是扰动**——同一条导入在空载与负载下差 5 倍，
所以每条记录都要写清"在什么状态下取的"。
