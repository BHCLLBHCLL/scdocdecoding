# 改进规划 R56（2026-09-12 立，R55 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R56。
> R55 交付见 `docs/ROUND_R55_20260912.md`（图纸标注：引线 + 形位公差，含二维吸附与快照升级）。

## 1. R56 起点基线

| 口径 | 实测 |
|---|---|
| 图面 | 尺寸多选/吸附/undo ✅（R52）/ **引线 + 形位公差 ✅（R55）**；标注样式与基准体系 ⏳ |
| 成形族 | 凹坑/百叶/敲落/角撑/舌片 全部 ✅五项+接线 |
| 梁/焊件域 | 6 截面 ✅ / 单段梁 ✅ / 折线梁+组元+符号 ✅（R53） |
| 测试 | 全量 **508 passed, 2 skipped**；`def test_` 扫描 485 条（18 页签 / 154 命令 / 167 live / 占位 3） |
| 官方库 | 除 ref 族外 6/6 精确（沿用 R48 实测）；打开到可交互 12.97 / 8.55 s |

## 2. R56 工作项（P303–P306）

### P303 · 钣金接缝 junction / 撕裂补项（最高优先，R55/P300 顺延）

- **动作**：接缝（两折弯相交处的释放/连接）几何闭式（去除量 = 三角形/矩形截面 × 板厚），
  沿用 `rip`/`corner_relief` 的既有路径与面框架；
- **验收**：闭式 + 非法 + 极端 + 回放 + **展开数值等价（纪律 60/67/71）**。**成本**：1 天。

### P304 · 图纸标注第二批：样式 + 基准体系

- **动作**：引线箭头（实心/空心/无）、标注样式（字高/颜色）、基准符号（A/B/C 与基准目标）、
  公差框多格（复合公差）；导出层同步。
- **验收**：样式字段往返一致 + 导出层含样式属性 + 吸附/undo 不回归（R52/R55 测试全绿）。**成本**：1 天。

### P305 · 可选补测（内存/帧率）

- **动作**：沿用 `tests/test_perf_budget.py` 口径补一次内存峰值与帧率基线。**成本**：0.5 天（可选）。

### P306 · 每轮维护（必做）

- 快照/CI/口径同步（`tools/gen_devplan_snapshot.py --update`）；编号递增；轮次记录与规划同轮补齐（纪律 65）。

## 3. 建议顺序

`P303 -> P304 -> P305（可选）`（P306 收尾）

## 4. 纪律（延续 + R56 新增）

1–71 条延续。

72. **新增**：可撤销交互的状态快照必须**覆盖所有可变字段**。R55 把画布 undo 从"偏移列表"升级为
    `{offsets, notes}`——否则拖动标注再撤销会出现"尺寸被撤销、标注留在原地"的错位（快照漏字段的典型症状）。
    新增可拖对象时，先问一句：快照收全了吗？
