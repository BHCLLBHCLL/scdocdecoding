# R24 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R24）执行 P139 的**逻辑层**（树↔视口联动的另一半）。
> 交付：`isolate_group`（隔离显示）落地并测试；GUI 勾选/快捷键绑定留给 R25（纪律 42：数据/逻辑层先行）。

## 改动

`scdm/import_sab.py` 新增 `isolate_group(kdoc, group) -> int`：
**只显示该组内的体**（其余 `visible=False`），返回显示的体数；与 `apply_group_visibility` 同一契约——
**只改 `visible`，几何对象恒等**（纪律 43）。

于是“按官方部件显隐 / 隔离”的两个逻辑原语齐备：
`import_hierarchy_groups`（分组）＋ `apply_group_visibility`（显隐）＋ `isolate_group`（隔离）。

## 测试

`test_p139_isolate_group_shows_only_that_part`（新增）：
①隔离首组后**可见集合恰为该组体 id**；②切到第二组后可见集合恰为该组；③全程 `{b.id: b.shape}` 恒等（几何不变）。

## 验证

| 套件 | 结果 |
|---|---|
| 隔离/显隐测试 | 2 passed |
| `tests/test_import_fidelity.py` | 见收尾实跑 |

## 下一步（写入 R25）

GUI 绑定：树上官方部件节点的勾选框 → `apply_group_visibility` + `scene.apply_visibility` 刷新；
右键/快捷键“隔离”→ `isolate_group`；节点提示 `ref_faces` 与未重建计数。随后转 P140（大模型 GUI 实测）。