# 改进规划 R19（2026-09-12 立，R18 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R19。
> R18 交付见 `docs/ROUND_R18_20260912.md`（丢弃碎片定性；samplemodel2 只剩 1 个未重建面）。

## 1. R19 起点基线

| 口径 | 实测 |
|---|---|
| 官方库导入 | **4/6 精确**；SampleModel4 2/143（未重建 35）；samplemodel2 37/**1810**（SAB 1813，−3：= 1 未重建 + 2 正确丢弃/碎片口径差） |
| 切分 | 0（模型级） |
| 真目标 | samplemodel2：**1 个未重建面**（模型 21 面 209，spline）；SampleModel4：**35 个未重建面** |
| 性能 | 导入 0.07–6.88 s |

## 2. R19 工作项（P111–P116）

### P111 · samplemodel2 归零（最高优先）

- **动作**：对模型 21 面 209（spline，uv [−8.3e−06, 1.000008, 0.0, 0.8196]）逐步骤取证：
  ①`_bspline_surfaces` 是否给出候选；②若有候选，`_face_from_window` 的采样误差是多少；③若无候选，缺的是 `nurbs`/`both` 还是 `nubs` 载荷；
  ④若窗口被拒，按 R12 的方法（容差由离散误差定值）决定是否放宽到样条面。
- **验收**：samplemodel2 **1813 ± 0**、漂移 **0.0%**。
- **成本**：1 天。

### P112 · SampleModel4 的 35 个未重建面（`ref` 引用表）

- **验收**：`--strict` 退出 0。**成本**：2–3 天（高风险）。

### P113 · 官方层级交互（积压七轮）｜P114 · 大模型 GUI（积压七轮）｜P115 · 特征族/梁焊件

### P116 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送（纪律 25/27）。

## 3. 建议顺序

`P111 -> P113 -> P114 -> P112 -> P115`（P116 收尾）

## 4. 纪律（延续 + R19 新增）

1–34 条延续。
35. **新增**：“丢弃”与“未重建”必须分开记账并各自定性（本轮：丢弃 = 错误几何、正确行为；未重建 = 真缺口），
    验收数字要写明“= SAB ± 哪几类”，避免把两类混成一个差额。