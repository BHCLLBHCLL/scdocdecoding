# 改进规划 R25（2026-09-12 立，R24 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R25。
> R24 交付见 `docs/ROUND_R24_20260912.md`（隔离原语 + 测试）。

## 1. R25 起点基线

| 口径 | 实测 |
|---|---|
| 除 ref 族外 | 6/6 精确 |
| 层级原语 | `import_hierarchy_groups` / `apply_group_visibility` / `isolate_group`（均已测，几何恒等） |
| GUI 绑定 | **未接**（下一步） |
| 性能 | 导入 0.06–6.4 s |

## 2. R25 工作项（P145–P150）

### P145 · GUI 绑定官方层级（最高优先）

- **动作**：①树节点（`scdm/gui/left_panel.py`）为 `imported` 分组画勾选框；②勾选 → `apply_group_visibility` + `scene.apply_visibility(session)`；
  ③右键“隔离”→ `isolate_group`；④节点提示 `ref_faces`/未重建计数（来自 `import_report`）。
- **验收**：samplemodel2（25 部件/37 体）逐组切换与隔离正确、无残留高亮、几何不变；GUI 测试（offscreen）覆盖勾选与隔离两条路径。
- **成本**：1–1.5 天。

### P146 · 大模型 GUI 可用性（积压十三轮）

- **动作**：导入 → 视口构建 → 首次渲染耗时/内存实测（samplemodel2/5），必要时懒装配。
- **验收**：< 20 s 可交互 + 帧率基线。**成本**：1–1.5 天。

### P147 · 特征族与梁焊件｜P148 · 图面尺寸第二批｜P149 · 可选：`ref` 外部曲面库否证实验

### P150 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P145 -> P146 -> P147/P148 -> P149（可选）`（P150 收尾）

## 4. 纪律（延续 + R25 新增）

1–43 条延续。
44. **新增**：GUI 绑定前必须先在**无 Qt 的纯函数层**把语义测通（本轮 `isolate_group` 先落地并断言几何恒等），
    GUI 侧只做“事件 → 调函数 → 刷新”的薄封装，测试也分两层写（纯函数层 + offscreen 交互层）。