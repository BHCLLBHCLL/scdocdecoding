# 改进规划 R85（2026-09-12 立，R84 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R85。
> R84 交付见 `docs/ROUND_R84_20260912.md`（导入缝合容差 1e-5 落定 + 两条否证）。

## 1. R85 起点基线（R84 实测）

| 口径 | 实测 |
|---|---|
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 **347/76**、samplemodel2 **1242/356**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 逐体封闭率 | samplemodel2 **20/37 体完全封闭**；缺口集中在少数体 |
| 面数保真 | 5/6 样例完全一致；SampleModel4 149/177（ref 25/27） |
| 缝合容差 | 导入路径 `1e-5`（实测 −58 缺口、面数零损失） |
| 测试 | 全量 CI 口径全绿（**644 passed, 2 skipped, 8 deselected**）；`def test_` 扫描 627 条 |

## 2. R85 工作项（择优取一）

### P407 · 未封闭度：samplemodel2 的最大缺口体逐类定位

- **动作**：对 240/207/199 缺口那三个体，用 `free_edge_report.py --pair` 分类（两侧曲面组合 + 配对距离），
  并核对"这些体是否含 ref/缺曲线面"；挑最大一类修，或给出否证表。
- **验收**：分类表可数；缺口/自由环下降且面数、包围盒、耗时三不回退。**成本**：1 天。

### P408 · 产品面：装配爆炸图第二批

- **动作**：爆炸方向支持沿轴/径向 + 步进参数；八层接线（op / 命令 / 回放 / 视口）。
- **验收**：爆炸后各体位置可数（位移向量），撤销可还原；命令测试 + 回放测试。**成本**：1 天。

### P409 · 每轮维护（必做）｜P410 · 后续候选

- P409 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P410 候选：型材库标准规格表、图面尺寸链第三批、`prep.small` 重定向、SampleModel4 ref 27 面。

## 3. 建议顺序

`P407 -> P408 -> P409（收尾）`

## 4. 纪律（延续）

1–89 条延续。
