# R93 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R93）执行 P427：**环面裁剪再试**。
> R85 拒绝环面（0.54 s/面）、R89 定位到成本在"两个候选曲面 × 采样兜底"；本轮按这个诊断改造，
> **每面 0.54 → 0.045 s（12×）**，环面因此重新进入白名单，缺口与自由环再降一档。

## 1. 改造（按 R89 的诊断，不动闸门）

| 改动 | 之前 | 现在 |
|---|---|---|
| 候选曲面 | 两个（major/minor 与对调）都试裁剪 | **只试第一个**（对调读法留给窗口路径） |
| 失败兜底 | 直接跑 `_face_from_window(accurate=True)` 的采样 UV 校验 | **先便宜读法**（accurate=False），落模型外才升级到采样校验 |

单面成本实测：**0.54 s → 0.045 s**（36 面 19.5 s → **1.63 s**）；导入总耗时回到 R92 水平（samplemodel2 7.2–7.6 s）。

## 2. 实测结果

| 样例 | 真缺口 R92 → R93 | 自由环 | 缝边 | 面数 | 导入耗时 |
|---|---|---|---|---|---|
| SampleModel1 | 0 → 0 | 0 | 0 | 109 | 1.47 s |
| SampleModel4 | 347 → **335** | 76 → **72** | 16 → 11 | 149 → **145** | 1.48 s |
| samplemodel2 | 1002 → **990** | 212 → **195** | 81 → 72 | 1813（不变） | 7.57 s |
| samplemodel3 / 5 | 0 | 0 | 0 | 111 / 1288 | 0.11 / 2.86 s |
| samplemodel6 | 3 | 2 | 0 | 28 | 0.10 s |

合计：真缺口 **1352 → 1328**、自由环 **290 → 269**；包围盒漂移全部 0.0%。

## 3. SampleModel4 少 4 个面：不是丢料，是**修掉了"短一截"**

| 口径 | 环面裁剪（R93） | 记录窗口（R92） |
|---|---|---|
| 面数 | **145** | 149 |
| **全部面的总面积** | **0.188455 m²** | 0.181274 m² |

裁剪版少 4 个面（多出来的分裂片），但**总面积反而 +3.96%**——窗口版本来就把环面片裁短了。
所以这次面数下降是"修掉多余分裂 + 补回缺失面积"，已写入测试（面积必须 >3% 才算通过）。

## 4. 测试

| 文件 | 项 | 内容 |
|---|---|---|
| `tests/test_torus_trim_r93.py` | 3 | ①白名单：环/柱/球放行、锥面仍拒绝；②环面每面耗时 **<0.1 s**（实测 0.045）、可裁剪 ≥14/36；③结果与**面积证据**：裁剪 145 面 vs 窗口 149 面，但面积 >3% 且缺口更少 |
| `tests/test_import_sew_r84.py` | 改 | SampleModel4 (335,72)、samplemodel2 (990,195) |
| `tests/test_free_edges_r75.py` | 改 | 三元组：346/11/335、1062/72/990 |
| `tests/test_import_geometry_r73.py` | 改 | 上限收紧：(340,75)、(1000,200) |
| `tests/test_sphere_trim_r85.py` / `test_trim_surface_guard_r86.py` | 改 | 结果与白名单同步（环面由拒绝改为放行） |

全量（CI 口径 `-m "not official"`）**678 passed, 2 skipped, 8 deselected**；`def test_` 扫描 659 条（快照：18 页签 / 162 命令 / 177 live / 占位 1）。

## 5. 下一步（写入 R94）

P428 维护 + P429 候选择优：ref 27 面（唯一的面数缺口）、装配爆炸动画帧、尺寸链 SVG/DXF 端到端断言。
