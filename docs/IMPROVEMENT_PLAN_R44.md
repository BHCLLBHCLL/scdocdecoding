# 改进规划 R44（2026-09-12 立，R43 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R44。
> R43 交付见 `docs/ROUND_R43_20260912.md`（钣金成形契约清单）。

## 1. R44 起点基线

| 口径 | 实测 |
|---|---|
| 成形族 | 凸台/凹坑五项验收全通；百叶、敲落：**契约已定、代码未写** |
| 落点 | 镜像 `bead_groove(solid, face, radius, length=None, ...)` |
| 官方库 | 除 ref 族外 6/6 精确；打开到可交互 12.97/8.55 s |

## 2. R44 工作项（P247–P252）

### P247 · 实现 `louver`（最高优先，P241 续）

- **动作**：内核 `louver(solid, face, length, width, height, origin=None)`；闭式用开口面积×板厚；非法（≤0/超出面）+ 极端（0.5 mm 小百叶）+ 展开兼容。
- **验收**：三条断言 + 展开断言。**成本**：1 天。

### P248 · 实现 `knockout`（P242 续）

- **动作**：带筋环切；判据“切除面积占比”+ 单连通；非法（web ≥ 直径）+ 极端（单筋）+ 展开兼容。
- **验收**：三条断言 + 展开断言。**成本**：1 天。

### P249 · 接线（8 层模板）｜P250 · 梁/焊件首批｜P251 · 图面尺寸第二批｜P252 · 维护

- 接线沿用攻丝孔/凹坑已验证路径：内核 → 脚本 op → 特征 → 目录+live → 命令 → 回放 → 视口标记。

## 3. 建议顺序

`P247 -> P248 -> P249 -> P250 -> P251 -> P252`

## 4. 纪律（延续 + R44 新增）

1–60 条延续。
61. **新增**：实现前把**目标签名与参照签名并排写进轮次文档**（如 `louver` vs `bead_groove`），实现时逐参数对齐；
    这是 R33 之后“先读契约”的制度化形式。