# 改进规划 R93（2026-09-12 立，R92 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R93。
> R92 交付见 `docs/ROUND_R92_20260912.md`（槽钢 C 形截面 + 3 条标准规格 + 非对称形心双重对拍）。

## 1. R93 起点基线（R92 实测）

| 口径 | 实测 |
|---|---|
| 型材库 | 7 种截面（含 `c` 槽钢）；**15 条**标准规格（i 4 / l 3 / pipe 3 / flat 2 / c 3） |
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 347/76、samplemodel2 **1002/212**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 命令面 | 162 命令 / **177 live** / 占位 1（safety.tab） |
| 测试 | 全量 CI 口径全绿（**675 passed**，656 条 `def test_`） |

## 2. R93 工作项（择优取一）

### P427 · 环面裁剪"单候选 + 便宜兜底"再试

- **动作**：R89 测出环面成本在"两个候选曲面 × 采样兜底"。本轮：①只对**第一个候选**（major/minor）试裁剪；
  ②失败时不再对两个候选都跑 `_face_from_window(accurate=True)` 的采样校验，改为**先便宜窗口**（accurate=False），仅在窗口也落在模型外时才升级到采样校验。
- **验收**：36 个环面片总耗时 < 3 s；缺口/自由环下降；面数、包围盒、耗时三不回退；若不达标则保留否证。**成本**：0.5–1 天。

### P428 · 每轮维护（必做）｜P429 · 后续候选

- P428 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P429 候选：ref 27 面、装配爆炸动画帧、尺寸链 SVG/DXF 端到端断言（两条标注都出现）。

## 3. 建议顺序

`P427 -> P428（收尾）`

## 4. 纪律（延续）

1–89 条延续。
