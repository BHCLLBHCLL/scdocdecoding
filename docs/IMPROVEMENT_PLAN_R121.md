# 改进规划 R121（2026-09-12 立，R120 挑着修/批量改指向/只读护栏之后）

> 上轮：R120 交付 A-1 只修选中行、A-2 批量改指向（动作映射 + 目标映射）、A-3 导入结构只读，
> 见 `docs/ROUND_R120_20260912.md`。本文件 = R120 新暴露的候选 + 顺延项。

---

## A. 草图链（承 R120）

| # | 项 | 现状证据 | 验收（可复算） |
| --- | --- | --- | --- |
| A-1 | **预览可逐条勾掉** | 预览是一次性清单，要么全修要么只修选中行 | 清单带勾选状态：`repair_selected(ids)` 由勾选集合驱动，组合 2 次断言「勾掉的那条仍在、其余清空、文档其余逐项不变」 |
| A-2 | **导入组件的配合也只读** | 只读只看条目 `imported`，配合没有标记 | 组件带来源标记（导入/新建）：导入组件上的配合告警 `readonly=True`，修复拒绝、计划跳过；断言配合仍在 |
| A-3 | **改指向用下拉选草图** | GUI 只能填 `S<序号>` | 选项页给草图清单（或树上选中目标草图）；断言目标与所选一致、找不到时不改文档 |
| A-4 | **尺寸驱动预览** | 驱动后立即重解并重建，没有预览态 | 改值 → 半透明预览 → 提交/取消；断言「未提交体积不变、提交后 = 闭式」 |
| A-5 | **拉之前先重解表达式尺寸** | `extrude_active` 按**草图所画**拉伸（R111 口径） | 存了 `2*dim6` 而几何未解时，拉伸前自动 `redrive_expressions` → 体积 = 表达式闭式；同轮更新 R111 的锁口径测试 |
| A-6 | **沿曲线锚点可控** | 锚点固定为路径起点 | `pattern_curves(..., anchor=(u,v))`：指定点对准路径；断言锚点落在路径上且与起点偏移 = 指定值 |
| A-7 | **三维模式下选中草图曲线** | 草图只在草图模式可选 | 三维模式点中草图曲线 → 高亮 + 「双击进入草图编辑」；断言模式切换后选择集仍指向同一条曲线 |

## B. 装配与配合（承 R105/R106，仍未开工）

| # | 项 | 验收 |
| --- | --- | --- |
| B-1 | 装配层级 / 局部坐标系 | 两级链 A→B→C：移动 A 10mm，B、C 按链式矩阵更新（1e-12） |
| B-2 | 配合驱动量的多帧求解 | 螺旋副转 90°：位移 = pitch·(θ/2π) 闭式 |
| B-3 | 组件移动后重新求解 | 移动基座后 `solve_mates`：从动件位姿 = 重新求解的解 |
| B-4 | 脚本层文档级特征 | 脚本跑完 `document_features.ops()` 与 GUI 同序列 |
| B-5 | handler 端到端（QTimer） | 驱动 `det.params` / 树双击 / `asm.mate`，各断言一次闭式 |
| B-6 | 爆炸图走可回放平移 | 爆炸后 `can_replay` 真；还原后包围盒回原位 |

## C. 其他顺延项

1. **数字单一来源**：`gen_devplan_snapshot.py` 与 `state_inventory.py` 交叉校验（纪律 84）。
2. **配置语义扩展**：替换件 / 驱动尺寸 / 配置对比；BOM 行数与质量 = 快照值。
3. **工程图标注补齐**：孔标注 / 中心线 / 粗糙度 / 剖面线；DXF 与 SVG 实体计数 = 闭式条数。
4. **钣金折弯表 + 展开图工程化**：`ΔBA = θ/2·(t·K₂ − t·K₁)` 闭式；DXF 折弯线数 = 折弯数。
5. **未封闭度**：SampleModel4 335/72 → 缺口 < 200、环 < 40；samplemodel2 990/195 → 缺口 < 700。
6. **导入峰值内存**：768 MB（负载中）目标 −30%，否则否证表。
7. **P2 整域**：去毛刺 / 参考图像+标定 / 用户图层 / 3D PMI（边界）/ 型材库+尺寸链第四批 / `scdm_gui.py` 拆分。

## D. 明确不做（沿用）

ref 族 27 面（三条否证）、字节恒等、CATIA/Parasolid 直读、ACIS bit 等价、求解链、KeyShot 材质库、
3D PMI 读取（NYI-1..6）、`safety.tab` 空壳（按设计保留）。

## E. 口径（R121 起沿用）

1. **批量操作要能挑**（R120）：范围可缩到选中项，未选中的逐项不变。
2. **只读就是只读**（R120）：导入结构只报不改，计划要写「跳过」。
3. **动作与目标都可按范围给**（R120）：`{scope: 动作}`、`{id: 目标}`。
4. **两条路径合一**（R120）：`repair_all` 委托 `repair_selected`。
5. **修复要保留意图**（R119）：改指向 > 冻结 > 删除；**破坏性动作先说后做**（R119）；
   **作用域收到具体条目**（R119）。
6. **诊断的下一步是修复**（R118）；**删除按值不按下标**（R118）；**冻结 = 把当前几何写成数**（R118）。
7. **诊断指到「哪一条」/ 体检常驻结构树 / 破坏性动作给选择 / Qt 对象必须有活着的 QApplication**（R117）。
8. **标记能说话 / 体检覆盖所有按 ID 指向的结构 / 选择顺序是用户意图 / 不连续就拒绝**（R116）。
9. **诊断落在几何上 / 一行坏数据不许让整张报告失效 / 模式的真相在会话 / 文件类型看内容**（R115）。
10. **画出来的和能选中的是同一份几何 / 拾取偏好可预期 / 按折线走就按折线说 /
    失败指到具体对象且仍列可引用名**（R113/R114）。
11. **一个概念一个数 / 局部化与计数同源 / 测量只读 / 多闭环体积断言求和 /
    再拉一次不复制已建体 / 尺寸单位三分**（R107–R112）。
12. 每轮结束照旧：定向 + 定向回归批 + 全量回归 + 快照 `--check`（两种解释器）+ 同轮文档 + 提交推送。
