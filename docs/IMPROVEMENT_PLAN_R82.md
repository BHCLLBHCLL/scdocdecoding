# 改进规划 R82（2026-09-12 立，R81 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R82。
> R81 交付见 `docs/ROUND_R81_20260912.md`（两节点 nubs 记录修复 + ref 族逐边否证表）。

## 1. R82 起点基线（R81 实测）

| 口径 | 实测 |
|---|---|
| 面数保真 | 5/6 样例完全一致；SampleModel4 **149/177** |
| ref 族 | 52 面引用：**25 重建 / 27 未重建**（6 无边界、7 线框不闭合、51 条失败边全部只有 `ref` 载荷） |
| 真缺口 / 自由环 | SampleModel1 36/4、SampleModel4 **385/94**、samplemodel2 **1393/372**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 2/2 |
| 导入耗时 | 1.68 / 1.61 / 7.09 / 0.09 / 2.60 / 0.07 s |
| 占位命令 | 2 条（`prep.small`、`safety.tab`） |
| 测试 | 全量 CI 口径全绿（**637 passed, 2 skipped, 8 deselected**）；`def test_` 扫描 620 条 |

## 2. R82 工作项（择优取一）

### P401 · 未封闭度：裁剪失败面的 `invalid` 类

- **动作**：逐面统计裁剪失败的 `invalid` / `bbox-gap` / `wire-open` 条数与代表面；
  对 `invalid` 试 `ShapeFix_Face` 之外的两条路（`ShapeFix_Shape`、`BRepLib.SameParameter`），逐条给出能否修复的实测。
- **验收**：分类表可数；能修则缺口/自由环下降，面数、包围盒、耗时三不回退；不能修给出否证。**成本**：1 天。

### P402 · 产品面：几何体检命令

- **动作**：把 `open_edges`/`free_loops` 做成一条命令（一键体检 + 定位到面/体），八层接线（脚本 op / 命令 / 回放 / 视口标记）。
- **验收**：命令在 catalog + live + 命令测试 + 回放测试里各就位；体检结果与 `tools/free_edge_report.py` 复算一致。**成本**：1 天。

### P403 · 每轮维护（必做）｜P404 · 后续候选

- P403 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P404 候选：装配爆炸图第二批、型材库标准规格表、图面尺寸链第三批、占位命令 `prep.small`。

## 3. 建议顺序

`P401 -> P402 -> P403（收尾）`

## 4. 纪律（延续）

1–89 条延续。
