# 改进规划 R118（2026-09-12 立，R117 冗余指认/体检进树/保留实体之后）

> 上轮：R117 交付 A-1 冗余指认「重复了谁」、A-2 体检进结构树（可双击定位）、A-6 删体或保留可选，
> 见 `docs/ROUND_R117_20260912.md`。本文件 = R117 新暴露的候选 + 顺延项。

---

## A. 草图链（承 R117）

| # | 项 | 现状证据 | 验收（可复算） |
| --- | --- | --- | --- |
| A-1 | **冗余来源画出来** | 标记里有 `duplicate_of`，视口只按状态分色 | 冗余与被它重复的两条一起高亮（同色系深浅或虚线连接）；断言高亮引用集合 = {被重复, 冗余} 且与被指认一致 |
| A-2 | **体检一键修复** | 双击能定位，但仍要手工改 | 「把悬空引用冻结为当前数值」/「删除该约束」两个原子动作：体积/约束数与 `can_replay` 可复算；拒绝信息给出可修选项 |
| A-3 | **体检行覆盖配合/配置** | 只有草图与体能跳转 | 配合行 → 选中该组件对；配置行 → 切换到该配置；断言选择集/当前配置 |
| A-4 | **尺寸驱动预览** | 驱动后立即重解并重建，没有预览态 | 改值 → 半透明预览 → 提交/取消；断言「未提交体积不变、提交后 = 闭式」 |
| A-5 | **拉之前先重解表达式尺寸** | `extrude_active` 按**草图所画**拉伸（R111 口径） | 存了 `2*dim6` 而几何未解时，拉伸前自动 `redrive_expressions` → 体积 = 表达式闭式；同轮更新 R111 的锁口径测试 |
| A-6 | **沿曲线锚点可控** | 锚点固定为路径起点，几何不在起点时整体平移 | `pattern_curves(..., anchor=(u,v))`：指定点对准路径；断言锚点落在路径上且与路径起点偏移 = 指定值 |
| A-7 | **三维模式下选中草图曲线** | 草图只在草图模式可选；退出即清空 | 三维模式点中草图曲线 → 高亮 + 「双击进入草图编辑」；断言模式切换后选择集仍指向同一条曲线 |

## B. 装配与配合（承 R105/R106，仍未开工）

| # | 项 | 验收 |
| --- | --- | --- |
| B-1 | 装配层级 / 局部坐标系 | 两级链 A→B→C：移动 A 10mm，B、C 按链式矩阵更新（1e-12） |
| B-2 | 配合驱动量的多帧求解 | 螺旋副转 90°：位移 = pitch·(θ/2π) 闭式 |
| B-3 | 组件移动后重新求解 | 移动基座后 `solve_mates`：从动件位姿 = 重新求解的解 |
| B-4 | 脚本层文档级特征 | 脚本跑完 `document_features.ops()` 与 GUI 同序列 |
| B-5 | handler 端到端（QTimer） | 驱动 `det.params` / 树双击 / `asm.mate`，各断言一次闭式 |
| B-6 | 爆炸图走可回放平移 | 爆炸后 `can_replay` 真；还原后包围盒回原位 |

## C. 其他顺延项

1. **数字单一来源**：`gen_devplan_snapshot.py` 与 `state_inventory.py` 交叉校验（纪律 84）。
2. **配置语义扩展**：替换件 / 驱动尺寸 / 配置对比；BOM 行数与质量 = 快照值。
3. **工程图标注补齐**：孔标注 / 中心线 / 粗糙度 / 剖面线；DXF 与 SVG 实体计数 = 闭式条数。
4. **钣金折弯表 + 展开图工程化**：`ΔBA = θ/2·(t·K₂ − t·K₁)` 闭式；DXF 折弯线数 = 折弯数。
5. **未封闭度**：SampleModel4 335/72 → 缺口 < 200、环 < 40；samplemodel2 990/195 → 缺口 < 700。
6. **导入峰值内存**：768 MB（负载中）目标 −30%，否则否证表。
7. **P2 整域**：去毛刺 / 参考图像+标定 / 用户图层 / 3D PMI（边界）/ 型材库+尺寸链第四批 / `scdm_gui.py` 拆分。

## D. 明确不做（沿用）

ref 族 27 面（三条否证）、字节恒等、CATIA/Parasolid 直读、ACIS bit 等价、求解链、KeyShot 材质库、
3D PMI 读取（NYI-1..6）、`safety.tab` 空壳（按设计保留）。

## E. 口径（R118 起沿用）

1. **诊断要指到「哪一条」**（R117）：冗余给出被重复的序号，三条重复指向原始那条。
2. **体检要常驻**（R117）：结构树节点，不是会滚掉的状态栏。
3. **破坏性动作要给选择**（R117）：删除是默认，但要能「保留（断开参数）」；两种都要点名。
4. **Qt 对象必须有活着的 QApplication**（R117）：测试统一 `_ensure_app()` 挂模块全局。
5. **标记要能说话**（R116）；**体检覆盖所有按 ID 指向的结构**（R116）；
   **选择顺序是用户意图**（R116）；**不连续就拒绝**（R116）。
6. **诊断落在几何上**（R115）；**一行坏数据不许让整张报告失效**（R115）；
   **模式的真相在会话**（R115）；**文件类型看内容**（R115）；**删要有据、可回退**（R115）。
7. **画出来的和能选中的是同一份几何**（R114）；**拾取偏好可预期**（R114）；
   **按折线走就按折线说**（R114）；**失败指到具体对象且仍列可引用名**（R113/R114）。
8. **一个概念一个数**（R112/R113）；**局部化与计数同源**（R112）；**测量只读**（纪律 85）；
   **多闭环体积断言求和**（R107）；**再拉一次不复制已建体**（R112）。
9. **尺寸单位三分**（R110）：约束里数值是米、表达式与 `dimN` 是毫米、API/UI 传入是毫米。
10. 每轮结束照旧：定向 + 定向回归批 + 全量回归 + 快照 `--check`（两种解释器）+ 同轮文档 + 提交推送。
