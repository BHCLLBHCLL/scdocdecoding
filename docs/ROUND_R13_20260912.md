# R13 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R13）执行 P75（最后 2 个圆柱弧面 + 1 个未重建面）。
> 结论：**两条假设均被实测否定**（代码已回滚到 R12 状态），并把“2 个切分到底由哪条分支产生”升级为下一步的仪器化任务。下一轮规划见 `docs/IMPROVEMENT_PLAN_R14.md`。
> 另：本文件与代码在同一次提交中推送到远程（纪律 25）。

## 两条尝试与实测（均已回滚）

| 尝试 | 假设 | 实测 | 处置 |
|---|---|---|---|
| G 圆柱 O(1) 预筛（解析弧跨度 > π 时试记录窗口） | 记录窗口是 76° 窄弧，可避免布尔把面切两片 | 切分表**完全不变**（仍 `{2: 2} kinds {'cone': 2}`），面数 1811 不变 | 回滚（未触发；判据与这些面无关） |
| H 退化投影下用投影角跨度 | 薄片面的投影凸包退化 → 退回整圈 2π → 被布尔切两片 | 同样**完全不变** | 回滚（假设否定） |

两条都	extbf{没有}触发或没有效果，说明这 2 个切分**不是**由“解析窗口/退化凸包”这条分支产生的。

## 当前状态（R12 基线，已验证）

| 样例 | SAB | 实测 | 漂移 |
|---|---|---|---|
| SampleModel1 | 1/109 | **精确** | 0.0% |
| SampleModel4 | 2/177 | 2/143（未重建 35；= 177 − 35 + 1） | 0.0% |
| samplemodel2 | 37/1813 | 37/**1811**（−2） | 0.6%（4 个碎片被丢） |
| samplemodel3 / 5 / 6 | — | **精确** | 0.0% |

| 套件 | 结果 |
|---|---|
| `tests/test_import_fidelity.py` | **22 passed** |
| 切分 | 仅 `{2: 2} kinds {'cone': 2}`（模型 2 面 8/194） |
| 未重建 | samplemodel2 1 个面、SampleModel4 35 个面 |

## 下一步（写入 R14）

**仪器化 `_rebuild_face`**：让它返回“本面由哪条分支产出（解析窗口 / 记录窗口 / 布尔裁剪 / 多边形兜底）以及片数”，
然后对这 2 个面直接读出分支——不再猜。这是 R11/R13 两轮“假设→实测→否定”之后唯一能收敛的做法。