# 改进规划 R34（2026-09-12 立，R33 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R34。
> R33 交付见 `docs/ROUND_R33_20260912.md`（回放体积等价）。

## 1. R34 起点基线

| 口径 | 实测 |
|---|---|
| 攻丝孔 | 内核 ✅ / 符号 ✅ / 特征+脚本 ✅ / 回放 ✅ / **命令+视口 ⏳** |
| 线程测试 | 12 项（p175 3 / p181 5 / p187 4） |
| 官方库 | 除 ref 族外 6/6 精确；打开到可交互 12.97/8.55 s |

## 2. R34 工作项（P199–P204）

### P199 · 攻丝孔命令与视口示意（最高优先，P187 收尾）

- **动作**：①`prep.hole` 增“攻丝”选项 + 公称/螺距输入，非法参数弹用户可读提示（含具体规则“螺距须为正且小于公称直径”）；
  ②记录到特征历史（`Feature(op="hole_tapped")`）；③视口用 `thread_lines` 画示意 actor（不进拾取/布尔）。
- **验收**：命令级行为测试（含非法输入分支）1 条；示意调用前后 `K.volume` 不变 1 条；GUI offscreen 测试覆盖选项路径。**成本**：1–1.5 天。

### P200 · 成形族（凸台/凹坑/百叶/敲落）｜P201 · 梁/焊件首批｜P202 · 图面尺寸第二批｜P203 · 可选补测

### P204 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P199 -> P200 -> P201 -> P202 -> P203（可选）`（P204 收尾）

## 4. 纪律（延续 + R34 新增）

1–52 条延续。
53. **新增**：测试判据必须对准**契约**（体积、可见集合、几何恒等），不要对准**实现细节**（消息文案、内部计数）——
    本轮初版断言回放消息含“攻丝”即被实测否定；此类断言要么改成契约判据，要么明确标注为“文案回归锁”。