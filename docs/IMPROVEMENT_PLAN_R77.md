# 改进规划 R77（2026-09-12 立，R76 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R77。
> R76 交付见 `docs/ROUND_R76_20260912.md`（缺口配对仪器 + 柱面真实裁剪按样例启用；缺口 56→36、环 24→4；新增纪律 89）。

## 1. R77 起点基线（R76 实测）

| 口径 | 实测 |
|---|---|
| **真缺口 / 自由环** | SampleModel1 **36/4**、SampleModel4 **387/96**、samplemodel2 **2145/396**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 2/2 |
| 缝边（非缺口） | 0 / 18 / 82 / 0 / 0 / 0 |
| 裁剪策略 | SampleModel1/2/4 启用（裁剪率 0.87/0.58/0.57），samplemodel5/6 不启用（0.18/0.00）；开关按导入还原 |
| 面数保真 | 除 ref 族（SampleModel4 33 面）外 6/6 精确；samplemodel2 1813/1813、丢弃 0 |
| 导入耗时 | 1.58 / 1.45 / 7.24 / 0.11 / 2.51 / 0.07 s（samplemodel2 +17%，已记录） |
| 占位命令 | 2 条（`prep.small`、`safety.tab`） |
| 测试 | 全量（CI 口径）**627 passed, 2 skipped, 8 deselected**；`def test_` 扫描 609 条（18 页签 / 162 命令 / 176 live / 占位 2） |

## 2. R77 工作项（P379–P382）

### P379 · 剩余缺口逐类定位（最高优先）

- **动作**：用 `free_edge_report.py --pair` 对 SampleModel1（36 条）与 samplemodel2（2145 条）**分类**：
  ①配对面对的曲面组合与分支（裁剪/窗口/线框）；②配对距离分布；③缺口几何（弧长、是否在片的"斜切"处）。
  优先修条数最多的一类；若某类来自"裁剪未启用的面"（同一样例里另一半柱面），先量清它差多少。
- **验收**：分类表可数（每类条数 + 复算命令）；修掉一类并给出缺口/自由环前后对比；面数、包围盒、导入耗时不回退。**成本**：1–2 天。

### P380 · 真缺口进产品指标

- **动作**：`import_report` 增加 `open_edges`/`seam_edges`/`free_loops`（导入完成后一次性测）；
  状态行/部件树显示；措辞函数放 `scdm.import_sab`（Qt-free）。
- **验收**：与 `tools/free_edge_report.py` 复算一致；导入耗时不回退（含测量本身的开销）。**成本**：0.5 天。

### P381 · 每轮维护（必做）｜P382 · 后续候选

- P381 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P382 候选：ref 族 33 面、装配爆炸图、型材库标准规格表、图面尺寸链第三批、占位命令 `prep.small`。

## 3. 建议顺序

`P379 -> P380 -> P381（收尾）`

## 4. 纪律（延续）

1–88 条延续。

89. **比较几何对象要排除共享端点的伪重合**（R76：全采样点最小距离把 46/56 缺口报成"完全重合"；
    改用内部采样点后是 0 条重合、中位差 1.64 mm）。
