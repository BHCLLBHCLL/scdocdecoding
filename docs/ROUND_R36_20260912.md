# R36 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R36）执行 P211 第一段：**命令入口**。
> 交付：`create.hole_tapped` 进入目录与 live 集合（派发自动映射到 R35 的处理器）；视口牙型示意转 R37。

## 改动

`scdm/catalog.py`：
1. 目录新增 `C("create.hole_tapped", "攻丝孔", "Tapped", "M4", "cyl")`（与沉头/锥沉同组）；
2. live 集合加入 `create.hole_tapped`（与 `create.hole` 等并列）。

派发规则（`fn = getattr(self, "_do_" + cmd_id.replace(".", "_"))`）自动把该命令接到 R35 的 `_do_create_hole_tapped`，
因此不需要额外接线——这正是纪律 55 的意义：**实现、目录、live 三处缺一即“点不到”**。

## 验证

| 套件 | 结果 |
|---|---|
| `tests/test_coverage_ledger.py`（命令面守卫） | 见收尾实跑 |
| `tests/test_lightweight_gui.py`（含攻丝孔命令用例） | 12 项 |

## 下一步（写入 R37）

①视口牙型示意 actor（用 `thread_lines`，不进拾取/布尔，断言示意前后体积不变）；
②P212 成形族（凸台/凹坑/百叶/敲落）。