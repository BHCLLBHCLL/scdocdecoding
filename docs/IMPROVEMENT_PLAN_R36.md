# 改进规划 R36（2026-09-12 立，R35 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R36。
> R35 交付见 `docs/ROUND_R35_20260912.md`（攻丝孔命令层 `_do_create_hole_tapped`）。

## 1. R36 起点基线

| 口径 | 实测 |
|---|---|
| 攻丝孔 | 内核 ✅ / 符号 ✅ / 特征+脚本 ✅ / 回放 ✅ / 命令 ✅ / **入口（目录·彩带）+ 视口示意 ⏳** |
| 线程测试 | 12 项 + GUI 命令测试 1 项 |
| 官方库 | 除 ref 族外 6/6 精确；打开到可交互 12.97/8.55 s |

## 2. R36 工作项（P211–P216）

### P211 · 攻丝孔入口 + 视口示意（最高优先）

- **动作**：①目录登记 `create.hole_tapped`（M4 页签 + live 集合，派发自动走 `_do_create_hole_tapped`）；②视口用 `thread_lines` 建**独立示意 actor**（不进拾取/布尔）；③覆盖台账更新。
- **验收**：`create.hole_tapped` 出现在命令面且为 live；示意前后 `K.volume` 不变；覆盖台账守卫测试通过。**成本**：0.5–1 天。

### P212 · 成形族（凸台/凹坑/百叶/敲落）｜P213 · 梁/焊件首批｜P214 · 图面尺寸第二批｜P215 · 可选补测

### P216 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P211 -> P212 -> P213 -> P214 -> P215（可选）`（P216 收尾）

## 4. 纪律（延续 + R36 新增）

1–54 条延续。
55. **新增**：新命令必须**同时**出现在（a）目录 `C(...)`、（b）live 集合、（c）处理器 `_do_*` 三处；
    派发是 `_do_<cmd_id>` 自动映射，所以漏掉 (a)/(b) 会“实现正确但点不到”——每轮以覆盖台账测试兜底。