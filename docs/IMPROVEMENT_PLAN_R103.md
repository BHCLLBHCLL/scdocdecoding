# 改进规划 R103（2026-09-12 立，R102 特征编辑闭环交付之后）

> 上轮：R102 交付了 P0-1（特征参数编辑闭环，见 `docs/ROUND_R102_20260912.md`）。
> 本文件 = R102 未完成项（顺延）+ R102 暴露的新候选（入册优先）。
> 排序原则与纪律不变：每项给「可复算常数」验收（纪律 83）与八层接线（纪律 65）。

---

## A. R102 新暴露的候选（先做，都是可复算的小闭环）

| # | 项 | 现状证据（实测） | 验收（可复算） |
| --- | --- | --- | --- |
| A-1 | **刚体变换 base 随动** | `tool.move` / `op_move` 只改当前形状，`base_shape` 不动 → 移动后 改特征参数被拒（理由「包围盒/体积不一致」） | 平移 (10,0,0) 后改孔径：`can_replay` 为真，体积 = 闭式，包围盒整体 +10mm；旋转同理（用面心坐标复算） |
| A-2 | **脚本成形类 op 补记特征** | R102 只补了孔族/抽壳/倒圆/倒角/拉动；`op_dimple/op_louver/op_knockout/op_gusset/op_tab/op_sheet_junction/op_sheet_cross_break/op_boss` 仍未记录 | 每个 op 一条：脚本跑完后 `stack.ops()` 等于该 op，且改一个参数后体积落在该特征的闭式值 |
| A-3 | **对话框交互测试** | 对话框本体只被冒烟测试覆盖（构造），解析/应用逻辑已在库层单测 | 用 QTest 打开 `det.params`、写入 `B1 0 diameter = 8`、确定 → 体积闭式；不可重放体显示为注释行 |
| A-4 | **特征树的参数编辑入口** | 结构树已有"特征历史"节点，但双击不能改参数 | 双击特征 → 弹参数编辑 → 与 A-3 同一断言 |

## B. R102 顺延项（原 P0-2 / P1 / P2）

1. **P0-2 数字单一来源**：`gen_devplan_snapshot.py` 与 `state_inventory.py` 四个数交叉校验测试（纪律 84）。
2. **P1-1 装配配合 GUI 闭环**：剩余 DOF 必须等于 `mates.DOFS` 表值；过约束报错。
3. **P1-2 配置语义扩展**：替换件 / 驱动尺寸 / 配置对比；BOM 行数与质量 = 快照值。
4. **P1-3 工程图标注补齐**：孔标注 / 中心线 / 粗糙度 / 剖面线；DXF 与 SVG 实体计数 = 闭式条数。
5. **P1-4 钣金折弯表 + 展开图工程化**：`ΔBA = θ/2·(t·K₂ − t·K₁)` 闭式；DXF 折弯线数 = 折弯数。
6. **P1-5/P1-6 未封闭度**：SampleModel4 335/72 → 目标 缺口 < 200、环 < 40；samplemodel2 990/195 → 缺口 < 700，
   导入时间不增 > 10%（`PERF_BASELINE.json` 对比）。
7. **P1-7 导入峰值内存**：768 MB（负载中）目标 −30%，否则出否证表。
8. **P2-1..P2-6**：去毛刺 / 参考图像+标定 / 用户图层 / 3D PMI（边界）/ 型材库+尺寸链第四批 / `scdm_gui.py` 拆分。

## C. 明确不做（沿用）

ref 族 27 面（三条否证）、字节恒等、CATIA/Parasolid 直读、ACIS bit 等价、求解链、KeyShot 材质库、
3D PMI 读取（NYI-1..6）、`safety.tab` 空壳（按设计保留）。

## D. 纪律

1–89 条延续；R101 提出的候选 90（门禁每格要能用反向条件复现）与 91（UI 承诺 = 实现并集）在 R102 的两处修复里
已各被验证一次，**R103 起正式入册为纪律 90/91**。
