# 改进规划 R71（2026-09-12 立，R70 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R71。
> R70 交付见 `docs/ROUND_R70_20260912.md`（cross-break 浅 V/圆弧压筋 + 五项验收 + 八层接线）。

## 1. R71 起点基线

| 口径 | 实测 |
|---|---|
| 钣金 | 折弯链/圆锥/轴向/接缝/卷边/加强筋/撕裂/角释放 + **cross-break ✅**（V 与圆弧，闭式 rel ≤ 4e-14） |
| 图面 | 尺寸链与公差叠加 ✅ / 引线 + 形位公差 + 样式/基准/复合公差 ✅ |
| CAE | 面网格/门槛/体素/贴体裁剪/贴体四面体/误差预算/仿真报告 ✅ |
| 装配配置 | 可见性/抑制/位姿 + 属性快照 + BOM ✅ / .scdm v5 ✅ |
| 占位命令 | 2 条（`prep.small`、`safety.tab`） |
| 测试 | 全量（CI 口径）**598 passed, 2 skipped, 8 deselected**；`def test_` 扫描 583 条（18 页签 / 162 命令 / 176 live / 占位 2） |
| 官方库 | 除 ref 族外 6/6 精确（沿用 R48 实测）；打开到可交互 12.97 / 8.55 s |

## 2. R71 工作项（P354–P357）

### P354 · 可选补测：内存与帧率基线（最高优先，已顺延多轮）

- **动作**：沿用 `tests/test_perf_budget.py` 口径补一次内存峰值与帧率基线；与既有预算不冲突则**只记录不设新阈值**。
- **验收**：内存与帧率各一条可复算记录（含机器/规模口径）+ 与既有预算对照结论。**成本**：0.5 天。

### P355 · 每轮维护（必做）

- 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）。

### P356 · 导入保真：ref 族攻坚（本轮第二优先，若预算允许）

- **动作**：先做**差异定位**（ref 族与已精确族的几何/拓扑差异逐项计数：面数/边数/曲面类型/容差），产出可数的缺陷清单；
  不追求一次清零，先把"差在哪里、差多少"写成表。
- **验收**：差异表（每项可数）+ 至少一项差异有修复或明确结论（为何不能修）。**成本**：1–1.5 天。

### P357 · 图面尺寸链第三批（候选）

- **动作**：极值/统计两种叠加**并列标注**（同一链两条总尺寸）+ 链的图形化（链线）；导出同步。
- **验收**：两条标注都在 SVG/DXF + 链线为注记（不入几何）+ undo 往返。**成本**：0.5–1 天。

## 3. 建议顺序

`P354 -> P355 -> P356（预算允许）`；P357 择机。

## 4. 纪律（延续）

1–84 条延续，本轮不新增。
