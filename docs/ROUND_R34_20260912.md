# R34 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R34）执行 P199 第一步：把 R33 回滚的**回放断言**按契约正确地补上。
> 交付：`replay` 端到端断言通过（4 项），**攻丝孔链路四层全部有独立测试**。

## R33 失败的原因与本轮修法

R33 的回放测试之所以失败，是它**同时耦合了两件事**：`insert.box` 的 opts 契约 + 孔 op 的回放。
本轮按纪律 52（每层一条可独立失败的测试）改为**只回放孔那一步**：

```python
for _ in range(2):                       # 两次独立回放，验证确定性
    doc = KernelDoc()
    body = doc.add_body(K.make_box(0.02, 0.02, 0.02))   # 建体不走 op，绕开其契约
    replay([{"cmd": "create.hole_tapped", "opts": {...}}], doc, 1000.0)
```

断言两条（都对契约）：①切除量 == 底孔闭式（rel 1e-3）；②两次回放体积**逐位一致**（rel 1e-6）。

## 测试

| 套件 | 结果 |
|---|---|
| `tests/test_threads_p187.py` | **4 passed** |
| 线程三文件合计 | **12 项** |

## 攻丝孔链路完成度

| 层 | 状态 | 测试 |
|---|---|---|
| 内核 `hole_tapped` | ✅ | 3 项（闭式/非法三连/盲孔） |
| 符号层 `threads.py` | ✅ | 5 项（规格/环带/轴/不改体积） |
| 特征层 + 脚本 op | ✅ | 3 项（标签/注册/幂等） |
| 脚本回放 | ✅ **本轮** | 1 项（体积等价 + 确定性） |
| 命令选项 + 视口示意 | ⏳ | 转 R35 |

## 下一步（写入 R35）

命令层 `prep.hole` 攻丝选项（非法参数可读提示 + 记入特征历史）与视口牙型示意 actor（不进拾取/布尔）；
先读 `op_insert_box`/命令注册处的契约，再动手（R33 的教训）。