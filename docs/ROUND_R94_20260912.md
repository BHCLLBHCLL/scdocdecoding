# R94 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R94）执行 P431：**尺寸链导出侧端到端断言**。
> 交付：把 R90 的两条并列标注 + 链线**送进两个导出器**（DXF / SVG）并读回文件断言文字，
> 顺带把"导出不可能碰几何"这条结构性事实也钉成测试。

## 1. 为什么需要这一轮

R90 只验证到"对象被造出来"；导出侧是靠 `drawing.dim_text` 与 `annotation.annotation_geometry` 单点渲染的，
但**没有任何测试**保证两条标注与链线真的出现在文件里（历史上公差就发生过"画布有、导出没有"的问题）。
本轮补上端到端：同一个链 → 导出 → 读文件 → 断言文字。

## 2. 验收（每条都可复算）

| 检查 | 期望 | 实测 |
|---|---|---|
| DXF 含两条标注 | `60.0 ±0.6` 与 `60.0 ±0.374` 都在文件文本里 | ✅ |
| DXF 含链线 | `尺寸链 3 段` 且该折线落在 `NOTE` 层 | ✅ |
| SVG 含两条标注 + 链线 | 同上三条文字都在 | ✅ |
| 导出不碰几何 | 同一实体导出前后**体积与包围盒逐分量不变**（导出器根本不接收 shape） | ✅ 1e-12 |

## 3. 测试

| 文件 | 项 | 内容 |
|---|---|---|
| `tests/test_chain_export_r94.py` | 3 | ①DXF 端到端（两条公差文字 + 链线文字 + NOTE 层）；②SVG 端到端；③导出前后实体体积/包围盒不变 |

全量（CI 口径 `-m "not official"`）**681 passed, 2 skipped, 8 deselected**；`def test_` 扫描 662 条（快照：18 页签 / 162 命令 / 177 live / 占位 1）。

## 4. 下一步（写入 R95）

P430 ref 族 27 面（**唯一的面数缺口**，按"姊妹面反推"方向做最后一轮攻坚）→ P432 维护；
其余候选：装配爆炸动画帧、性能基线复测（导入路径已多次变动）、GProp 闸门缓存化。
