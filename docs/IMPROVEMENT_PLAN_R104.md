# 改进规划 R104（2026-09-12 立，R103 A 组交付之后）

> 上轮：R103 交付 A 组四项（变换不变量、脚本成形记录、对话框逻辑抽出、特征树入口），
> 见 `docs/ROUND_R103_20260912.md`。本文件 = R103 新暴露的候选 + R103 未完成项（原 B 组顺延）。
>
> 纪律 90/91 自 R104 起正式生效（R101 提出、R102 验证、R103 再次适用）：
> **90** 门禁的每一格必须能被"反向条件"复现；**91** UI 承诺 = 实现并集。

---

## A. R103 新暴露的候选（先做）

| # | 项 | 现状证据 | 验收（可复算） |
| --- | --- | --- | --- |
| A-1 | **配合/对齐的位姿回放** | `asm.mate` 的 face-flush/axis-align + 偏移直接改 `body.shape`，位姿不可回放 → 之后改特征参数被拒 | 对齐两体后 `can_replay` 为真；对齐体与"先对齐再建特征"的体体积/面数一致（rel 1e-6） |
| A-2 | **UI 旋转入口** | `rotate_body` 只有 API/脚本可达（R103 实测：GUI 移动工具只平移） | 移动工具选项加"旋转 90°"或角度输入；旋转后包围盒 = 解析映射，参数仍可改 |
| A-3 | **脚本层文档级特征** | `op_combine` / `op_split` / `op_mirror` 不写 `record_doc_feature`，脚本结果的整档重放链缺一环 | 脚本跑完 `document_features.ops()` 与 GUI 同序列；`replay_document(apply=True)` 后体积 = 原值（rel 1e-6） |
| A-4 | **对话框/树 handler 端到端** | 模态 `exec_` 循环只由冒烟测试覆盖 | 用 `QTimer` 驱动 `_do_det_params` 与树双击，断言体积闭式与撤销点各一次 |

## B. 顺延项（R102/R103 未完成，按 ROI 排序）

1. **P0-2 数字单一来源**：`gen_devplan_snapshot.py` 与 `state_inventory.py` 四个数交叉校验（纪律 84）。
2. **P1-1 装配配合 GUI 闭环**：剩余 DOF 必须等于 `mates.DOFS` 表值；过约束报错（与 A-1 同源，可合并）。
3. **P1-2 配置语义扩展**：替换件 / 驱动尺寸 / 配置对比；BOM 行数与质量 = 快照值。
4. **P1-3 工程图标注补齐**：孔标注 / 中心线 / 粗糙度 / 剖面线；DXF 与 SVG 实体计数 = 闭式条数。
5. **P1-4 钣金折弯表 + 展开图工程化**：`ΔBA = θ/2·(t·K₂ − t·K₁)` 闭式；DXF 折弯线数 = 折弯数。
6. **P1-5/P1-6 未封闭度**：SampleModel4 335/72 → 缺口 < 200、环 < 40；samplemodel2 990/195 → 缺口 < 700，
   导入时间不增 > 10%。
7. **P1-7 导入峰值内存**：768 MB（负载中）目标 −30%，否则否证表。
8. **P2-1..P2-6**：去毛刺 / 参考图像+标定 / 用户图层 / 3D PMI（边界）/ 型材库+尺寸链第四批 / `scdm_gui.py` 拆分。

## C. 明确不做（沿用）

ref 族 27 面（三条否证）、字节恒等、CATIA/Parasolid 直读、ACIS bit 等价、求解链、KeyShot 材质库、
3D PMI 读取（NYI-1..6）、`safety.tab` 空壳（按设计保留）。

## D. 本轮复用的交付口径（供 R104 参考）

- 特征编辑闭环的入口：`FeatureStack.editable()`（白名单）→ `KernelDoc.edit_feature()`（原子、带体积前后）
  → `scdm/gui/params.py`（对话框与树都走这里）；
- 位姿不变量：`KBody.base_pose` 必须随任何刚体变换一起更新，否则 `can_replay` 会（按纪律 86）明确拒绝；
- 每轮结束照旧：定向 + 全量回归 + 快照 `--check`（两种解释器）+ 同轮文档 + 提交推送。
