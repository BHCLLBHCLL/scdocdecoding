# 改进规划 R29（2026-09-12 立，R28 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R29。
> R28 交付见 `docs/ROUND_R28_20260912.md`（分段计时：samplemodel2 11.29 s、samplemodel5 7.32 s，达标；内存待补测）。

## 1. R29 起点基线

| 口径 | 实测 |
|---|---|
| 打开到可交互 | samplemodel2 **11.29 s**、samplemodel5 **7.32 s**（< 20 s 达标） |
| 分段 | GUI 初始化 0.06–0.45 s；导入+视口构建 7.27–10.84 s（瓶颈在导入，不在装配） |
| 待补 | 内存峰值（`psapi` 返回 0，需 `K32GetProcessMemoryInfo` 或 `psutil`）；真实帧率（需有窗口环境） |
| 除 ref 族外 | 6/6 精确 |

## 2. R29 工作项（P169–P174）

### P169 · 补齐性能证据（最高优先，收尾 P163）

- **动作**：①内存改用 `ctypes.windll.kernel32.K32GetProcessMemoryInfo`（或 `pip install psutil` 后 `psutil.Process().memory_info().peak_wset`），
  把 samplemodel2/5 的峰值补进 R28 表；②在**有窗口**环境（`QT_QPA_PLATFORM=windows`）测静止与旋转帧率并写入轮次文档。
- **验收**：表里三列（分段耗时/内存峰值/帧率）齐全，且< 20 s 结论不变。**成本**：0.5 天。

### P170 · 特征族与梁焊件（广度，最高优先并行）

- **动作**：孔向导（直径/深度/攻丝标注）、螺纹示意、凸台/凹坑；6 种梁轮廓 + 沿草图/边生成。
- **验收**：闭式体积/包围盒断言 + 官方外形对照。**成本**：4–6 天（可拆两轮）。

### P171 · 图面尺寸第二批（多选/吸附/undo 接线）｜P172 · 可选：`ref` 外部曲面库否证实验

### P173 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P169 -> P170 -> P171 -> P172（可选）`（P173 收尾）

## 4. 纪律（延续 + R29 新增）

1–47 条延续。
48. **新增**：性能证据缺项时**不要留白**——要么补测，要么在表里写明“未测到 + 原因 + 补测方法”（本轮内存一栏即按此写），
    禁止用“不可测”含糊带过。