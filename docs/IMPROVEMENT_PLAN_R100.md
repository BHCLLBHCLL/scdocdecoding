# 改进规划 R100（2026-09-12 立，R99 收尾后重新基线）——收官轮

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R100（收官）。
> R99 交付见 `docs/ROUND_R99_20260912.md`（爆炸动画帧：闭式插值 + 按帧应用）。

## 1. R100 起点基线（R99 实测）

| 口径 | 实测 |
|---|---|
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 **335/72**、samplemodel2 **990/195**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 面数 | 5/6 与声明一致；SampleModel4 **145/177**（ref 族 27 面：6 无边界 + 21 曲线不全，数据不在本 part） |
| 导入耗时（空载） | 1.39 / 1.34 / 6.89 / 0.09 / 2.45 / 0.08 s |
| 导入内存 | 解析 +240.9 MB（属调用方）/ 建面+缝合 +153.7 MB；峰值 415.3 MB（新进程 samplemodel2） |
| 命令面 | 18 页签 / 162 命令 / **177 live** / 占位 **1**（safety.tab，按设计保留） |
| 产品面 | 尺寸链并列标注 + 链线 + 导出端到端；爆炸图三模式 + 动画帧；型材库 7 截面 / 15 规格；几何体检（未封闭度 + 缺口标记） |
| 测试 | 全量 CI 口径全绿（**694 passed**，675 条 `def test_`） |

## 2. R100 工作项：P442 收官

- **动作**：①用与 R80 相同的口径复测官方六样例验收总表（面数/真缺口/自由缝边/ref 族/耗时/内存）；
  ②全量回归 + 快照 `--check`；③写 `docs/ROUND_R100_20260912.md` 的账本（R81–R100）与完成判定；
  ④遗留候选写进 `docs/IMPROVEMENT_PLAN_R101.md`（若仍有可做项）或给出"无新的 R* 修改"结论。
- **验收**：总表可复算；全量绿灯；快照一致；工作区干净、`HEAD == origin/main`。**成本**：0.5 天。

## 3. 纪律（延续）

1–89 条延续（R95–R99 未新增条目：这几轮的教训都落在既有条目上——
85（测量顺序也是扰动）、84（一处实现）、以及"模块级状态必须按导入/按调用重置"这条已并入 73/95 的注释）。
