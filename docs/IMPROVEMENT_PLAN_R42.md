# 改进规划 R42（2026-09-12 立，R41 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R42。
> R41 交付见 `docs/ROUND_R41_20260912.md`（凹坑回放等价，成形族四项验收齐备）。

## 1. R42 起点基线

| 口径 | 实测 |
|---|---|
| 圆形凹坑 | 闭式/非法/极端/回放 四项齐备；**视口标记 ⏳** |
| 攻丝孔 | 八层全通 |
| 测试 | forming 8 项 + 线程 12 项 + GUI 13 项 |
| 官方库 | 除 ref 族外 6/6 精确；打开到可交互 12.97/8.55 s |

## 2. R42 工作项（P235–P240）

### P235 · 凹坑成形标记（最高优先）

- **动作**：`_do_create_dimple` 成功后状态栏/面包屑给出成形提示；必要时用 `Scene` 画环状标记（不可拾取、不入包围盒，纪律 56）；改选/删除时清除。
- **验收**：offscreen 命令测试断言标记挂载与清除；几何不变。**成本**：0.5 天。

### P236 · 百叶 / 敲落（钣金成形）

- **动作**：先读 `sheetmetal.py` 契约，再实现 louver/knockout；每项闭式 + 非法 + 极端；说明与展开的兼容性。
- **验收**：断言齐备 + 展开兼容性说明。**成本**：1.5–2 天。

### P237 · 梁/焊件首批｜P238 · 图面尺寸第二批｜P239 · 可选补测（内存/帧率）

### P240 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P235 -> P236 -> P237 -> P238 -> P239（可选）`（P240 收尾）

## 4. 纪律（延续 + R42 新增）

1–58 条延续。
59. **新增**：每轮收尾时更新“能力矩阵”（族 × 层 × 验收四项）到轮次文档，
    让“哪一层/哪项验收缺”一眼可见，避免部分接入被遗忘（本轮 R41 的矩阵即此格式）。