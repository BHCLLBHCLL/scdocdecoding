# 改进规划 R38（2026-09-12 立，R37 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R38。
> R37 交付见 `docs/ROUND_R37_20260912.md`（不可拾取的牙型示意 actor）。

## 1. R38 起点基线

| 口径 | 实测 |
|---|---|
| 攻丝孔 | 内核 ✅ / 符号 ✅ / 特征+脚本 ✅ / 回放 ✅ / 命令 ✅ / 入口 ✅ / 视口示意 ✅（抽象层） |
| 待接 | 命令成功后自动显示示意 + 改选/删除时清除 |
| 官方库 | 除 ref 族外 6/6 精确；打开到可交互 12.97/8.55 s |

## 2. R38 工作项（P217–P222）

### P217 · 示意自动挂载（最高优先，收货攻丝孔链路）

- **动作**：`_do_create_hole_tapped` 成功后调用 `scene.show_thread_annotation(...)`（用选中面法向与底孔参数）；
  切换选择/删除实体/新建文档时 `clear_thread_annotation()`；命令级测试断言“成功后有 actor、删除后无 actor”。
- **验收**：offscreen 命令测试覆盖挂载与清除；体积始终不变。**成本**：0.5 天。

### P218 · 成形族（凸台/凹坑/百叶/敲落）

- **动作**：套用“闭式体积 + 非法参数 + 极端参数”模板；凹坑/百叶与 `sheetmetal.py` 协同。
- **验收**：每特征一条闭式 + 一条非法断言。**成本**：1.5–2 天。

### P219 · 梁/焊件首批｜P220 · 图面尺寸第二批｜P221 · 可选补测（内存/帧率）

### P222 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P217 -> P218 -> P219 -> P220 -> P221（可选）`（P222 收尾）

## 4. 纪律（延续 + R38 新增）

1–55 条延续。
56. **新增**：注释/示意类 actor 必须**双护栏**——`SetPickable(0)`（不进选择/捕捉）与 `_exclude_from_bounds`（不影响相机拟合），
    并在测试里显式断言“不可拾取 + 几何不变”（本轮即按此写）。