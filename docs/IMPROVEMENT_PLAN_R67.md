# 改进规划 R67（2026-09-12 立，R66 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R67。
> R66 交付见 `docs/ROUND_R66_20260912.md`（精度度量写死 + 误差预算表 + 精度对单元尺寸二阶的实测）。

## 1. R67 起点基线

| 口径 | 实测 |
|---|---|
| CAE | 面网格 ✅ / 门槛清单 ✅ / 体素 ✅ / 贴体裁剪 ✅ / 贴体四面体 ✅ + **误差预算与二阶（对 h）实测 ✅**；sim 报告 ⏳ |
| 装配配置 | 可见性/抑制/位姿 + 属性快照 + BOM ✅ / .scdm v5 ✅ |
| 材料/属性 | 材料表 + 注册 ✅ / 质量闭式 ✅ / BOM 联动 ✅ |
| 占位命令 | 3 条（`prep.small`、`weld.symbol`、`safety.tab`）；本轮计划清掉 `weld.symbol` |
| 测试 | 全量（CI 口径）**575 passed, 2 skipped, 8 deselected**；`def test_` 扫描 560 条（18 页签 / 161 命令 / 174 live / 占位 3） |
| 官方库 | 除 ref 族外 6/6 精确（沿用 R48 实测）；打开到可交互 12.97 / 8.55 s |

## 2. R67 工作项（P347–P350）

### P347 · 焊接符号元数据面板（`weld.symbol` 占位转 live）（最高优先）

- **动作**：把已有 `WeldSymbol` 模型接到命令：选焊件组元成员 → 类型/尺寸/长度/间距 → 写入 `Weldment.symbols` →
  状态栏/树节点显示；`.scdm` 往返（焊件已在 v3 清单里，符号随之）；BOM/清单带符号。
- **验收**：符号往返一致 + 指向不存在成员报错 + 面板路径可测 + **占位计数 3 → 2**（快照佐证）。**成本**：1 天。

### P348 · 仿真报告第二批

- **动作**：载荷/支撑/接触清单（已有 `scdm.simprep` 模型）+ 与网格统计（单元数/质量门槛结论）联动的报告导出（JSON）。
- **验收**：报告字段与模型一致（可数）+ 与网格门槛结论联动 + 非法（无模型/无网格）+ 往返。**成本**：1 天。

### P349 · 可选补测（内存/帧率）｜P350 · 每轮维护（必做）

- P349 沿用 `tests/test_perf_budget.py` 口径；P350 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）。

## 3. 建议顺序

`P347 -> P348 -> P349（可选）`（P350 收尾）

## 4. 纪律（延续 + R67 新增）

1–82 条延续。

83. **新增**：精度验收要给"**可复算的常数**"。只说阶数不够：把常数与达标所需尺度写出来
    （R66 实测 `err ≈ 1379·h²`，要 `rel 1e-3` 需 `h ≈ 8.5e-4 m`），"够不够用"才可判定；
    否则"二阶收敛"只是个形容词。
