# R41 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R41）执行 P229：凹坑**回放等价**。
> 交付：`create.dimple` 最小步骤回放测试（闭式 + 两次一致），成形族与孔族验收标准对齐（新纪律 58）。

## 改动（仅测试）

`tests/test_forming_p223.py` 新增 `test_p229_replay_reproduces_the_dimple`：
两次独立回放 `create.dimple`（建体直接走内核，绕开其他 op 契约，纪律 54），
断言①切除量 == `pi*(4mm)^2*2mm`（rel 1e-3）；②两次回放逐位一致（rel 1e-6）。

## 成形族验收对齐（纪律 58）

| 项 | 凹坑 | 攻丝孔（参照） |
|---|---|---|
| 闭式体积 | ✅ R39 | ✅ R30 |
| 非法参数 | ✅ R39（四连） | ✅ R30（三连） |
| 极端参数 | ✅ R39（50 µm 浅坑） | ✅ R30（盲孔深度） |
| 回放等价 | ✅ **R41** | ✅ R34 |
| 视口标记/示意 | ⏳ P230 | ✅ R37/R38 |

## 验证

| 套件 | 结果 |
|---|---|
| `tests/test_forming_p218.py` + `tests/test_forming_p223.py` | 见收尾实跑 |

## 下一步（写入 R42）

P230 凹坑成形标记（状态栏/面包屑 + 必要时环状标记，改选/删除时清除）；随后 P231 百叶/敲落（先读 `sheetmetal.py` 契约）。