# R21 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R21）执行 P122（找 `ref` 表）。
> 交付：**判定 `ref` 表不在本 part**（证据链），并把它变成产品口径——`import_warnings` 如实上报 `ref` 族。

## 判定：`ref` 表不在本 part

实体 kind 直方图（4052 个实体，**22 种类型，全部已归类**）：

```
string_attrib 569  pcurve 536  coedge 464  tcoedge 316  point 292  loop 284
edge 232  intcurve 226  tvertex 180  face 177  rgb_color 165  tedge 158
ellipse 144  vertex 112  spline 71  cone 49  plane 40  straight 16  torus 15
body 2  lump 2  shell 2
inner: exactsur 58 nubs 548 exppc 341 cone 23 ref 483 spline 273 exactcur 132 ...
```

**没有**任何“未被归类、且规模与引用表（52/542 量级）吻合”的类型；inner 记录里 `ref` 有 483 条但都是引用点，不是映射表。
结合 R8/R20 的五条否证与“SAT 侧同样无内联几何”，结论：**该类曲面的数据不在本 part**（由内核的外部/共享曲面库解析）。

## 落地：把结论变成产品口径（纪律 39）

`import_scdoc_bundle` 现在额外统计“引用 `ref` 间接曲面的面数”并写进警告与 `import_report["ref_faces"]`，实测输出：

- SampleModel4：`52 个面引用 ACIS ref 间接曲面（该类曲面的数据不在本 part；其中未重建者已计入上面的未重建数）`
- samplemodel2：`20 个面引用 ACIS ref 间接曲面（…）`

措辞刻意区分“引用该类曲面”与“未重建”：samplemodel2 的 20 个引用面里大部分仍能从**边界曲线**建出来（只有 1 个真未重建），
不能把引用数当成缺失数（本轮一度写错，已修正并写入注释）。

## 验收口径（按纪律 39 调整）

- **除 `ref` 族外 6/6 精确**：SampleModel1 1/109、samplemodel3 1/111、samplemodel5 80/1288、samplemodel6 1/28 精确；
  samplemodel2 1810/1813（= SAB − 1 ref 未重建 − 2 正确丢弃/口径差）、SampleModel4 143/177（= SAB − 35 未重建，其中 34 属 ref 族）；
- `ref` 族在 `import_warnings` 里如实计数（本轮新增）。

| 套件 | 结果 |
|---|---|
| `tests/test_import_fidelity.py` | 22 passed |