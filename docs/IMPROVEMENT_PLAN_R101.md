# 改进规划 R101（2026-09-12 立，R100 收官后的遗留候选）

> R81→R100 目标已完成，见 `docs/ROUND_R100_20260912.md` 的验收总表与完成判定。
> 本文件只登记**遗留候选**，不含"本轮必做"。

## 1. 遗留候选（按价值排序）

1. **SampleModel4 未封闭度 335/72**（当前最大单一样例）；samplemodel2 990/195 次之——需按"两侧分支"继续细分（R84/R93 的方法）。
2. **ref 族 27 面**：已三条独立否证 + 逐面计数（数据不在本 part，21 面曲线不全 / 6 面无边界）；除非拿到共享曲面库，否则不修。
3. **性能**：`docs/PERF_BASELINE.json` 已含负载中的导入记录；可做"导入峰值内存"目标（samplemodel2 415 MB 新进程 / 768 MB 负载中）。
4. **产品面**：`safety.tab` 按设计保留空壳；型材库可扩（更多 GB 规格 / 焊接型材）；尺寸链可做第四批（形位公差并入）。

## 2. 纪律

1–89 条延续；新轮教训继续同轮落盘（纪律 65）。
