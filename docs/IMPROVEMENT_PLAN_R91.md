# 改进规划 R91（2026-09-12 立，R90 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R91。
> R90 交付见 `docs/ROUND_R90_20260912.md`（尺寸链极值/统计并列标注 + 链线注记）。

## 1. R91 起点基线（R90 实测）

| 口径 | 实测 |
|---|---|
| 图面 | 尺寸链：判定 + 叠加（worst/rss）+ **并列标注 + 链线**（R69→R90） |
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 347/76、samplemodel2 **1002/212**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 占位命令 | **2 条**（`prep.small`、`safety.tab`） |
| 测试 | 全量 CI 口径全绿（**667 passed, 2 skipped, 8 deselected**）；`def test_` 扫描 650 条 |

## 2. R91 工作项：P423 `prep.small` 重定向到统一检查向导

- **动作**：`prep.small` 现在没有实现（占位）。让它走**与 `repair.check` 同一套**向导/检出（小面修复已由 `repair.check` 全项覆盖）——
  即：命令入口不同，实现与文案同一处（纪律 84），并把占位从 2 条降到 **1 条**。
- **验收**：catalog/live/命令测试/回放四处就位；快照 placeholder 计数 2 → 1；命令行为测试断言两者产出同一套检出（同一 `K.check_geometry` 路径）。**成本**：0.5 天。

## 3. P424 · 每轮维护（必做）｜P425 · 后续候选

- P424 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P425 候选：ref 27 面、槽钢（C 形）截面、环面裁剪的"单候选 + 便宜兜底"改造、装配爆炸的动画帧。

## 4. 纪律（延续）

1–89 条延续。
