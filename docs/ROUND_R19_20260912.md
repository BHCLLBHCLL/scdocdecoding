# R19 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R19）执行 P111（samplemodel2 归零）。
> 交付：把最后 1 个未重建面取证到**根因级**，发现它与 SampleModel4 的 35 个面**同源**——都是 ACIS `ref` 间接引用。

## 诊断：模型 21 面 209（spline）

```
kind spline   uv [-8.3e-06, 1.000008, 0.0, 0.8196]
inner ['ref']            <- 簇内层记录只有 ref
candidates 0             <- _bspline_surfaces 给不出任何候选
rebuild pieces 0
```

**结论**：这个面**不是**“窗口被拒/载荷是 nubs 但读不对”，而是**簇里根本没有曲面载荷**——只有 `ref`。
这与 SampleModel4 的 52 个 `ref` 型样条面、以及 R8 记录的四条排除证据（SAT 侧同为 `{ ref N }`、N 不是实体下标、无 `exactsur` 簇、非曲面共享）**完全一致**。

→ **samplemodel2 的 −1 与 SampleModel4 的 −35 是同一个根因**；在攻下 `ref` 引用表之前，两者都无法归零/精确。
（新纪律 36：同一根因跨样例时合并为一个工作项，不再分别排队。）

## 当前状态

| 样例 | SAB | 实测 | 备注 |
|---|---|---|---|
| SampleModel1 | 1/109 | **精确** | — |
| SampleModel3 | 1/111 | **精确** | — |
| SampleModel5 | 80/1288 | **精确** | — |
| SampleModel6 | 1/28 | **精确** | — |
| samplemodel2 | 37/1813 | 1810 | = SAB − 1（`ref` 未重建）− 2（正确丢弃/口径差） |
| SampleModel4 | 2/177 | 143 | = SAB − 35（`ref` 族未重建）+ 1 |

模型级切分 **0**；`tests/test_import_fidelity.py` 22 passed；导入 0.07–6.88 s。