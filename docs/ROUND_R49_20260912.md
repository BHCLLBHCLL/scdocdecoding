# R49 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R49）执行 P271：**百叶八层接线**。
> 交付：louver 接通脚本 op / 特征 / 目录+live / 命令 / 回放 / 视口标记；3 条新测试。

## 改动（五处接线，全部沿用攻丝孔/凹坑已验证模板）

| 层 | 位置 | 内容 |
|---|---|---|
| 脚本 op | `scdm/scripting.py` | `op_louver(target, face_i, length, width, height)` + 注册 `create.louver` |
| 特征层 | `scdm/features.py` | 标签 `百叶 L×W` + 派发 `K.louver`（含唇边 height） |
| 入口 | `scdm/catalog.py` | `C("create.louver", "百叶", "Louver", "M4", "cyl")` + live 集合 |
| 命令 | `scdm_gui.py` | `_do_create_louver`：长/宽/唇高 → 造形 → `record_feature` → `_record` → 成形标记（双护栏） |
| 回放/视口 | 测试 | 两次最小步骤回放（纪律 54）；命令挂载/清除标记 |

## 测试

`tests/test_forming_p271.py`（3 项）：①注册与标签；②op 闭式**含 0.5 mm 唇边** `L×W×(t−h)`（rel 2e-3）；③回放可复现（rel 1e-6）。
合跑 `p271 + p247 + coverage_ledger` = **14 passed**（覆盖台账守卫通过）。

## 百叶能力矩阵（五项标准达成）

| 项 | 状态 |
|---|---|
| 闭式 | ✅ 轴对齐 R44 / 反向法向 R45 / 45° 斜面 R46 / 含唇边 R48 / op 层 R49 |
| 非法 | ✅ 尺寸、唇高超板厚 |
| 极端 | ✅ 0.5 mm 小百叶、唇高=板厚 |
| 回放 | ✅ R49 |
| 视口标记 | ✅ R49（双护栏：不可拾取 + 不入包围盒） |

→ 百叶与**攻丝孔、圆形凹坑在五项验收上完全对齐**。

## 下一步（写入 R50）

P272 敲落 `knockout`（带筋环切 + 单连通 + 展开兼容，纪律 60）；随后 P273 梁/焊件首批、P274 图面尺寸第二批。