# R91 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R91）执行 P423：**`prep.small` 去占位**。
> 交付：小特征（Defeaturing）不再另写实现，而是与 `repair.check` **共用同一套**几何检查/修复；
> 占位命令 **2 → 1**（只剩按设计保留空壳的 `safety.tab`），live **176 → 177**。

## 1. 为什么是"重定向"而不是"新实现"

小面检出与修复早已由 `repair.check` 的全项 H4 检查覆盖（`K.check_geometry` 的 `small_faces/short_edges/…`），
再写一份只会让同一事实有两处实现（纪律 84）。所以本轮做的是**入口不同、实现同一处**：

| 层 | 文件 | 改动 |
|---|---|---|
| 脚本 op | `scdm/scripting.py` | 把原 `op_repair_check` 的函数体提成 `_geometry_check(kdoc, opts, scale, prefix)`；`op_repair_check` 与 `op_prep_small` 都是薄包装（只差 prefix） |
| GUI | `scdm_gui.py` | `_do_repair_check` 与 `_do_prep_small` 都调 `_run_geometry_check(prefix)`；prefix 只影响状态栏文案与录制的步骤名 |
| 目录 | `scdm/catalog.py` | `prep.small` 加入 `M4_LIVE` |
| 台账 | `tests/test_coverage_ledger.py` | 占位声明改为"R91: routed to the geometry-check wizard" |
| 快照 | `DEV_PLAN.md` | 占位 safety.tab、live 177（由快照工具重算） |

## 2. 验收（每条都可复算）

| 检查 | 期望 | 实测 |
|---|---|---|
| 命令存在且在 live 集 | `prep.small ∈ all_commands() ∩ live_commands()` | ✅ |
| 一处实现 | 两个 op 的字节码都引用 `_geometry_check`；GUI 两个 handler 都走 `_run_geometry_check` | ✅ |
| 同一结果 | 同一实体上 `repair.check` 与 `prep.small` 的检出文本**完全相同**（只差前缀） | ✅ |
| 回放 | `replay([{cmd: prep.small}])` → `OK prep.small` | ✅ |
| 占位清零（除设计保留） | `sorted(all − live) == ["safety.tab"]` | ✅ |
| 快照 | 18 页签 / 162 命令 / **177 live** / 占位 **1** | ✅ `--check` 通过 |

## 3. 测试

| 文件 | 项 | 内容 |
|---|---|---|
| `tests/test_prep_small_r91.py` | 4 | ①命令在 live 集且两个 op 共用一处实现；②唯一剩下的占位是 safety.tab；③同一实体两条命令的检出文本一致；④可回放 |
| `tests/test_coverage_ledger.py` / `test_g1.py` / `test_weld_symbol_p347.py` | 改 | 占位声明与占位列表断言同步（65 项定向测试全绿） |

全量（CI 口径 `-m "not official"`）**671 passed, 2 skipped, 8 deselected**；`def test_` 扫描 652 条。

## 4. 下一步（写入 R92）

P424 维护（快照/口径同步）+ P425 候选择优：ref 27 面、槽钢（C 形）截面、
环面裁剪的"单候选 + 便宜兜底"改造、装配爆炸动画帧。
