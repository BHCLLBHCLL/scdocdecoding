# 改进规划 R40（2026-09-12 立，R39 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R40。
> R39 交付见 `docs/ROUND_R39_20260912.md`（圆形凹坑 + 深径比/极端浅坑判定）。

## 1. R40 起点基线

| 口径 | 实测 |
|---|---|
| 成形族 | 凸台 `boss_round`（已有）/ **圆形凹坑 `dimple_round`（R39）** / 百叶、敲落 ⏳ |
| 孔族 | 简单/沉头/锥沉/攻丝（八层链路全通） |
| 测试 | 新增 `test_forming_p218.py` 4 项；线程三文件 12 项；GUI 13 项 |
| 官方库 | 除 ref 族外 6/6 精确；打开到可交互 12.97/8.55 s |

## 2. R40 工作项（P223–P228）

### P223 · 凹坑接线（最高优先）

- **动作**：`_do_create_dimple` + 目录 `create.dimple` + live 集合 + 特征历史（`Feature(op="dimple")`）+ 脚本 op `create.dimple`；
  套用攻丝孔已验证的八层模板（内核✅ → 命令 → 入口 → 回放）。
- **验收**：命令级测试 1（正常+非法）、回放体积等价 1、覆盖台账通过。**成本**：0.5–1 天。

### P224 · 百叶 / 敲落（钣金成形）

- **动作**：先读 `sheetmetal.py` 契约（R33 教训），再实现 louver/knockout；与既有 bend/hem/bead 的元数据模型保持一致。
- **验收**：每项一条闭式断言 + 一条非法断言 + 与钣金展开的兼容性说明。**成本**：1.5–2 天。

### P225 · 梁/焊件首批｜P226 · 图面尺寸第二批｜P227 · 可选补测（内存/帧率）

### P228 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P223 -> P224 -> P225 -> P226 -> P227（可选）`（P228 收尾）

## 4. 纪律（延续 + R40 新增）

1–56 条延续。
57. **新增**：成形族与孔族的区别要**显式写进代码注释与校验**（凹坑=浅盲孔几何 + forming 元数据 + 深径比上限），
    否则后续维护会把两者混为一谈，导致“成形特征退化成钻孔”而无人察觉。