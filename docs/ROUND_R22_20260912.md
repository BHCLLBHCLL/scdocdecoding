# R22 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R22）执行 P127 第一小步（官方层级）。
> 交付：导入侧产出**只读部件层级**（part → body ids）并纳入 `import_report`，GUI 可据此建显示分组；新增回归测试。

## 改动（数据层先行）

`import_scdoc_bundle` 在逐 part 合并实体时记录 `hierarchy = [{"name": part标签, "bodies": [body ids]}]`，
写进 `import_report["hierarchy"]`。这样 GUI 不必自己反推“哪个体属于哪个部件”（官方 part 与体是多对一的），
并且与已有的 `import_report`（parts / failed_parts / unbuilt_faces / dropped_faces / ref_faces / mesh_bodies）同一数据源。

## 测试

`tests/test_import_fidelity.py::test_import_reports_a_part_to_body_hierarchy`（新增）：
①层级项 > 10；②每项名字与体列表非空；③**每个体恰好属于一个部件**（`len(ids) == len(set(ids)) == len(kdoc.bodies)`）。

## 验证

| 套件 | 结果 |
|---|---|
| `tests/test_import_fidelity.py`（含新增） | 见收尾实跑 |
| 层级测试 + 告警测试 | 2 passed |

## 下一步（写入 R23）

GUI 侧把 `hierarchy` 建成**只读显示分组**并接到树与视口：勾选显隐 / 隔离，几何不变（P127 剩余部分）。