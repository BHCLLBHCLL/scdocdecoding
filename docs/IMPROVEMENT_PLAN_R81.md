# 改进规划 R81（2026-09-12 立，R80 收尾后的后续候选）

> R50→R80 目标已完成，见 `docs/ROUND_R80_20260912.md` 的验收总表与完成判定。
> 本文件只登记**后续候选**，不再有"本轮必做"的强制项；每项都带可数验收与已实测的现状。

## 1. 现状基线（R80 实测）

| 口径 | 实测 |
|---|---|
| 面数保真 | 6 个官方样例中 **5 个完全一致**；SampleModel4 147/177 |
| ref 族 | 52 面引用：23 重建 / **29 未重建**（6 个无任何边界边） |
| 未封闭度 | SampleModel1 36 缺口/4 环、SampleModel4 378/95、samplemodel2 1395/374、samplemodel6 2/2、samplemodel3 与 5 = 0/0 |
| 性能 | `docs/PERF_BASELINE.json`（R80 复测与 R71 一致） |
| 占位命令 | 2 条（`prep.small`、`safety.tab`） |
| 测试 | 全量 CI 口径全绿；`def test_` 扫描 618 条 |

## 2. 候选（按价值排序，任选其一即可成为下一轮）

### C1 · ref 族 29 面（唯一的面数缺口）

- 现状：`inner == ref` 且 6 个面无边界边；R72 的三条否证（别名 0/29、填面 0/20、容差无效）仍然成立。
- 可试：**同一文件内其他 body/model 的未使用曲面池**（R72 只数过本 model 的属主）、ACIS `exactsur` 与 `ref` 的配对关系。
- 验收：能给"能修/不能修"各一条可复算证据；能修则面数上升且包围盒漂移 ≤0.01。

### C2 · 未封闭度继续下降

- 现状：SampleModel4 378 缺口 / 95 环，samplemodel2 1395/374。
- 可试：`invalid 32` 类失败面（`ShapeFix_Face` 之外的修复路径）、`no-curve` 里真正缺数据的部分分类处理。
- 验收：缺口/自由环下降且面数、包围盒、耗时三个不回退。

### C3 · 产品面：占位命令与看板

- `prep.small`（小件准备流程）落地并接线八层；
- 把 `open_edges`/`free_loops` 做成"几何体检"命令（一键体检 + 定位到面）。

### C4 · 其他既有候选

- 装配爆炸图第二批（沿轴/径向 + 动画帧）、型材库标准规格表（槽钢/工字钢）、图面尺寸链第三批（极值/统计并列标注 + 链线图形化）。

## 3. 纪律

1–89 条延续；新轮的教训继续按"同轮写入轮次文档 + 规划文件"的方式累积（纪律 65）。
