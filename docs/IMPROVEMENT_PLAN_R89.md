# 改进规划 R89（2026-09-12 立，R88 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R89。
> R88 交付见 `docs/ROUND_R88_20260912.md`（爆炸图三种模式 + 位移可数可还原 + op/回放接线）。

## 1. R89 起点基线（R88 实测）

| 口径 | 实测 |
|---|---|
| 爆炸图 | `asm.explode` op：轴/径向/等比；位移 = 公式值（rel 1e-12）；`restore` 精确回位 |
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 347/76、samplemodel2 **1002/212**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 型材库 | 12 条标准规格（R87） |
| 测试 | 全量 CI 口径全绿（**660 passed, 2 skipped, 8 deselected**）；`def test_` 扫描 643 条 |

## 2. R89 工作项（择优取一）

### P419 · 环面裁剪的"廉价闸门"（把 R85 的否证翻案）

- **背景**：R85 否证环面裁剪的理由是"每面 0.54 s"，本轮实测那 0.54 s **几乎全在闸门**（8 面 0.82 s；
  其中 `BRepCheck_Analyzer` + `K.area` + `_shape_within` 在裁剪环面片上很贵），而线框/pcurve/MakeFace 合计只有 0.07 s。
- **动作**：把闸门改成"**先便宜后贵**"：①先用面自身**边界曲线的包围盒**做一次廉价的"在模型内"判断；②只在通过后才跑 `BRepCheck`/`GProp`；
  再测环面可裁剪率与总耗时（目标：把 36 面从 19.4 s 降到 <3 s），若成立则把环面也纳入裁剪白名单。
- **验收**：每面耗时、可裁剪率、缺口/自由环前后对比；面数/包围盒/耗时不回退。**成本**：1 天。

### P420 · 每轮维护（必做）｜P421 · 后续候选

- P420 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P421 候选：图面尺寸链第三批、`prep.small` 重定向、ref 27 面、槽钢（C 形）截面。

## 3. 建议顺序

`P419 -> P420（收尾）`

## 4. 纪律（延续）

1–89 条延续。
