# 改进规划 R24（2026-09-12 立，R23 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R24。
> R23 交付见 `docs/ROUND_R23_20260912.md`（官方部件 → 只读显示分组，显隐只改 visible）。

## 1. R24 起点基线

| 口径 | 实测 |
|---|---|
| 除 ref 族外 | 6/6 精确（4 样例完全精确；差额已定性并如实上报） |
| 层级 | `import_report["hierarchy"]` + `kdoc.groups` 只读显示分组（R22/R23） |
| 显隐 | `apply_group_visibility` 只改 `visible`，几何恒等（已测） |
| 性能 | 导入 0.06–6.4 s |

## 2. R24 工作项（P139–P144）

### P139 · 树 ↔ 视口联动（最高优先）

- **动作**：树上勾选官方部件分组 → 立即调用 `scene.apply_visibility(session)` 刷新；提供“隔离”（只显示该组）；
  组节点显示 `ref_faces`/未重建计数提示。
- **验收**：samplemodel2（25 部件/37 体）逐组切换正确、无残留高亮、几何不变。**成本**：1–1.5 天。

### P140 · 大模型 GUI 可用性（积压十二轮）

- **动作**：导入 → 视口构建 → 首次渲染耗时/内存实测（samplemodel2/5），必要时懒装配。
- **验收**：< 20 s 可交互 + 帧率基线。**成本**：1–1.5 天。

### P141 · 特征族与梁焊件｜P142 · 图面尺寸第二批（多选/吸附/undo）｜P143 · 可选：`ref` 外部曲面库否证实验

### P144 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P139 -> P140 -> P141/P142 -> P143（可选）`（P144 收尾）

## 4. 纪律（延续 + R24 新增）

1–42 条延续。
43. **新增**：显隐/隔离类验收必须包含“**几何恒等**”断言（比较 `id(shape)` 或对象字典），
    只测“显示数量”容易被“重建了几何”蒙混过关。