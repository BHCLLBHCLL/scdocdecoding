# 改进规划 R68（2026-09-12 立，R67 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R68。
> R67 交付见 `docs/ROUND_R67_20260912.md`（焊接符号面板：占位 3 → 2，符号往返与命令路径可测）。

## 1. R68 起点基线

| 口径 | 实测 |
|---|---|
| 焊件 | 6 截面 / 单段梁 / 折线梁+组元 ✅ / **焊接符号 live ✅**（面板可挂、往返一致） |
| CAE | 面网格 + 门槛 + 体素/贴体裁剪/贴体四面体 + 误差预算 ✅；**sim 报告 ⏳** |
| 装配配置 | 可见性/抑制/位姿 + 属性快照 + BOM ✅ / .scdm v5 ✅ |
| 占位命令 | **2 条**（`prep.small`、`safety.tab`） |
| 测试 | 全量（CI 口径）**579 passed, 2 skipped, 8 deselected**；`def test_` 扫描 564 条（18 页签 / 161 命令 / 175 live / 占位 2） |
| 官方库 | 除 ref 族外 6/6 精确（沿用 R48 实测）；打开到可交互 12.97 / 8.55 s |

## 2. R68 工作项（P348–P351）

### P348 · 仿真报告第二批（最高优先）

- **动作**：载荷/支撑/接触清单（`scdm.simprep` 已有模型）+ **与网格统计联动**（单元数/质量门槛结论/体积误差）
  → 报告导出 JSON；字段与模型逐条一致（可数）。
- **验收**：报告条数 = 模型条数（相等，不是"看起来差不多"）+ 门槛结论随网格统计变 + 非法（无模型/无网格）
  + 报告往返。**成本**：1 天。

### P349 · 可选补测（内存/帧率，已顺延多轮）

- **动作**：沿用 `tests/test_perf_budget.py` 口径补一次内存峰值与帧率基线；若与既有预算无冲突则只记录，不设新阈值。
- **成本**：0.5 天。

### P350 · 每轮维护（必做）｜P351 · 后续候选

- P350 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P351 候选（写入 R69 时择优）：图纸标注第三批（尺寸链/公差叠加）、钣金弯管/cross-break、导入保真 ref 族攻坚。

## 3. 建议顺序

`P348 -> P349 -> P350（收尾）`

## 4. 纪律（延续）

1–83 条延续，本轮不新增（接线轮以"占位退休 + 命令可测"为准，不硬凑判据）。
