# 改进规划 R35（2026-09-12 立，R34 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R35。
> R34 交付见 `docs/ROUND_R34_20260912.md`（回放断言落地，攻丝孔四层测试齐备）。

## 1. R35 起点基线

| 口径 | 实测 |
|---|---|
| 攻丝孔 | 内核 ✅ / 符号 ✅ / 特征+脚本 ✅ / 回放 ✅ / **命令+视口 ⏳** |
| 线程测试 | **12 项**（p175 3 / p181 5 / p187 4） |
| 官方库 | 除 ref 族外 6/6 精确；打开到可交互 12.97/8.55 s |

## 2. R35 工作项（P205–P210）

### P205 · 攻丝孔命令与视口示意（最高优先，P199 收尾）

- **动作**：①先读命令注册与 `prep.hole` 处理器的 opts 契约（R33 教训：不猜契约）；
  ②增“攻丝”选项 + 公称/螺距输入；非法参数返回可读提示（含规则原文）；③记入特征历史；
  ④视口用 `thread_lines` 建独立示意 actor（不进拾取/布尔）。
- **验收**：命令级行为测试（正常 + 非法各 1 条）；示意前后 `K.volume` 不变 1 条；GUI offscreen 覆盖选项路径。**成本**：1–1.5 天。

### P206 · 成形族（凸台/凹坑/百叶/敲落）｜P207 · 梁/焊件首批｜P208 · 图面尺寸第二批｜P209 · 可选补测

### P210 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P205 -> P206 -> P207 -> P208 -> P209（可选）`（P210 收尾）

## 4. 纪律（延续 + R35 新增）

1–53 条延续。
54. **新增**：回放/脚本类测试要**最小化被测步骤**（本轮只回放孔 op，建体直接调内核），
    让“哪个 op 的契约坏了”一眼可辨；跨 op 的端到端回放在各 op 单独绿之后再补。