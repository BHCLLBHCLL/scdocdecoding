# R82 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R82）执行 P401：**裁剪失败面的修复路径**。
> 结果：**SampleModel1 变成完全封闭的壳体（缺口 36→0、自由环 4→0）**，samplemodel2 缺口 1393→1272。

## 1. 修复候选的实测（先量后排，纪律 86 的反面用法：**先证有用再接线**）

对每个"线框闭合但产物过不了闸"的裁剪面，试 5 条修复路径并统计"过闸"数（SampleModel1 / samplemodel2 / SampleModel4）：

| 候选 | 做法 | 恢复数（S1 / s2 / S4） |
|---|---|---|
| V1 | `ShapeFix_Face` + FixOrientation（R76 现状） | 11 / 142 / 22 |
| **V2** | V1 + `FixMissingSeam` | **16 / 160 / 23** |
| **V3** | `ShapeFix_Shape` | 16 / **165** / 23 |
| V4 | `BRepLib.SameParameter` + V1 | —（构造签名不匹配，未采用） |
| **V5** | `ShapeFix_Face` 精度放到 1e-4 | 16 / 161 / 23 |

接线：`_trim_fix_candidates()` 按 V2 → V3 → V5 顺序产出候选，`_trimmed_face()` 取**第一个通过全部几何闸门**的
（有效性 / 面积 >0 / 模型包围盒内 / 面自身点包围盒不超一个对角线）。闸门抽成 `_trim_gates_ok()` 供探针与产品共用（纪律 84）。

## 2. 实测结果

| 样例 | 真缺口 R81 → R82 | 自由环 | 缝边 | 面数 | 裁剪率 | 导入耗时 |
|---|---|---|---|---|---|---|
| SampleModel1 | 36 → **0** | 4 → **0** | 0 | 109（不变） | 33/38 → **38/38** | 1.51 s |
| SampleModel4 | 385 → **375** | 94 → **86** | 18 | 149（不变） | — | 1.69 s |
| samplemodel2 | 1393 → **1272** | 372 → **359** | 34 | 1813（不变） | — | 7.56 s |
| samplemodel3 | 0 | 0 | 0 | 111 | — | 0.11 s |
| samplemodel5 | 0 | 0 | 0 | 1288 | 131/132 → **132/132** | 2.82 s |
| samplemodel6 | 2 → **3**（如实记录） | 2 | 0 | 28 | 0/6 → **6/6** | 0.10 s |

合计：真缺口 **1816 → 1650**（−9%）、自由环 472 → 447。SampleModel1 是官方库里**第一个完全封闭**的样例。
samplemodel6 的 +1 缺口来自策略翻转（它的 6 个柱面现在都能裁剪，比率 1.0 ≥ 0.5 阈值），面数与包围盒漂移（0.0%）不变——
按 R79 的教训**不动阈值去凑 ±1**，如实记录。

## 3. 测试

| 文件 | 项 | 内容 |
|---|---|---|
| `tests/test_watertight_report_r77.py` | 改 | SampleModel1 的 (open, seam, loops) = (36,0,4) → **(0,0,0)**；裁剪率表 38/38、132/132、6/6 |
| `tests/test_trim_patches_r76.py` | 改 | 缺口与自由环断言收紧为 `== 0`；策略表 samplemodel6 → True；泄漏测试的数值断言放宽为 ≤3 |
| `tests/test_free_edges_r75.py` | 改 | 三元组：SampleModel1 **(0,0,0)**、SampleModel4 393/18/375、samplemodel2 1306/34/1272、samplemodel6 3/0/3；直方图断言改为"非空且无单面边" |
| `tests/test_import_geometry_r73.py` | 改 | 上限收紧：SampleModel1 (0,0)、SampleModel4 (390,90)、samplemodel2 (1300,365)、samplemodel6 (3,2) |

全量（CI 口径 `-m "not official"`）**637 passed, 2 skipped, 8 deselected**；`def test_` 扫描 620 条（快照：18 页签 / 162 命令 / 176 live / 占位 2）。

## 4. 下一步（写入 R83）

P402 产品面：**几何体检命令**（把 `open_edges`/`free_loops` 做成一条命令 + 八层接线 + 定位到面），
之后 P403 维护；未封闭度剩余项（SampleModel4 375/86、samplemodel2 1272/359）登记为 P404 候选。
