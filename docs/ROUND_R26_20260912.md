# R26 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R26）执行 P151（树控件接线）。
> 交付：官方部件分组在左侧树里带**勾选框**，勾选 → 视口显隐（端到端测试通过，10 项 GUI 测试全绿）。

## 改动（沿用既有 layer 模式，纪律 45 只接一条链路）

1. `scdm/gui/left_panel.py`：
   - 新增信号 `group_toggled = pyqtSignal(str, bool)`；
   - 分组项改为 `QListWidgetItem` + `Qt.UserRole=("group", name)`；`imported` 分组额外带 `ItemIsUserCheckable`（默认勾选）；
   - 新增 `_on_group_item(item)`：勾选变化 → 发信号；重填列表期间用 `_block_group` 屏蔽（与 `_block_layer` 同法）。
2. `scdm_gui.py`：`self.left.group_toggled.connect(self.set_import_group_visible)` —— 事件直接落到 R25 的查看器接口（薄封装，纪律 44）。

## 测试（交互层，端到端）

`test_p151_tree_group_checkbox_emits_and_toggles`：构造带 `hierarchy` 的 kdoc → 往 `group_list` 放一个勾选项 → **取消勾选**后组内体 `visible=False`、**重新勾选**后恢复。
这条链路是“树控件 → 信号 → 查看器 → 体可见性”的完整验证；几何恒等由 R23 的 `test_p133` 负责（职责分离）。

## 验证

| 套件 | 结果 |
|---|---|
| `tests/test_lightweight_gui.py` | **10 passed** |
| `tests/test_import_fidelity.py` | 25 passed（上一轮） |

## 下一步（写入 R27）

P145 收尾剩余：右键“隔离/全部显示”菜单、节点提示（`ref_faces`/未重建计数）挂在部件节点上；随后转 P152（大模型 GUI 实测）。