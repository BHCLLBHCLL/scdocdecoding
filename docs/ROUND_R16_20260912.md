# R16 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R16）执行 P93（平面面环解码）。
> 本轮仍为**诊断轮**：把 `loops=0` 的成因查到“环不闭合”这一层，修法（按几何重连边）留给 R17。

## 探针结果（逐面打印环/协边/端点）

三个代表性平面面的实测：

| 面 | loops | coedges | 边类型 | 端点可解 | 环是否闭合 |
|---|---|---|---|---|---|
| 模型 21 面 269 | 1 | 6 | 全 straight | 是 | 前 5 条首尾相接，第 6 条待查（探针只打印前 5 条） |
| 模型 8 面 3455 | 1 | 3 | 全 straight | 是 | **否**：ce3045 终点 (0.5849,−5.5674,0.922) ≠ ce3046 起点 (0.5152,−5.5728,0.779) |
| 模型 24 面 3090 | ≥1 | ≥5 | straight + ellipse | 是 | **否**（`loop_polygon` 返回 None） |

**结论（把上一轮的“拓扑层”再细化一层）**：`face_loops_polygons` 为空**不是**因为解码取不到环或取不到端点，
而是**按 `next` 链 + 端点走出来的环不闭合**——`_walk_sensed`（按 sense 定向）与 `_walk_ring`（纯端点链、两种朝向）都返回 None。
模型 8 面 3455 的三条边彼此相接成一个“开链”：ce3046→ce3452→ce3045，而 ce3045 的终点与 ce3046 的起点差了 0.008（x）与 0.005（y）。

## 下一步（写入 R17）

1. **补全探针**：把这几个环的**全部** coedge 打全（本轮受`[:5]`截断，违了纪律 31），确认是“最后一条端点错”还是“中间断”；
2. **两条候选修法**（按代价排序）：
   a. 环不闭合时，**改用顶点源重算端点**再走一次（`edge_endpoints` 在“曲线端点与顶点在容差内一致”时会用曲线端点，可能正是偏差来源）；
   b. 仍不闭合则**按几何重连**：把该面的所有 coedge 端点当无向边，用端点邻接重建闭合环（R5 的 `_walk_ring` 只在“链顺序正确”时有效）。
3. 验收不变：samplemodel2 **1813 ± 0**、漂移 **0.0%**。

## 状态（未变）

SampleModel1 精确 · SampleModel4 2/143（未重建 35）· samplemodel2 **1809**（SAB 1813）· samplemodel3/5/6 精确；模型级切分 **0**；`test_import_fidelity.py` 22 passed。