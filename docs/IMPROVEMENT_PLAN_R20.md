# 改进规划 R20（2026-09-12 立，R19 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R20。
> R19 交付见 `docs/ROUND_R19_20260912.md`（确认 samplemodel2 的 −1 与 SampleModel4 的 −35 **同源：ACIS `ref`**）。

## 1. R20 起点基线

| 口径 | 实测 |
|---|---|
| 官方库导入 | **4/6 精确**；samplemodel2 37/1810；SampleModel4 2/143 |
| 切分 | 0（模型级） |
| 唯一根因 | **ACIS `ref N` 间接引用**（samplemodel2 1 面 + SampleModel4 35 面） |
| 性能 | 导入 0.07–6.88 s |

## 2. R20 工作项（P117–P122）

### P117 · 攻 `ref` 引用表（最高优先，唯一挡 `--strict` 的账）

- **已完成取证（不必重做）**：SAT 侧同为 `{ ref N }`；N 不是实体下标；52 个 `ref` 型 spline 实体均无 `exactsur` 簇；曲面共享只命中 4/52。
- **本轮动作（交叉定位法）**：
  1. 取 SAT 的 71 个 `spline-surface` 块，按“用该面 uv 窗口裁出的 bbox”与各块的极点包络配对，锁定某个 `ref` 面对应的**具体极点集合**；
  2. 在 SAB 的**全部记录**（含 inner、含文件尾部）里按这些极点值搜索——能搜到就定位到“数据在哪个节”，搜不到就确认“数据不在本 part”，转而解 `ref N` 的**编号语义**（对照 SAT 里 `ref` 出现顺序与实体顺序）；
  3. 只改读取端（`_bspline_surfaces`），命中即用 `_face_from_window` 验证（口径已在 R9–R12 建好）。
- **验收**：`tools/import_fidelity.py --strict` 退出 0。**成本**：2–3 天（高风险，允许分两轮）。

### P118 · 官方层级参与交互（积压八轮）｜P119 · 大模型 GUI（积压八轮）｜P120 · 特征族/梁焊件

### P121 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送（纪律 25/27）。

## 3. 建议顺序

`P117 -> P118 -> P119 -> P120`（P121 收尾）；P117 允许按“极点定位 → 编号语义”分两轮推进。

## 4. 纪律（延续 + R20 新增）

1–35 条延续。
36. **新增**：多个样例的残量若同源（本轮 samplemodel2 −1 与 SampleModel4 −35 同为 `ref`），**合并为一个工作项**，
    验收也合并写（`--strict` 6/6），不再按样例各自排队——否则会在同一个根因上重复投入。
37. **新增**：高风险深水区（如 `ref` 引用表）每轮只推进一个**可证伪的小步**（本轮：极点定位），并允许“只出结论不出代码”，
    但必须在轮次文档里留下可复用的中间产物（配对表、搜索脚本、否证结论）。