# R4 轮次交付记录（P21–P28，2026-09-12）

> 规则：每完成一轮改进即产出下一轮规划（本轮 -> `docs/IMPROVEMENT_PLAN_R5.md`）。
> 前序：`docs/P0_P8_IMPROVEMENTS_20260912.md`（P0–P12）、`docs/ROUND_R3_20260912.md`（P13–P20）。

## 交付一览

| 优先级 | 主题 | 交付 | 测试 |
|---|---|---|---|
| **P21** | 草图约束可视化 + 3D 草图 | `Sketch.constraints` 持久化；`constraint_glyphs`（H/V/∥/⊥/=/●/M/▣/T + mm 值，索引失效则跳过）；scene `show_constraint_marks/clear`（vtkVectorText 标签）；GUI 应用约束后记录并刷新标签；自定义平面（贴合面法向）草图拉伸 | test_command_behavior +3、test_smoke +1 |
| **P22** | 文档级特征模型 | `DocFeature`/`FeatureHistory`（slot 依赖 + 重放）：线性/圆周阵列、镜像、并/减/交、分割；`KernelDoc.record_doc_feature/replay_document`（先参数基体+单体特征、再文档特征，可 apply 替换体）；特征删除后重放；左栏"特征历史"节点；GUI 阵列/镜像/合并写历史 | test_doc_features（8，含删除/往返/参数化重放） |
| **P22b** | 项目持久化补全 | **修正文档/实现落差**：`.scdm` 清单此前只存 bodies/notes/named/groups，现补 features/document_features/instances/components/mates（version 2） | 含在上面 8 项内 |
| **P23** | 装配配置 | `Configuration`（隐藏组件/抑制体/变换覆盖）+ `capture_configuration/apply_configuration/configuration_by` + `active_configuration`；GUI `asm.config`/`asm.config_apply`；随 .scdm 往返 | test_configurations（3） |
| **P24** | 工程图尺寸关联 | `Dimension.assoc` + `edge_selector/remeasure_edge/refresh_dimensions`（最近中点 30% 容差）；几何变化后自动重测（20→25mm），找不到则标记 stale；offset 拖动不改值 | test_drawing_p7 +3（并因此把 `det.dim` 从台账豁免中移除） |
| **P25** | 大模型预算 + 实测 | 官方库 6 样例 载入/B-rep 导入/耗时全表（见 §实测）；**修掉导入崩溃**（`edge_endpoints` 对可选 t0/t1 的 None 崩溃）；预算回归（load ≤5/20 s、导入不崩不超 60 s） | test_perf_budget（3） |
| **P26** | 钣金折弯表 + 展开图标注 | `BEND_TABLES`（钢/铝/不锈钢/铜 → K、R 系数）+ `bend_table_entry`；`flat_pattern` 增 `bend_lines`（位置 = flat1 + BA/2、角度、半径）；`flat_pattern_dxf` 用 BEND 图层写折弯线 + 角度文本 | test_sheetmetal +3 |
| **P27** | 只读打开保真度 | 左栏新增"文档结构（只读）"：PartDef 数、图层（含名称子节点）、Caption 数、命名选择数——直接来自 document.xml，无需内核导入 | test_smoke +1 |
| **P28** | 每轮维护 | 快照 `--update`（145 命令/159 live、367 测试、覆盖 105/159）+ CI `--check` 已就位 | — |

## 实测（P25，本机 conda env scdm）

| 样例 | load_scdoc | B-rep 导入 | SAB 体/面 | 导入后体/面 |
|---|---|---|---|---|
| samplemodel6 | 0.12 s | 2.01 s | 1 / 28 | 1 / 12 |
| samplemodel3 | 0.38 s | 0.28 s | 1 / 111 | 1 / 348 |
| SampleModel1 | 0.72 s | 0.08 s | 1 / 109 | 1 / 3 |
| SampleModel4 | 1.74 s | 0.19 s | 2 / 177 | 2 / 6 |
| samplemodel2 | 5.15 s | 56.42 s | 37 / 1813 | 35 / 12321 |
| samplemodel5 | 6.06 s | 1.35 s | 80 / 1288 | 80 / 313 |

**结论（本轮最重要的新发现）**：`load_scdoc` 侧已 6/6 且计数正确，但 **B-rep 导入层对官方几何既不保真也慢**——
面数大幅偏离（3 vs 109、6 vs 177、12 vs 28、313 vs 1288），samplemodel2 丢 2 个体且耗时 56 s。
这直接决定"打开官方文件"的用户可见质量，列为 R5 第一优先。

## R4 边界（未做）

- P24 的**视口拖拽手柄**未做（仅数据模型 + 导出 + 自动重测）。
- P27 仅为只读信息树；官方层级未参与选择/隔离等交互。
- P22 的重放是"整体重建"而非增量；GUI 尚未提供特征删除/重排的交互入口（模型层已支持 `remove_at`）。
- 官方整域仍缺：梁/焊件、孔/螺纹/成形特征库、CAE 网格、PMI/GD&T、制造（这些在 R5 规划中按 ROI 排队）。

## 验证（R4 收尾实跑）

| 套件 | 结果 |
|---|---|
| 非官方全量 | **378 passed / 1 skipped / 8 deselected · 97.9 s**（收集 387） |
| 官方门禁 | `official_gate` **5 passed · 20.0 s** |
| 快照 | `gen_devplan_snapshot.py --update/--check` 通过（145 命令 / 159 live / 367 def test_） |
| 命令级覆盖 | 提及 105/159；`det.dim` 因新增行为测试已从豁免名单移除 |

## R4 记录在案的自身问题

- P25 首轮探针把 `load_scdoc` 的返回值当成对象用（实际是 dict），导致误报"bodies=0"；
  已改为按 `models`/`fac` 计数。
- P22 的首版持久化测试断言写错（实例是实体化副本，保存后应为 2 个体），已按实现语义修正。
- 插入 GUI 测试时再次踩到 `__main__` 块位置问题（把新测试插到了 `if __name__` 之后），
  已修正——与 R3 的"截断"教训同源：**改动文件尾部前必须确认结构锚点**。
