# 改进规划 R86（2026-09-12 立，R85 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R86。
> R85 交付见 `docs/ROUND_R85_20260912.md`（球面片真实裁剪；缺口 1242→1002、自由环 356→212；环面否证）。

## 1. R86 起点基线（R85 实测）

| 口径 | 实测 |
|---|---|
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 347/76、samplemodel2 **1002/212**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 缝边 | 0 / 16 / 81 / 0 / 0 / 0 |
| 面数保真 | 5/6 样例完全一致；SampleModel4 149/177（ref 25/27） |
| 导入耗时 | 1.49 / 1.66 / 7.04 / 0.11 / 2.74 / 0.10 s |
| 测试 | 全量 CI 口径全绿（**646 passed, 2 skipped, 8 deselected**）；`def test_` 扫描 629 条 |

## 2. R86 工作项（择优取一）

### P411 · 第二类缺口：锥面↔柱面（samplemodel2 165+129 条）

- **动作**：复核锥面片（`_rebuild_face` 的 cone 分支）在**真实裁剪**下的表现：锥面片现在走的是"解析窗口 + 角度候选"路径，
  量一下 `_trimmed_face` 对锥面片的可裁剪率与每面耗时；若像球面一样便宜就接上。
- **验收**：可裁剪率 + 每面耗时 + 缺口/自由环前后对比；面数/包围盒/耗时不回退。**成本**：0.5–1 天。

### P412 · 产品面：装配爆炸图第二批

- **动作**：爆炸方向支持沿轴/径向 + 步进参数；八层接线（op / 命令 / 回放 / 视口）。
- **验收**：位移向量可数、撤销可还原、命令与回放测试就位。**成本**：1 天。

### P413 · 每轮维护（必做）｜P414 · 后续候选

- P413 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P414 候选：环面裁剪的**低成本**做法（先便宜闸门再贵闸门）、型材库标准规格表、尺寸链第三批、`prep.small`、ref 27 面。

## 3. 建议顺序

`P411 -> P412 -> P413（收尾）`

## 4. 纪律（延续）

1–89 条延续。
