# 改进规划 R90（2026-09-12 立，R89 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R90。
> R89 交付见 `docs/ROUND_R89_20260912.md`（闸门便宜优先 + 环面否证的量化成本结构）。

## 1. R90 起点基线（R89 实测）

| 口径 | 实测 |
|---|---|
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 347/76、samplemodel2 **1002/212**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 裁剪白名单 | 柱面、球面（环面：−12 缺口换 +18 s，拒绝；锥面：原生崩溃，拒绝） |
| 闸门成本 | BRepCheck 0.003 s / 包围盒 ~0 / GProp 0.049 s（**便宜优先**） |
| 爆炸图 | `asm.explode` 三模式（R88） |
| 型材库 | 12 条标准规格（R87） |
| 测试 | 全量 CI 口径全绿；`def test_` 扫描 6xx 条 |

## 2. R90 工作项（择优取一）

### P422 · 图面尺寸链第三批：极值/统计并列标注 + 链线图形化

- **动作**：`scdm/dimchain.py` 已有 `stack_up`（worst/rss）；本轮把它**落到图面**：
  同一链输出两条总尺寸标注（±worst / ±rss 文字），并画出**链线**（注记层折线，不入几何、不入包围盒）。
- **验收**：两条标注文字含各自公差值（可复算）；链线是注记对象（`kdoc.notes` 或 annotation 层），
  不改变实体包围盒与体积；undo 往返一次回到原状；SVG/DXF 导出都带这两条标注。**成本**：1 天。

### P423 · `prep.small` 重定向到统一检查向导（消掉一个占位命令）

- **动作**：让 `prep.small` 打开与 `repair.check` 同一套向导（同一事实一处实现），占位从 2 条降到 1 条。
- **验收**：命令在 catalog/live/命令测试/回放里各就位；快照 placeholder 计数变化。**成本**：0.5 天。

### P424 · 每轮维护（必做）｜P425 · 后续候选

- P424 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P425 候选：ref 27 面、槽钢（C 形）截面、环面裁剪的"单候选 + 便宜兜底"改造。

## 3. 建议顺序

`P422 -> P423 -> P424（收尾）`

## 4. 纪律（延续）

1–89 条延续。
