# R20 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R20）执行 P117 第一小步（`ref` 引用表）。
> 交付：**两条新的否证结论**（连带一个重要的结构事实），把 `ref N` 的候选空间进一步压缩；本轮不出代码（纪律 37 允许）。

## 结构事实（本轮新发现）

**SAT 的记录条数 = SAB 的实体条数 = 4052（1:1）**。即 SabSatConverter 的输出顺序就是 SAB 的实体顺序，
因此“SAT 里的编号语义”与“SAB 实体下标”是同一套坐标系——后续任何“用 SAT 定位再回 SAB 取数”的推论都可放心用这条对应关系。

## 两条否证（`ref N` 的语义）

| 假设 | 实测 | 结论 |
|---|---|---|
| `ref N` = SAT/SAB 实体序号 | `ref 1`→body、`ref 19`→string_attrib、`ref 9`→face、`ref 53`→loop（0 基/1 基都试过，命中 2/52） | **否** |
| `ref N` = 该 part 的曲面表序号（175 个曲面实体） | N 范围 **0–542** 超出表长；逐条取第 N 个曲面，**0/52** 是带载荷的 spline | **否** |

加上 R8 已排除的三条（实体下标、曲线级 `exactsur` 簇、曲面共享），`ref N` 已被压缩到“**独立引用命名空间**”一种可能：
其表要么不在本 part，要么藏在我们尚未分类的记录里。

## 当前状态（未变）

4/6 精确；samplemodel2 37/1810；SampleModel4 2/143；模型级切分 0；`tests/test_import_fidelity.py` 22 passed。

## 下一步（写入 R21）

1. **统计 4052 个实体的 kind 直方图**，找未被分类、且计数与 52/542 这类“引用表规模”吻合的记录类型；
2. 若存在，解析它作为 `ref` 表（映射 N→实体）；否则转“确认数据不在本 part”并在轮次文档里给出该结论的证据链，
   把 `--strict` 的目标改为“除 `ref` 族外 6/6 精确 + `ref` 族按缺失如实上报”（口径要写清）。