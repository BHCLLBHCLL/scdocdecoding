# 改进规划 R18（2026-09-12 立，R17 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R18。
> R17 交付见 `docs/ROUND_R17_20260912.md`（几何重连兜底，1809 → 1810）。

## 1. R18 起点基线

| 口径 | 实测 |
|---|---|
| 官方库导入 | **4/6 精确**；SampleModel4 2/143（未重建 35）；samplemodel2 37/**1810**（SAB 1813，−3） |
| 切分 | 0（模型级） |
| 残量 | samplemodel2：1 个未重建面 + 3 个被丢弃碎片；SampleModel4：35 个未重建面 |
| 漂移 | 5/6 为 0.0%、samplemodel2 0.6% |
| 性能 | 导入 0.07–6.88 s |

## 2. R18 工作项（P105–P110）

### P105 · 补齐探针并吃掉最后 1 面 + 3 碎片（最高优先）

- **动作**：①把环探针改成**打印整环**（含末条协边，纪律 32）；②对仍不闭合的环检查是“缺口大于 2% 环对角”还是“存在多条分叉”，
  必要时把 `_walk_gap` 的容差改成“按缺口分布自适应”或加“允许一次跳接”；
  ③3 个被丢弃碎片按“所属面 + 超出轴向 + 超出量”打印后定点处理（P100 的落地）。
- **验收**：samplemodel2 **1813 ± 0**、漂移 **0.0%**。
- **成本**：1–1.5 天。

### P106 · SampleModel4 的 35 个未重建面（`ref` 引用表）

- **验收**：`tools/import_fidelity.py --strict` 退出 0。**成本**：2–3 天（高风险）。

### P107 · 官方层级交互（积压六轮）｜P108 · 大模型 GUI（积压六轮）｜P109 · 特征族/梁焊件

### P110 · 每轮维护（必做）：快照/CI/口径同步；编号递增；关键功能完成即提交推送。

## 3. 建议顺序

`P105 -> P107 -> P108 -> P106 -> P109`（P110 收尾）

## 4. 纪律（延续 + R18 新增）

1–33 条延续。
34. **新增**：容差型兜底上线后，**必须**记录“触发面数 / 新增成功面数 / 丢弃碎片变化”三项计数（本轮：触发后 +1 面、碎片 4→3），
    否则无法判断兜底是“救回了面”还是“只是把错误几何放行”。