# 改进规划 R106（2026-09-12 立，R105 草图/三维边界之后）

> 上轮：R105 交付草图/三维模式边界（编辑草图页签、模式 chip、三维命令自动越界、草图→实体一步、
> 活动草图与悬空平面检测），见 `docs/ROUND_R105_20260912.md`。
> 原本排在 R105 的装配层级等条目顺延到本文件 A 组（R105 被用户反馈的模式边界占用）。

---

## A. 顺延项（原 R105 计划，按 ROI）

| # | 项 | 现状证据 | 验收（可复算） |
| --- | --- | --- | --- |
| A-1 | **装配层级 / 局部坐标系** | 配合把 B 摆到 A 的当前帧后即结束；改 A 不带动 B | 两级链 A→B→C：移动 A 10mm，B、C 按链式矩阵更新（坐标可复算到 1e-12）；重放后体积不变 |
| A-2 | **配合驱动量的多帧求解** | 角度/滑移/螺距只在求解那一刻生效 | 螺旋副转 90°：位移 = pitch·(θ/2π) 闭式；圆柱滑移 = 输入值；帧序列可回放 |
| A-3 | **组件移动后重新求解** | `asm.move` 平移组件不触发配合重解 | 移动基座后 `solve_mates`：从动件位姿 = 重新求解的解 |
| A-4 | **脚本层文档级特征** | `op_combine`/`op_split`/`op_mirror` 不写 `record_doc_feature` | 脚本跑完 `document_features.ops()` 与 GUI 同序列；`replay_document(apply=True)` 后体积 = 原值 |
| A-5 | **handler 端到端（QTimer）** | 模态 `exec_` 循环只由冒烟测试覆盖 | 驱动 `det.params` / 树双击 / `asm.mate` 三个 handler，各断言一次闭式结果 |
| A-6 | **爆炸图走可回放平移** | `asm.explode` 用 `K.translate` 直接改形状 → 爆炸后带历史的实体被拒编辑 | 爆炸后 `can_replay` 真；还原后包围盒回原位；回调改 `translate_body` 并同步 3 处调用点/2 个测试 |

## B. R105 新暴露的候选——**B-1 已交付（R106）**

> B-1（草图特征化）见 [ROUND_R106_20260912.md](ROUND_R106_20260912.md)：改轮廓/改高度都重建实体，定义随特征持久化，
> 同步失败回滚。B-2..B-5 与 A 组顺延项一并转 [IMPROVEMENT_PLAN_R107.md](IMPROVEMENT_PLAN_R107.md)。

| # | 项 | 现状证据 | 验收（可复算） |
| --- | --- | --- | --- |
| B-1 | **草图特征化** | 拉伸出的实体不记录来源草图（草图不是 feature），改草图不会重建实体 | 在草图模式下改矩形尺寸 → 拉伸体体积 = `w·h·t` 新值（闭式）；`FeatureStack.ops()` 含 `sketch` |
| B-2 | **精确圆/样条截面拉伸** | 圆拉伸体积 1.2546e-07 vs 闭式 1.2566e-07（多边形近似 0.16%） | 圆拉伸体积 = `πr²h`（rel 1e-6）；样条截面面数可数 |
| B-3 | **草图模式进撤销栈** | 模式切换/自动越界不计入 undo | 自动越界后一次 undo → 回到草图模式且活动草图恢复；redo 再回三维 |
| B-4 | **活动草图持久化** | `_sketch_session` 是内存态 | 存盘重开后 `resolve_active` 仍返回同一草图（工程清单记 `active_sketch`） |
| B-5 | **草图专属命令** | 草图页签复用 Design 页的 id（去重后命令数不变） | 新增 `sketch.mirror` / `sketch.pattern`：草图模式下可用、三维模式下 refuse；各带闭式验收 |

## C. 其他顺延项（R104 起累积）

1. **数字单一来源**：`gen_devplan_snapshot.py` 与 `state_inventory.py` 交叉校验（纪律 84）。
2. **配置语义扩展**：替换件 / 驱动尺寸 / 配置对比；BOM 行数与质量 = 快照值。
3. **工程图标注补齐**：孔标注 / 中心线 / 粗糙度 / 剖面线；DXF 与 SVG 实体计数 = 闭式条数。
4. **钣金折弯表 + 展开图工程化**：`ΔBA = θ/2·(t·K₂ − t·K₁)` 闭式；DXF 折弯线数 = 折弯数。
5. **未封闭度**：SampleModel4 335/72 → 缺口 < 200、环 < 40；samplemodel2 990/195 → 缺口 < 700。
6. **导入峰值内存**：768 MB（负载中）目标 −30%，否则否证表。
7. **P2 整域**：去毛刺 / 参考图像+标定 / 用户图层 / 3D PMI（边界）/ 型材库+尺寸链第四批 / `scdm_gui.py` 拆分。

## D. 明确不做（沿用）

ref 族 27 面（三条否证）、字节恒等、CATIA/Parasolid 直读、ACIS bit 等价、求解链、KeyShot 材质库、
3D PMI 读取（NYI-1..6）、`safety.tab` 空壳（按设计保留）。

## E. 口径（R106 起沿用）

1. **模式边界**：草图侧命令 = `sketch.*` / `con.*` / `create.project`；中性 = 视图/显示/编辑/测量/文件/工具；
   其余是三维侧——三维命令在草图模式下**先越界再执行**（`sketchmode.plan()` 是唯一判据）。
2. **命令面计数**：ribbon 命令**去重**计数（一条命令两个按钮算一条），按钮数另报；新增命令必须进 live 并有 handler 或台账声明。
3. 每轮结束照旧：定向 + 全量回归 + 快照 `--check`（两种解释器）+ 同轮文档 + 提交推送。
