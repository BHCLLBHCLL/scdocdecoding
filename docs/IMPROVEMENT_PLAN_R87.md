# 改进规划 R87（2026-09-12 立，R86 收尾后重新基线）

> 规则：每完成一轮改进重新基线并产出**下一编号**规划——本文件是 R87。
> R86 交付见 `docs/ROUND_R86_20260912.md`（锥面裁剪＝原生崩溃 → 曲面类型守卫 + 回归测试）。

## 1. R87 起点基线（R86 实测）

| 口径 | 实测 |
|---|---|
| 真缺口 / 自由环 | SampleModel1 **0/0**、SampleModel4 347/76、samplemodel2 **1002/212**、samplemodel3 0/0、samplemodel5 0/0、samplemodel6 3/2 |
| 裁剪曲面白名单 | 柱面、球面（锥面＝崩溃、环面＝0.54 s/面且仅 16/36） |
| 面数保真 | 5/6 样例完全一致；SampleModel4 149/177（ref 25/27） |
| 导入耗时 | 1.45 / 1.63 / 7.00 / 0.08 / 2.52 / 0.10 s |
| 测试 | 全量 CI 口径全绿（**648 passed, 2 skipped, 8 deselected**）；`def test_` 扫描 631 条 |

## 2. R87 工作项（择优取一）

### P415 · 产品面：装配爆炸图第二批（沿轴/径向 + 步进参数）

- **动作**：爆炸方向由单一比例升级为可选**沿轴/径向**，并支持步进与每体位移可数；八层接线（op / 命令 / 回放 / 视口标记）。
- **验收**：位移向量可数（每体一组）、撤销可还原、命令测试 + 回放测试就位。**成本**：1 天。

### P416 · 型材库标准规格表（槽钢/工字钢/角钢）

- **动作**：`scdm/beams.py` 增加标准规格表（按 GB/H 型命名），参数与闭式截面积/惯性矩对拍。
- **验收**：规格表每项都有可复算的截面属性（与闭式公式比 ≤1e-9）。**成本**：0.5–1 天。

### P417 · 每轮维护（必做）｜P418 · 后续候选

- P417 快照/CI/口径同步 + 编号递增 + 轮次记录同轮补齐（纪律 65）；
- P418 候选：环面裁剪的廉价闸门、图面尺寸链第三批、`prep.small` 重定向、ref 27 面。

## 3. 建议顺序

`P416（快）→ P415（产品面）→ P417（收尾）`

## 4. 纪律（延续）

1–89 条延续。本轮的教训可归纳为一句：**原生崩溃不能靠 except，只能靠不调用**——
凡是要在既有几何上试新路径，先加"类型/规模"闸门，再谈收益。
