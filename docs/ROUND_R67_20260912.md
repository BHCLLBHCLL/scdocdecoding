# R67 轮次交付记录（2026-09-12）

> 规则：每完成一轮改进即重新基线并产出**下一编号**的规划。本轮（R67）执行 P347：**焊接符号元数据面板**。
> 交付：`weld.symbol` 从**占位转为 live**（占位 3 → 2）、符号挂到焊件成员、`.scdm` 往返、命令路径可测。

## 1. 交付内容

| 层 | 位置 | 内容 |
|---|---|---|
| 模型（R53 已有） | `scdm/beams.py` | `WeldSymbol`（类型/尺寸/长度/间距/成员/位置）+ 校验 + `to_dict/from_dict`；`Weldment.add_symbol()` 校验成员存在 |
| 入口 | `scdm/catalog.py` | `weld.symbol` 移入 M5 live 集合；note 由"占位…"改为"挂到焊件成员的符号元数据（类型/尺寸/长度/间距）" |
| 命令 | `scdm_gui.py` | `_do_weld_symbol`：无焊件组元 → 明确提示；多组元 → 选组元；选成员 → 选类型 → 尺寸/长度/间距 → `add_symbol` → `populate_tree` → `_record` → 状态栏报"该组元共 n 个" |
| 覆盖台账 | `tests/test_coverage_ledger.py` | 移除 `weld.symbol` 的占位豁免（命令现在有行为测试，台账要求"无陈旧条目"） |
| 快照 | `DEV_PLAN.md` | 占位从 **3 条降到 2 条**（`prep.small`、`safety.tab`），live 174 → 175 |

## 2. 可数验收（实测）

| 项 | 证据 |
|---|---|
| 符号往返 | `.scdm` 存读后 `symbols[0].to_dict()` 与写入前**逐字段相等**（含 member/pitch） |
| 指向不存在成员 | `add_symbol(member="M9")`、未知类型 `laser`、空组元 → 均报错（3 类） |
| 面板路径 | GUI 用例：无焊件 → 提示"没有焊件组元"；有焊件 → 一条命令写入 `M1/fillet/6×30×100` 且状态栏含"焊接符号" |
| 占位退休 | 目录扫描：`placeholders == ["prep.small", "safety.tab"]`（测试直接断言这个**可数集合**） |

## 3. 测试

| 文件 | 项 | 内容 |
|---|---|---|
| `tests/test_weld_symbol_p347.py` | 3 | ①命令 live + note 去占位 + **占位集合恰为 2 条**；②符号随焊件往返（逐字段）+ 计数；③三类非法 + 合法符号挂上并可计数 |
| `tests/test_lightweight_gui.py` | +1 | `_do_weld_symbol`：空状态提示、写入成员符号、状态栏报数 |
| 既有 | 38 项 | 台账/焊件（p291）/GUI 合跑 41 passed, 1 skipped；另更新 R51 的 @@test_p283_beam_is_registered_labelled_and_live@@：它当年钉的是"weld.symbol **非** live"，本轮改为守卫**已转 live**（钉子随事实更新，而不是删掉） |

全量（CI 口径 `-m "not official"`）**579 passed, 2 skipped, 8 deselected**；`def test_` 扫描 564 条（快照：18 页签 / 161 命令 / **175 live** / **占位 2**）。

## 4. 下一步（写入 R68）

P348 仿真报告第二批（载荷/支撑/接触清单 + 与网格统计联动 + 报告导出 JSON，字段与模型一致可数）→
P349 可选补测（内存/帧率，已顺延多轮）→ P350 维护；
纪律延续（1–83），本轮无新增纪律——本轮是"把已有模型接到入口"的接线轮，价值在占位退休与命令可测，不在新判据。
