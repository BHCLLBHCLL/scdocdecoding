# -*- coding: utf-8 -*-
# scdm_cases case script (SpaceClaim 2019 R3, RunScript). Generated by _gen\gen_cases.py from _gen\specs\B10_beam_sheetmetal.py.
# Batch: loaded by _framework\scdm_fw.py run_case_files(). Standalone: SpaceClaim.exe /RunScript="<this file>" /ExitAfterScript=True /Headless=True
CASE = {'case_id': '09_beam_create_003_orient30_centroid',
 'category': '09_beam',
 'commands': ['Beam.Create(sel curves, profile)',
              'Beam.SetOrientation/SetPosition/SetType/ReverseBeam',
              'Beam.ExtractProfile'],
 'expect': {'beams': 1},
 'feature': 'beam_create',
 'inventory_expect': None,
 'out_dir': 'D:\\training\\caedecoder\\scdm_cases\\09_beam',
 'params': {'anchor': 'Centroid', 'orientation_deg': 30},
 'priority': 'P1',
 'title': 'Beam along curve: 003_orient30_centroid'}


def build(ctx):
    def nn(x):
        if x is None:
            raise Exception("None")
        return x
    def profile_part(ctx, r):
        ctx.data["profile_result_members"] = _safe(lambda: [n for n in dir(r) if not n.startswith("_")][:40])
        comps = list(GetRootPart().Components)
        return first_ok(ctx, "profile_part", [lambda: nn(_safe(lambda: r.CreatedProfile)), lambda: nn(comps[-1].Template),
                                              lambda: nn(master(comps[-1]).Template)])
    def make_beams(ctx, prof, curves):
        BM = rawt("Beam")
        out = []
        for c in curves:
            out.append(first_ok(ctx, "beam", [lambda: raw("beam", lambda: BM.Create(prof, master(c))),
                                              lambda: raw("beam", lambda: BM.Create(prof, c)),
                                              lambda: Beam.Create(sel(c), prof)]))
        return out

    dcurve(seg((0, 0, 0), (1000, 0, 0)), "Beam_Line_1000")
    prof = profile_part(ctx, BeamProfile.CreateI(MM(50), MM(100), MM(8), MM(5), "I_100x50"))
    bm = make_beams(ctx, prof, curves_now())
    first_ok(ctx, "orientation", [lambda: Beam.SetOrientation(sel(bm), DEG(30))])
    first_ok(ctx, "position", [lambda: Beam.SetPosition(sel(bm), Beam.AnchorPosition.Centroid),
                               lambda: [setattr(x, "AnchorPosition", enum_pick(type(x.AnchorPosition), "Centroid")) for x in bm]])
    ctx.data["beam_state"] = _safe(lambda: [[unicode(x.Orientation), unicode(x.AnchorPosition)] for x in bm])

if "SCDM_BATCH" not in globals():
    execfile(r"D:\training\caedecoder\scdm_cases\_framework\scdm_fw.py")
    run_standalone(CASE, build, r"D:\training\caedecoder\scdm_cases\09_beam\09_beam_create_003_orient30_centroid.py")
