# -*- coding: utf-8 -*-
# scdm_cases case script (SpaceClaim 2019 R3, RunScript). Generated by _gen\gen_cases.py from _gen\specs\B11_repair_prepare.py.
# Batch: loaded by _framework\scdm_fw.py run_case_files(). Standalone: SpaceClaim.exe /RunScript="<this file>" /ExitAfterScript=True /Headless=True
CASE = {'case_id': '12_prepare_workpiece_001',
 'category': '12_repair_prepare',
 'commands': ['VolumeExtract.Create', 'Enclosure.Create', 'Workpiece.Create', 'Shrinkwrap.Create'],
 'expect': {},
 'feature': 'rp_volume_extract',
 'inventory_expect': None,
 'out_dir': 'D:\\training\\caedecoder\\scdm_cases\\12_repair_prepare',
 'params': {},
 'priority': 'P2',
 'title': 'Volume extract / enclosure / workpiece / shrinkwrap: 001'}


def build(ctx):
    b = block((0, 0, 0), (40, 30, 20), "Part_With_Pocket")
    cut = block((10, 10, 10), (30, 20, 25), "Pocket_Cutter")
    subtract(ctx, b, cut)
    first_ok(ctx, "workpiece", [lambda: Workpiece.Create(sel(largest()), WorkpieceOptions()), lambda: Workpiece.Create(sel(largest()))])
    ctx.data["bodies_after"] = [[unicode(x.Name), round(vol(x) or 0, 1)] for x in all_bodies()]
    intent_range("bodies", len(all_bodies()), 2, 3)

if "SCDM_BATCH" not in globals():
    execfile(r"D:\training\caedecoder\scdm_cases\_framework\scdm_fw.py")
    run_standalone(CASE, build, r"D:\training\caedecoder\scdm_cases\12_repair_prepare\12_prepare_workpiece_001.py")
