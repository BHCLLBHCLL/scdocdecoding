# -*- coding: utf-8 -*-
# scdm_cases case script (SpaceClaim 2019 R3, RunScript). Generated by _gen\gen_cases.py from _gen\specs\B11_repair_prepare.py.
# Batch: loaded by _framework\scdm_fw.py run_case_files(). Standalone: SpaceClaim.exe /RunScript="<this file>" /ExitAfterScript=True /Headless=True
CASE = {'case_id': '12_prepare_enclosure_001_box_cushion10',
 'category': '12_repair_prepare',
 'commands': ['VolumeExtract.Create', 'Enclosure.Create', 'Workpiece.Create', 'Shrinkwrap.Create'],
 'expect': {'bodies': 2},
 'feature': 'rp_volume_extract',
 'inventory_expect': None,
 'out_dir': 'D:\\training\\caedecoder\\scdm_cases\\12_repair_prepare',
 'params': {'cushion_mm': 10},
 'priority': 'P2',
 'title': 'Volume extract / enclosure / workpiece / shrinkwrap: 001_box_cushion10'}


def build(ctx):
    b = block((0, 0, 0), (40, 30, 20), "Enclosed_Block")
    o = EnclosureOptions()
    setp(ctx, o, [("EnclosureType", _safe(lambda: EnclosureType.Box))], "encl_opts")
    def by_obj():
        e = Enclosure(sel(b), o)
        c = e.EnclosureCushion
        ctx.data["cushion_type"] = unicode(_safe(lambda: c.GetType().FullName))
        mem = [n for n in dir(c) if not n.startswith("_")]
        ctx.data["cushion_members"] = mem
        done = []
        for n in mem:
            try:
                if isinstance(getattr(c, n), float):
                    setattr(c, n, MM(10))
                    done.append(n)
            except Exception:
                pass
        ctx.data["cushion_set"] = done
        _safe(lambda: setattr(e, "EnclosureCushion", c))
        return e.Execute()
    first_ok(ctx, "enclosure", [by_obj, lambda: Enclosure.Create(sel(b), o)])
    bbs = [bbox(x) for x in all_bodies()]
    ctx.data["bboxes"] = bbs
    intent_range("enclosure_x_span_mm", max(x[1][0] - x[0][0] for x in bbs), 40.001, 200)

if "SCDM_BATCH" not in globals():
    execfile(r"D:\training\caedecoder\scdm_cases\_framework\scdm_fw.py")
    run_standalone(CASE, build, r"D:\training\caedecoder\scdm_cases\12_repair_prepare\12_prepare_enclosure_001_box_cushion10.py")
