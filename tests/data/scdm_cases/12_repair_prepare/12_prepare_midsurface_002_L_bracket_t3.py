# -*- coding: utf-8 -*-
# scdm_cases case script (SpaceClaim 2019 R3, RunScript). Generated by _gen\gen_cases.py from _gen\specs\R2_fixes.py.
# Batch: loaded by _framework\scdm_fw.py run_case_files(). Standalone: SpaceClaim.exe /RunScript="<this file>" /ExitAfterScript=True /Headless=True
CASE = {'case_id': '12_prepare_midsurface_002_L_bracket_t3',
 'category': '12_repair_prepare',
 'commands': ['Midsurface.Convert(bodies, thickness)',
              'MidsurfaceOffsetFaces.Create',
              'MidsurfaceAspectExtensions.SetThickness'],
 'expect': {},
 'feature': 'rp_midsurface',
 'inventory_expect': None,
 'notes': 'fix R2: Midsurface by thickness range 2..4 mm on the merged L solid, then explicit face pairs, '
          'then Convert; each form checked',
 'out_dir': 'D:\\training\\caedecoder\\scdm_cases\\12_repair_prepare',
 'params': {},
 'priority': 'P1',
 'title': 'Midsurface: 002_L_bracket_t3'}


def build(ctx):
    def chk_mid():
        if not any(is_sheet(x) for x in all_bodies()):
            raise Exception("no midsurface sheet created")
    def mk_mid():
        try:
            return Midsurface(MidsurfaceOptions())
        except Exception:
            return Midsurface()
    def mid_pairs(pairs):
        m = mk_mid()
        for f1, f2 in pairs:
            m.AddMatchingFacePairs(f1, f2)
        m.Execute()
        chk_mid()
    def mid_range(s, lo, hi):
        m = mk_mid()
        m.AddFacePairsByRange(s, MM(lo), MM(hi))
        m.Execute()
        chk_mid()
    def mid_conv(s, t):
        Midsurface.Convert(s, MM(t))
        chk_mid()

    a = block((0, 0, 0), (60, 40, 3), "L_Bracket")
    w = block((0, 0, 3), (3, 40, 40), "L_Wall")
    Combine.Merge(sel(a, w))
    b = largest()
    set_name(b, "L_Bracket")
    first_ok(ctx, "midsurface", [lambda: mid_range(sel(b), 2, 4),
                                 lambda: mid_pairs([(face_extreme(b, (0, 0, 1), -1), [f for f in b.Faces if gtype(f) == "Plane" and abs(bbox(f)[0][2] - 3) < 1e-6 and abs(bbox(f)[1][2] - 3) < 1e-6][0]),
                                                    (face_extreme(b, (1, 0, 0), -1), [f for f in b.Faces if gtype(f) == "Plane" and abs(bbox(f)[0][0] - 3) < 1e-6 and abs(bbox(f)[1][0] - 3) < 1e-6][0])]),
                                 lambda: mid_conv(sel(b), 3)])
    ctx.data["bodies_after"] = [[unicode(x.Name), is_sheet(x)] for x in all_bodies()]
    intent("has_sheet", any(is_sheet(x) for x in all_bodies()), True)

if "SCDM_BATCH" not in globals():
    execfile(r"D:\training\caedecoder\scdm_cases\_framework\scdm_fw.py")
    run_standalone(CASE, build, r"D:\training\caedecoder\scdm_cases\12_repair_prepare\12_prepare_midsurface_002_L_bracket_t3.py")
