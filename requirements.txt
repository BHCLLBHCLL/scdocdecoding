# scdocdecoding / SCDM-compatible CAD kernel
# Proven environment (2026-09-06 full-suite run: 212 passed / 1 skipped):
#   conda env create -f environment.yml && conda activate scdm
#   python -m pytest -m "not official"
# The geometry kernel is conda-forge only (not pip-installable).
pytest>=7
numpy>=2

# geometry kernel: conda-forge pythonocc-core==7.9.3  (see environment.yml)
# GUI (optional): PyQt5==5.15.11, vtk==9.6.1              (see environment.yml)
